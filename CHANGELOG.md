# FORGE 変更履歴

**このファイルは過去の記録。** 現在地は `HANDOFF.md`、未完了は `BACKLOG.md`、
設計理由は `README.md` を見る。

ここに書いた件数・バージョンは**その時点の事実**であって、今の値ではない。
最新のテスト件数は `npm test` の出力、配信中の版は `pwa/sw.js` の `VERSION` が正。

新しい記録を上に足す。古い記録は書き換えない（当時そう判断した記録なので、
あとから直すと「なぜそうしたか」が読めなくなる）。

---

## forge-v120（2026-08-20・到達した段に留まれるようにする）

v119で入れた101〜103%帯が**隔週でしか出なかった**。段の希望値が「前回実施した段」
から始まる一方、同じ形式には14日のペナルティ（−4）がかかる。その帯のレシピは
1本しかないので、1回やると次は避けられて下の段が選ばれ、希望値も下がる。
上がっては落ちるを繰り返していた。v119の副作用。

- 段のずれの重みを ×2 → ×6 にした。ローテーションのために段を下げない
- 希望する段に選択肢が1つしか無いときは、繰り返しのペナルティを効かせない
  （あの規則は入れ替えられる相手がいる前提のもの）
- 無い段を希望しない。候補にある段へ丸める

「到達した最高段」を起点にする案は、崩れたあとの復帰が速すぎるので採らなかった。
他の2つで足りることを検査でも確認している（この変更だけ戻しても落ちなかった）。

---

## forge-v119（2026-08-20・レースペース経済走に101〜103%帯を足す）

設定ペースのGRP比に空白があった。高乳酸95〜97%、モデリング99〜100%（分割走）、
経済走104〜106%で、**101〜103%を連続走で走る枠が無かった**。
対象選手の制限因子は「後半の維持＝レースペース経済性」（`limiter.ts`）なので、
いちばん鍛えたい帯が抜けていたことになる。目標1:48.9の600mで 82.5〜84.2秒。

判断は BACKLOG F-1 に4点まとめた。要点だけ。

- **新カテゴリは作らない。** 刺激の性質は104〜106%と同じで濃さが違うだけ。
  カテゴリを分けると制限因子の振り替え（M-7）の対象から外れる実害もある。
- **`specificPace` の床1.04は下げない。** あれは導入の到達点であって経済走の下限ではない。
  代わりにレシピが `grpRatio` を持てるようにした。指定が無ければ従来どおりなので
  既存の処方は1つも変わらない。
- **追加ではなく置換。** 同じカテゴリの別レシピなので週の高負荷回数は増えない。
  `progressionStage` を既存より1つ上に置き、104〜106%を2回きちんと実施してから
  進むようにした。カレンダーが進んだだけでは濃くならない。
- **`recoveryDays` は3→4。** 104〜106%より深く、高乳酸・モデリング（5）より浅い。

距離の漸進（400→500→600を階段にするか）は選択の意味が変わるので BACKLOG F-2 に切った。

---

## forge-v118a（2026-08-20・verifyにカバレッジを含める）

`npm run verify` がカバレッジを見ていなかったため、閾値のラチェットが実質機能して
いなかった。実際、v116・v117 は periodization の branches が閾値を割ったまま配信して
いる（89.92% < 90%）。原因は到達しない分岐を足したことで、v118 で回復した。

`npm test` を `npm run test:coverage` に置き換えた。同じテスト実行に計測を足しただけ
なので二重には走らない。閾値をわざと割る値にして、**verify が exit 1 で止まること**を
確認済み（止まらなければ入れた意味がない）。

---

## forge-v118（2026-08-20・レストの下限を2種類に分ける）

`MIN_REST_SEC = 90` をレストを持つ全部の処方に一律で当てていたため、
**下限が元のレストを上回る**ケースがあった。モデリング核（500m＋300m・レスト60秒）は
密度を上げる週に `max(90, 60×0.8=48) = 90` となり、**60秒が90秒へ伸びていた**。
理由には「レストを20%詰めます」と出る。表示と実際が逆で、しかも起きるのは
レース再現性が最も高い練習だった。進め方の2案（tighten・hold）でも同じことが起きていた。

原因は2つの概念を1つの定数で扱っていたこと。反復のレスト（完全回復してから次を走る）と、
分割走のつなぎ（レースの一部を再現する時間＝処方そのもの）は別物。
**`MIN_REST_SEC` の値は変えていない。**

- `restFloorSec()` を新設し、下限が元のレストを上回らないようにした
- モデリングを密度操作の対象から外した。週を追って変わるのは設定タイムだけになる
- 進め方の2案も、モデリングでは「この形のまま実施する」の1案にした
- 詰まっていないのに「詰めます」と書かないようにした

高乳酸・経済走の挙動は変わらない（変わっていないことをテストで固定）。

---

## forge-v117（2026-08-20・テーパーの境界を2か所で食い違わせない）

テーパーは生成（`periodization`）と差分案（`taper.planTaper`）の2か所が別々に境界を
持っている。同じ日に2つの答えが出ていないか、残0〜16日の表を作って確かめた（BACKLOG E-6）。
4つ食い違っていた。

- **同じ日を2回削っていた。** 3日前は生成器が「調整ジョグ20分」に決めているのに、
  そこへ段階の比率（0.4）が重なって15分の案が出ていた。生成器が日ごとに中身を
  決めている3日前以降は、差分案を重ねない。14〜7日前は生成器が比率を持たないので残す。
- **レース当日が「レース前日」と表示されていた。** `taperStage` が残0日でも `eve` を
  返していた。当日は段階を持たない。
- **14日前がフェーズと段階で食い違っていた。** 段階は t14（テーパー開始）なのに
  フェーズはModelingだった。フェーズもテーパー初日から始める。
- **7日前だけ「高負荷を置かない」保険が効いていなかった。** 条件が `< 7` だったため。
  8日前に最終高乳酸を置く設計なので、7日前は置かない側でそろえる。

境界テストは、壊すと4件とも落ちることを確認している。7日前の検査は最初
空振りしていた（通常の生成では7日前に高負荷が来ない／高乳酸だと最短5日間隔の規則が
先に効く）ので、曜日に経済走を指定する経路で測り直した。

---

## forge-v116（2026-08-20・曜日を固定してもレース再現が消えないようにする）

**水曜・木曜に予定を作り直すと、ブロックが終わるまでレース再現が1本も入らなかった。**
原因は2つあり、どちらも「週に高負荷は2日まで」の枠の使い方だった。

1. 枠が足りないときの選び方が「先に来た日が勝つ」だった。
   曜日を固定してポイントを2日にしても、フェーズのテンプレートは指定外の日にも
   経済走を置くので候補が週3日になり、毎週どれか1日が落ちる。
   どれが落ちるかは**予定を作り始めた曜日**で決まり、水・木から作ると
   週の1本目（高乳酸・レース再現）が落ち、落ちた状態が翌週の条件になって戻らなかった。
   → 残す優先順位（レース再現 > 高乳酸 > 経済走、同順位なら指定した曜日を残す）で選ぶ。
2. 制限因子の振り替え（M-7）が、CVに落とした枠を経済走に戻して3日目を作り直していた。
   判断が後ろ向き7日間しか見ておらず、週の前半では枠が空いて見えていた。
   → その週に置くと決めた枠数も見る。

**`isFixed` が2つの意味を兼ねていたのも直した。**
チーム練習など本人が登録した「動かせない予定」と、曜日・周期の設定で置いた枠を
同じ印で扱っていたため、火・土をポイントに固定すると、その2日が設定ペースの調整からも
進め方の2案からも外れ、**調整できるポイント練習が1本も無い週**ができていた。
本人が固定したのは曜日であって中身ではないので、`isContentLocked` で分けた。
日付を動かせないことは変えていない。

- 進め方の2案で「案が無ければ次のポイント練習へ移す」フォールバックが、
  `/api/adaptive` を同じ引数で呼び直していたため**一度も移れていなかった**。
  候補を順に当たるようにし、移す直前の再描画で自分の探索が止まっていたのも直した。
- E2Eの不備を3件直した。秒表記の処方しか見ていなかった箇所が2つ（km表記だと
  落ちる・例外になる）と、**落ちてもOKと表示していた**締め。

---

## forge-v115（2026-08-18・アップのペースと流しの実測入力）

- iPhoneの数字キーボードにコロンが無くても入力できるよう、ジョグ系のペースを
  「分」「秒」の2欄に分け、画面上で `分:秒/km` と読める形にした。
- 流し・加速走・短い刺激は1kmペースへ読み替えず、本数ぶんの実測タイムを秒で入力する。
  小数秒と未計測の本を区別して保存し、入力済みの各本だけをアップ合計時間へ足す。
- iPhone幅のE2Eで数字キーボード指定、単位表示、本数と入力欄の一致、合計時間への反映を固定した。
- E2Eの検査IDが実装と食い違っていた（`warmup-pace-min-0` と `warmup-pace-0-min`）ため、
  実装が正しくても必ず落ちる状態だった。実装側の命名（親の下に `-min`/`-sec`）へ揃えた。
- 「複合の欄」の検査が失敗したとき、**何があったのか**を出すようにした。
  以前は「モデリングの予定が無い」としか言わず、生成の形が変わったのか・
  先の検査が完了済みにしたのか・日付で外れたのかを区別できなかった。
  実際にこれで原因を特定した（BACKLOG E-8）。予定生成側の修正は範囲外として分離した。

---

## forge-v114（2026-08-18・能力換算の適用範囲と文書整理）

- 片側だけ距離補正した場合の数値テストを、実装の回帰検査ではなく測定値の記録だと
  分かる名前とコメントへ修正。実装を見張るのは `impliedFromInterval` を通す不変条件テストだと明記。
- A-2cに、必要データ、探索用5組＋独立検証3組の根拠、条件を揃えた比較、
  本番から隔離したシャドー計算、方針Bを見直す判定条件を追記。標準距離と指数は未決定のまま。
- E-4〜E-7をコードへ再照合し、影響範囲と重大度を追記。回復間隔は目的の違う判定を
  一律統合しない形へ、テーパーは境界が整合済みという未確認の記述を削除して訂正。

- 設定ペース用の `GRP_RATIOS` は neural を残し、能力換算用の
  `ABILITY_CONVERSION_RATIOS` を分離。神経系は800m能力へ換算しない
- `impliedFromInterval` のCV/閾値に対する到達不能な信頼度分岐を削除。
  カバレッジは全件再計測し、上がった指標の下限だけを上げた
- 200/300/400/600/800mで「設定どおりなら implied800 = CFE」を固定。
  換算側だけに標準400m補正を入れると落ちることも壊して確認した
- 距離補正は根拠が揃うまで処方・換算とも変えないと判断。未検証課題は
  `BACKLOG.md` だけに記録した
- READMEの旧時系列部分をCHANGELOGへ移し、設計根拠を主題別に再配置。
  READMEの先頭から能力推定・生成・入力・保存・UI・運用を引けるようにした

---

## forge-v113（2026-08-18・処方文を形の欄から分離）

処方の本文がカレンダーの「形」に入り行が崩れていた。構造化した距離・本数と
自由記述を分け、同じ形式化をカレンダー、ホーム、分析で共有した。

---

## forge-v112（2026-08-18・神経系とLTフォールバック）

- 流し・レペの設定基準を目標800mから400mPBへ移し、用途別の比率に分けた
- 400mPBが無い場合は旧来の800m基準へフォールバックし、推定値と明示する
- LT推定はマーカー実測、1500mPB、800mPBの順に使い、根拠と精度を表示する

---

## forge-v111（2026-08-18・予定と違う量）

「30分ジョグの予定を50分にしたら一目で分かるようにしたい」という指摘。
設計理由は `README.md`「予定と違う量をやった日を一目で分かるようにする」を参照。

- `actualDiffersFromPlan` が種目の食い違いしか見ていなかった。**量の食い違い**も見る
- 割合15%かつ最低5分/1kmの両方を満たしたときだけ（普通の揺れに印を付けない）
- インターバルの本数は数えない（「中止 2/4本」の印と二重にしない）
- カレンダーは**予定と実際を並べて**出す（予定側に取り消し線）

---

## forge-v110（2026-08-18・アップ欄の入力）

利用者からの指摘4件。設計理由は `README.md`「アップ欄の入力を直す」を参照。

1. **押せるのか分からない** → 枠・開閉のしるし・aria-expanded
2. **2.199999999 になる** → `stepperNext()` が刻みの桁で丸める（値そのものを丸める）
3. **小数点を打てない** → `pattern` を添える（iOSは inputMode だけだとテンキーになる）
4. **ペースから合計を計算** → 区間に paceSecPerKm を持たせ、距離×本数×ペースで時間を出す

手で入れた合計は計算で上書きしない。どちらを使っているかを常に画面に出す。

反省: このE2Eは**一度、実装が正しいのに落ちた**。
合計を手で触ってから（計算をやめる状態にしてから）ペースを入れていた。
検査の順番が仕様と噛み合っていないと、正しい実装を落とす。

---

## forge-v109（2026-08-17・靴の用途を複数）

設計理由は `README.md`「靴の用途を複数選べるようにした」を参照。

- `ShoeProfile.purpose`（単数）→ `purposes`（配列）。**古い単数データも読む**
- 「決めていない」は他と併用しない（`normalizePurposes`）
- 1つでも噛み合えば加点、ただし**加点は1回だけ**（選ぶほど有利にしない）

あわせて、**曜日によって落ちるE2E**を3件直した（今日が固定曜日に当たる火・土は必ず赤だった）。

- S-9: 固定枠は変更が断られるのが正しい。断りの理由が出ることも見る
- 処方と欄の一致: 画面で選べるカテゴリに対象を限る（有酸素だとフォームが開かない）
- ホーム: 今日の枠は固定枠だと**消せも変えられもしない**（isFixed は予定に保存された値）。
  日付に依らない範囲だけを見るようにし、**未実施の部分をstepの文に書く**

反省: 「固定枠が原因」と決めつけて曜日の固定を外し、それに依存する検査を5件落とした。
1回の実行で3件まとめてプローブすれば、推測での往復を減らせた。

---

## forge-v108（2026-08-17・主練習の時間帯）

主練習が午後で固定だったのを、曜日ごとに選べるようにした。
設計理由は `README.md`「主練習の時間帯を選べるようにした」を参照。

- `weekTemplate.ts` に `mainTimeOfDay` を足した（未設定は午後）。**枠の中身は動かさない**
- 補助枠は主練習の反対側に置く（同じ時間帯に2本入るとidが衝突して片方消える）
- 検証の文を主/補助の言い方にした（午前に主練習を置いた日に逆さまになっていた）
- メニュー設定に曜日ごとの「主練習 午前／午後」を足した

反省: E2Eが**一度空振り**した。前のブロックが周期モードにしたままで、
行が「1日目」になっていて「火曜」の選択が見つからなかった。
検査したい状態は検査の側で作る（曜日モードに戻してから見る）。

---

## forge-v107（2026-08-17・3秒で読める画面）

利用者からの指摘「メニュー名だけでなく設定タイムまで切れている」。
設計理由は `README.md`「3秒で読める画面にする」を参照。

- `core/prescriptionSummary.ts` — 処方を**切ってよい部分と切ってはいけない部分**に分ける
  （距離×本数・設定・レストは切らない）。`isRedundantName` で重複する名称を省く
- `core/analysisHeadline.ts` — 分析の結論・行動・リスクの並べ方
- カレンダー: 2段表示（`種目｜距離×本数` / `設定｜レスト`）。操作（✎・＋）は `⋯` で畳む
- 分析: 最上部は3つだけ。根拠は開くまでDOMに出さない。不足データは1カードに統合
- **固定の「＋」を削除。** 余白も詰めた
- ホーム: 数字と結論を先に、理由は畳む（消さない）

作業中に見つけて直したもの:

- `40分ジョグ （カレンダー反映テスト）` の括弧が「形」に混ざり、
  **215pxの切れない塊**になって320px幅で32pxはみ出していた。
  括弧の中を落とし、呼び名が長ければ時間だけにする
- `--fab-clearance` を0にしたら最下部がタブバーに触れた（20px残した）

反省: **壊して確認を1つずつE2Eで回して時間をかけすぎた**（1回5分 × 十数回）。
同じ変更に対する複数の破壊は1回のE2Eにまとめられた。

もう1つ。ホームの検査が**一度空振りした**。
「注記があれば見る」だけにしていたので、その日の処方に注記が無くて検査ごと飛び、
理由を最初から出す実装に変えても落ちなかった。
検査したい状態は検査の側で作る。

---

## forge-v106（2026-08-17・ポイント練習前のアップ）

アップを**主練習の子データ**として記録する。設計理由は `README.md`
「ポイント練習前のアップ」を参照。

- `core/warmup.ts` — 型・語彙・集計・正規化・型（テンプレート）・FITのアップ区間からの組み立て
- `core/warmupInsight.ts` — 相性の分析。3回未満は数字を出さない
- 距離・時間・負荷・シューズ走行距離には足し、**週間の刺激回数・カテゴリ配分・
  CFE・進行段階には流さない**
- 記録画面に折りたたみの入力欄（前回と同じ／型／FITから）、分析画面に傾向
- `/api/warmup` は読むだけ。保存は `/api/results` を通る

作業中に見つけて直したもの:

- 靴を履き替えた日、アップのぶんが主練習の靴に加算されていた。
  アップの靴が指定されていればそちらに足すようにした（回数は増やさない）

反省: E2Eの組み立てで**推測したセレクタを何度も書いた**（保存ボタン名・確認ダイアログ・
主観の呼び名・RPEスライダー）。既存のE2Eに同じことをしている箇所があったので、
先に読んでから書けば1回で済んだ。
RPEは「未入力のとき真ん中を指している」ため、同じ値を入れても
Reactが変化として扱わない。`setSlider` で真ん中と違う値を入れる必要がある。

---

## forge-v105（2026-08-17・シューズ推薦）

登録した靴のうち、その日の練習に合う1足を薦める。設計理由は `README.md`
「シューズの推薦」を参照。

- `core/shoeProfile.ts` — 種類ごとの既定値と、項目ごとの手動上書き。上書きが必ず勝つ
- `core/shoeRecommend.ts` — 推薦の唯一の実装。練習の種類・場所・天候・疲労・痛み・
  走行距離から並べる。同点は登録順（毎回同じ結果が出る）
- 画面3か所（設定で性格を編集 / 練習詳細に推薦と理由 / 記録で推薦順に並べる）
- 実績が3回に満たないうちは「まだ足りません」と断る。**「学習済み」と見せない**

作業中に見つけて直したもの:

- トレッドミルにスパイクが1番で出ていた。場所違いの −8 ではポイント練習の加点に
  負けていた。ベルトを切るので −20 にして実質外した
- 痛みで靴を選び直したのに、**選ばれた靴の側に理由が出ていなかった**。
  注意書きは順位を下げた靴に付くので、いつもと違う靴が出た日に理由が読めなかった
- `deleteShoe` が知らないidでも `deleted: true` を返していた（消していないのに消したと言う）

反省: **機能を先に足してテストが後になり、カバレッジ閾値を割った状態でコミットしていた。**
`npm run verify` にカバレッジは入っていないので気づけない。埋めたうえで閾値を上げ直した。

---

## forge-v46（2026-08-01・報告4件）

利用者からの報告4件。詳細な設計判断はREADME.md
「実運用で見つかった4件の修正（forge-v46 / 2026-08-01）」を参照。

1. **`1000m×4` が `396m×12` と読まれる** → `deriveFitActuals` が lap 1つ＝1本と
   数えていた。時計で1000mの中を400/400/200と刻むと本のlapが3つ入る。
   `groupIntoReps` を足し、間に休みのlapが無く時刻が連続しているメインlapを
   1本にまとめる。通過タイムは `RepResult.splitsSec` に残して `/summary` に出す。
   距離は `snapToTrackDistance` でトラックの距離に丸める（3%以内・forge-v48で追加）。
   丸めたら警告に出す。近いものが無ければ実測のまま
2. **固定枠をやらなかったときにカレンダーから外せない** → 固定枠にも
   `SessionEditSheet` を開けるようにし、内容は変えられないと断ったうえで
   「やらなかった」だけ記録できるようにした。消さずに中止（`status: "skipped"`）に
   するので実施率に残り、再生成で復活しない。`restoreSkippedSession`
   （`DELETE /api/skip`）で戻せる。固定枠は後ろ倒ししない
3. **カレンダーの表示期間** → 既定を2週間から1週間に。選んだ期間を端末に覚えさせる
   （`app/components/view-pref.ts`。Storeには入れない）
4. **分析タブが文字の壁** → 心拍を帯＋位置の図に（`HrRow`）、制限因子の妥当域と
   PB・目標を同じ数直線に（`LimiterScale`）、週次レビューの本文を畳んだ。
   文は消していない（開けば読める）

### ついでに直したもの（視覚回帰ハーネスの不具合2件）

`pwa/visual.mjs` が**スプラッシュのつもりでアプリ本体を撮っていた**。
Service Worker が bundle.js をキャッシュから返すので `route(..., abort)` が効かず、
その状態のまま baseline が固定されていた。`serviceWorkers: "block"` を入れて解決。

同じページで、時刻の固定が `page.addInitScript` にあったためスプラッシュ用の
別ページには効かず、「GOOD MORNING / GOOD EVENING」が撮った時間帯で
変わっていた。`context.addInitScript` に移した。

### E2Eに足した経路

- 固定枠を「やらなかった」→ 一覧から外れる →「戻す」で復帰する
- 既定表示が1週間になったので、検証用セッションを作る日付を
  表示範囲（今週の月曜から7日）に合わせた（`calendarWeek`）
- 週次レビューの本文は畳んであるので、M-11は開いてから中身を見る

### 未着手

- なし（報告4件はすべて実装済み）

---

## NEXT-006（統合監査: 入力→保存→同期→分析→提案→バックアップ）✅ 実装・検証済み・未コミット

**優先順位メモ**の4番目。FIT/Garmin取込（Phase 1〜6）が既存サブシステムと
正しく噛み合っているかを7つの結合点について確認し、**既存のバグを2件発見・
修正**した。

FIT由来のセッションは2種類ある（`backfilled: true`の新規セッション／
Phase 6の紐付け済み＝`processResult`経由で`backfilled`が立たない）ため、
両方が正しく扱われるかを見る必要があった。

**PASSだった5点**: ACWR/負荷計算・週次レビュー（M-11）・カバレッジ確認・
`fitImports`の同期ラウンドトリップ・`isOwnedByAthlete`の保護（両種類とも
OR条件でカバー済み）。

### バグ1: 週次サマリーから過去データが抜け落ちていた

`weeklySummary`（`rules.ts`。ホーム・分析画面の週間カテゴリ集計・転移度
スコア）が`ctx.sessions`＝**ルール評価用にbackfilledを除いたリスト**を
使っており、「その週に実際に何をやったか」の集計として過小評価になって
いた。ACWR・週次レビュー・カバレッジ確認は未フィルタの全セッションを
使っており**ここだけ食い違っていた**。`RuleContext`に`allSessions`を
追加し`weeklySummary`だけ切り替え（ルール評価そのものは今まで通り
backfilled除外を維持）。FIT固有ではなく一括入力由来の過去データでも
起きていた既存バグ。

### バグ2: `rebuildFitDerived`が紐付け済み取込を壊す（潜在バグ）

全レコードを無条件に`buildBackfilledSessionAndResult`へ通していたため、
**紐付け済みレコードを作り直すと元の計画済みセッションではなく新規の
`fit-s-*`を作り、元セッションが結果を失って孤立**していた。
`record.sessionId === \`fit-s-${record.id}\``で判定して分岐し、紐付け済みは
取込時と同じ経路（`buildLinkedResult` + `processResult`）で作り直す。
紐付け先が消えていたら新規作成に化けさせず`orphaned`として報告。
現状どのAPIからも呼ばれていない（テストのみ）が、READMEで「分類ロジックを
直した後に実行するもの」としている以上、実行した瞬間に全紐付けが壊れる
ところだった。

### 検証

両バグとも**直す前の状態に戻すとテストが実際に赤くなることを確認**（T-4）。
バグ2は回帰テスト2件を追加し、`wasBackfilled`を常に`true`にする改変で
両方赤くなることを確認。`npm run verify`緑（**900件 60ファイル** /
`ALL E2E PASS` / `UPDATE E2E PASS`）。VERSIONは`forge-v35`のまま。

### 指摘したが直していないこと

バックアップ・同期スナップショットに**FIT元ファイル（base64）が全件入る**
ため、取込が増えるほどサイズが際限なく膨らむ（Supabaseスナップショット・
モバイル回線の転送量に影響）。正しさの問題ではなく保持期間・間引きの
方針判断が要るため記録に留めた。

---

## NEXT-005（UI/UX・iPhone操作性・アクセシビリティ改善 1〜5）✅ 実装・検証済み・未コミット

**優先順位メモ**の3番目（FIT/Garminの後、統合監査の前）。NEXT-004でFIT/Garmin
Phase 1〜4が完了した時点で、Phase 5・6（二重登録防止・既存予定との紐付け）の
計画を提示したが、「先に優先順位メモの次項目（UI/UX）に進む」という指示で
Phase 5・6は保留し、こちらへ着手。

`docs/FORGE_BACKLOG.md`の「9. アイコン・ロード画面・アプリ内UI」「10. iPhone・
アクセシビリティ」を調査し、実機無しで対応・検証できる項目から着手する計画を
提示 →「いいよ」で1〜3を承認、続けて4〜5も「これ」で承認を得て実施。

### 1. 320px幅（iPhone SE相当）での横スクロール検証

既存のE2E横はみ出しチェックは390px幅でしか回っていなかった。同じチェックを
320px幅でも実行するようにした（`pwa/e2e.mjs`）。実装変更は無し、検証範囲を
広げただけ——結果、既存コードのまま320px幅でも横スクロールは発生しないことを
確認できた。

### 2. タップ領域44×44の統一

`.seg button`（40px→44px）、`app/results/page.tsx`故障ログの「+記録する」
トグル（32px→44px）、`app/plan-settings/page.tsx`自作メニューの「+登録する」
トグル（36px→44px）、カレンダー期間送り矢印（高さ44pxはあったが幅が不足→
`min-w-[44px]`追加、`aria-label`も付与）。FIT取込lap一覧の`<select>`（32px）は
密なレビュー用リストのため対象外にした（README参照）。

E2Eにセグメントタブの高さ・期間送りボタンの幅の実測チェックを追加。**T-4で
両方の数値を戻して赤に戻ることを確認して復元。**

### 3. ConfirmDialogのアクセシビリティ

`role="dialog"`/`aria-modal="true"`/`aria-labelledby`、開いた瞬間のフォーカス
移動、Escapeで閉じる、Tabキーのフォーカストラップ（ダイアログの外へ抜けない）、
閉じたときに呼び出し元へフォーカスを戻す——を`ConfirmDialog`
（`app/components/ui.tsx`）に追加。`ConfirmButton`はこれを内部で使うため
自動的に反映される。

`onCancel`はほとんどの呼び出し元がインライン関数で渡すため、`useEffect`の
依存配列にそのまま入れると親の再描画のたびにフォーカスが先頭ボタンへ
引き戻されるバグになる。最新の関数はrefで保持し、effectの依存は`open`だけにした。

**検証**: このプロジェクトには単体テストでReactコンポーネントを描画する土台
（jsdom・testing-library）が無く、既存の方針（コアロジックは単体テスト・画面は
E2E）に合わせ、E2E（自作メニュー削除の確認ダイアログを流用）で
role/aria-modal・Escape・フォーカス復帰・フォーカストラップの4点をキーボード
操作で確認。**T-4でrole/aria-modal属性の削除、Tabのラップアラウンド処理の
無効化をそれぞれ行い、両方とも赤に戻ることを確認して復元。**

### 4. iOSキーボード対策（interactive-widget=resizes-content）

編集フォーム（`SessionEditSheet`・カレンダー追加シート）を調べると、保存
ボタンは`position: fixed`ではなく通常のフローにあり、ネイティブのフォーカス
スクロールで足りる作りだった——固定位置の保存ボタンが隠れる問題は無かった。
一方、下部タブバー・FABは`position: fixed`で、iOSはキーボード表示中も
レイアウトビューポートを縮めないため裏に取り残される可能性がある。独自の
`visualViewport`監視JSではなく、標準の**`interactive-widget=resizes-content`**
をviewport metaに追加（`pwa/index.html`・`app/layout.tsx`。後者は
`viewportFit: "cover"`も無かったため併せて追加——既存の抜けだった）。

検証: E2Eでmetaタグの内容を確認。**実機での最終確認は別途必要**（Chromiumは
キーボードによるビューポート縮小を再現しない）。

### 5. 状態表現を色だけに頼らない（StatusText）

色だけで警告・エラーを示していた箇所24個のうち21個（同じ`<p style={{color:
"var(--red|amber)"}}>`という形）を共通コンポーネント`StatusText`
（`app/components/ui.tsx`。既存の`ViolationList`のアイコン・色の組み合わせに
揃え、`role="alert"/"status"`を付与）へ機械的に置き換え。残り3個は太字という
色以外の視覚的区別が既にあるため対象外、`Notices`（ホームの1行通知）は項目に
よって色の意味が違う（片方は警告色でなくニュートラル）ため、警告色の項目
だけに条件付きでアイコン・roleを追加。

検証: E2E（S-6の他選手メニュー換算。PBの差を10秒超にして`converted.notes`が
実際に出るケースに変更）で`StatusText`がrole/アイコンつきで描画されることを
確認——1箇所の実装なので、ここで確認できれば他20箇所の呼び出しも同じ実装を
経由していることの裏付けになる。**T-4でrole/アイコンの出力を削り、赤に戻る
ことを確認して復元。**

`npm run verify`緑（**875件 60ファイル** / `ALL E2E PASS` / `UPDATE E2E PASS`）。
`bundle.js`は892,757バイト（Phase 4の891,467バイトから+1,290バイトのみ）。
VERSIONは`forge-v35`のまま。

### 残っていること

- 実機確認待ちの既存項目（アプリアイコン・起動画面・Safe Area・iOSキーボード
  対策の最終確認）はそのまま
- 色だけに頼っていた24箇所のうち21箇所は`StatusText`へ置き換え済み。残り3箇所
  （太字による区別が既にある箇所）と`Notices`の一部は意図的に対象外（README参照）

---

## NEXT-004（FIT/Garmin取込 Phase 1〜6・全項目完了）✅ 実装・検証済み・未コミット

**優先順位メモ**（P0監査 → FIT/Garmin → UI/UX → 統合監査 → トレーニングロジック監査 →
セキュリティ監査 → 運用整備 → 配信実行 → 実機受入試験 → データ分析）の2番目に着手。
9 Phaseの全量は一度にやらず、**Phase 1（ファイル選択と安全な受信）だけ**実装。

### 事前調査で判明した重要な事実（実装方針を決めた根拠）

- **Garmin Connect のiPhoneアプリにはFITを書き出す・共有する機能が無い。** 個別
  アクティビティの書き出しは **Garmin ConnectのWeb版でのみ**可能
  （[Garmin Forums](https://forums.garmin.com/apps-software/mobile-apps-web/f/garmin-connect-web/421060/exporting-individual-activity-raw-data)）
- **iOS SafariはWeb Share Target API未対応**で、PWAを共有先に登録できない
  （[MDN](https://developer.mozilla.org/en-US/docs/Web/Progressive_web_apps/Manifest/Reference/share_target)）
- 上記2点により **`share_target`は実装しない**（実現できない共有方式を動くように
  見せない、という指示に従った）。導線は「Safariで connect.garmin.com を開く →
  Web版でFIT書き出し（ファイルアプリに保存）→ FORGEで通常のファイル選択」

### やったこと

`validateFitBytes`（`src/lib/core/fitImport.ts`、新規）を追加。拡張子ではなく
FITプロトコルのヘッダー（`.FIT`シグネチャ、`header_size`が12/14、宣言された
`data_size`と実ファイルサイズの整合）で判定する。サイズ上限5MB（800m練習の
FITは通常数十〜数百KBという用途上の根拠、コメントに残した）。CRC値そのものの
検証はPhase 2（`fit-file-parser`導入時）に委ねる。

`pwa/entry.tsx` に `FitImportCard`（Apple Healthカードと同じ場所・パターン）を追加。
ファイル選択→検証結果表示のみで、**解析・保存はまだしない**（Phase 2以降）。
ファイル名はReactの自動エスケープで描画（HTML化しない）。ファイル内容はログに
出さない。二重送信は`busy`ガードで防止。

Web Workerは今回のファイルサイズ想定（同期処理で数十ms以内）では見送り、
サイズ上限チェックを読み込み前に置くことで画面の固まりを防ぐ。

### 見つけたテストの落とし穴（アプリのバグではない）

E2Eで「拡張子だけ.fitの非FITファイル」を模したフィクスチャの1バイト目が
たまたま`header_size`として不正な値になり、狙っていた`bad_signature`ではなく
`bad_header`で拒否されていた。アプリ側は正しく動作しており、フィクスチャの
作り方を直した（`header_size`は正しいがシグネチャだけ不正、という形に変更）。

### 検証

先に赤を確認（10件）→ 実装 → 緑。**署名チェックを無効化すると1件赤に戻ることを
確認して復元**（T-4）。`npm run verify` 緑（**845件 56ファイル** / `ALL E2E PASS` /
`UPDATE E2E PASS`）。VERSIONは`forge-v35`のまま。

### 変更ファイル（Phase 1）

新規: `src/lib/core/fitImport.ts` / `tests/fitImport.test.ts`（10件）。
変更: `pwa/entry.tsx`（`FitImportCard`）/ `pwa/e2e.mjs`（1経路）/ `README.md`。

### Phase 2: FIT解析（file_id / session / lap / record / event を区別）✅

**採用ライブラリ**: `fit-file-parser`（MIT、`npm install`で追加）。理由と選定過程は
README参照。**bundle.jsが約873KB→約1307KB（+約434KB）に増加**——Garmin公式の
FIT SDKプロファイル定義を丸ごと含むため。今回は許容したが、次のPhaseに
進む前に一度立ち止まって確認したい点として残す（下記「相談したいこと」）。

`parseFitFile`（`src/lib/core/fitParse.ts`、新規）が session/lap/record/event/
file_idを区別して抽出し、FORGEの単位（km・秒/km・UTC ISO文字列）へ変換する。
NaN・Infinity・負数・ゼロ除算は推測で埋めず`undefined`にする。

**重要な発見**: FITのfield番号は**メッセージ種別ごとに異なる**（例:
`avg_heart_rate`はsessionがfield 16、lapはfield 15）。これは実際に
`FitEncoder`で組み立てて`FitParser`でデコードし、ライブラリ本体のプロファイル
定義ファイルと突き合わせて確認した（当初の想定は誤りだった）。テスト用
フィクスチャ（`tests/fixtures/fitEncoder.ts`）はこの確認済みの番号を使っている。

**タイムゾーン**: `activity`メッセージの`timestamp`（UTC）と`local_timestamp`
（デバイスのローカル時刻）の差からUTCオフセットを求める。`local_timestamp`が
無ければ推測せず未確定のまま（iPhoneの現在タイムゾーンでは代用しない）。

`pwa/entry.tsx`の`FitImportCard`をPhase 2まで拡張し、構造検証に続けて実際に
解析し、種目・距離・時間・平均心拍・lap/record件数・タイムゾーンを表示する。

検証: 先に赤を確認（11件、うち1件は自分のテストの計算ミスで修正）→ 緑。
E2Eに本物のFITメッセージ（`FitEncoder`で組み立て）を使った解析確認を追加。
**sport抽出を壊すと1件赤に戻ることを確認して復元**（T-4）。`npm run verify`緑
（**856件 57ファイル** / `ALL E2E PASS` / `UPDATE E2E PASS`）。`npm audit`の
脆弱性件数は7件のまま変化なし（`fit-file-parser`由来の新規脆弱性なし）。

### 変更ファイル（Phase 2）

新規: `src/lib/core/fitParse.ts` / `tests/fitParse.test.ts`（11件）/
`tests/fixtures/fitEncoder.ts`（テスト用FIT組み立て）。
変更: `package.json`・`package-lock.json`（`fit-file-parser`追加）/
`pwa/entry.tsx` / `pwa/e2e.mjs` / `README.md`。

### Phase 2.5: コード分割によるbundleサイズ対策 ✅

Phase 2完了時点の+434KBについて(a)許容/(b)動的import分割の2択を提示 →
「おすすめで」の指示により(b)を実施。

`scripts/build-pwa.mjs`を`format: "esm"` / `splitting: true`に変更し、
`pwa/entry.tsx`の`FitImportCard`から`fitImport.ts`・`fitParse.ts`の読み込みを
`await import(...)`による動的importに変更。`pwa/index.html`の`<script>`は
`type="module"`が必須になったため変更。生成されるchunkはファイル名に
ハッシュが入るため（`chunk-[hash].js`）、`pwa/sw.js`の`isAppShell()`
（network-first対象）には含めていない——ハッシュ付きファイル名は「同じ名前
なのに中身が古い」が原理的に起きないため、cache-first（既定の分岐）で問題ない。

**結果**: `bundle.js`は約1307KB→**880,189バイト**（元の873KB相当＝
`872,693バイト`とほぼ同水準、増分は約+7.5KBのみ）。FIT関連コードは
`chunk-69sycd27.js`（425,121バイト）に分離され、FIT画面を開いた時だけ読み込む。

**新たな制約**: 一度もFIT取込を開いていない状態でオフラインだとchunkが未
キャッシュのため使えない（起動自体には影響しない）。一度オンラインで開けば
以後はService Workerのキャッシュ経由でオフラインでも使える。

**検証**: E2Eに新規経路を追加——「一度使えばオフラインでも解析できる
（chunkがキャッシュ済み）」。`page.goto("about:blank")`→オフライン化→
再度URLへ`goto`という**本物の文書再読み込み**で検証（ハッシュ変更だけの
擬似ナビゲーションでは検証にならないため、途中で作り直した）。
`npm run verify`緑（**856件 57ファイル** / `ALL E2E PASS` / `UPDATE E2E PASS`、
うち3経路がFIT関連）。VERSIONは`forge-v35`のまま。`npm audit`の脆弱性件数
（7件）は変化なし。

### Phase 3: ラップ→区間の自動分類（ウォームアップ／メイン／リカバリー／レスト／クールダウン）✅

着手前に単独の計画を提示 →「いいよ」で承認を得て実施。

`classifyLaps`（`src/lib/core/intervalClassify.ts`、新規）がPhase 2で抽出した
lap列をペース（`elapsedSec/distanceKm`）と列内の位置だけで分類する。**ルール
ベースのみ・LLM不使用**。心拍は個人差・当日のコンディション由来の変動が
大きいため判定に使わない。

判定手順: ①距離0は「レスト」固定信頼度0.85 ②距離・時間欠落は推測せず「不明」
信頼度0.15 ③残りをペース昇順に並べ、隣接比が最大のところで速い群と遅い群に
切る（比が1.20未満なら切らない。`MIN_GROUP_SEPARATION`。根拠はREADME）
④速い群が空ならインターバル構成があると決めつけず全て「不明」（0.3・警告つき）
⑤速い群が見つかれば、その前後でウォームアップ／クールダウン、間の遅いlapは
リカバリー、速いlap自体はメイン疾走。信頼度は遅い群の下限からのペース差で
連続的に決まる。

当初は中央値×0.93で切っていたが、W-up/C-downのlapが無いFITで
`300m×4` が `300m×1` になる不具合があり、2026-08-01 に隔たり方式へ変更した
（README「なぜ中央値をやめたか」）。

保存はまだしない（3層データモデルはPhase 4）。`pwa/entry.tsx`の
`FitImportCard`に、lapごとの行（番号・ペース・種別プルダウン・信頼度%）を
追加。プルダウンでの変更はこの画面上だけの一時的なもの（保存されない旨を
明記）。`intervalClassify.ts`は重い依存を持たないため、Phase 2のような
動的import化はしていない（静的importでも bundle への影響は無視できる小ささ）。

**検証**: 先にユニットテスト（`tests/intervalClassify.test.ts`、6件）を書いて
実装 → 緑。**fast/slow判定の不等号を反転させて赤に戻ることを確認して復元**
（T-4）。E2Eに新規経路を追加——ウォームアップ→メイン→リカバリー→メイン→
クールダウンの構成を持つ実際のFIT（`FitEncoder`で組み立て）を読み込ませ、
5行の分類結果（`["warmup","main","recovery","main","cooldown"]`）・信頼度(%)
表示・プルダウンでの手動修正が実際に反映されることを確認。**同経路をJSXの
条件式を壊して(`laps.length > 999`)赤に戻ることを確認して復元**（T-4）。

`npm run verify`緑（**862件 58ファイル** / `ALL E2E PASS` / `UPDATE E2E PASS`、
うち4経路がFIT関連）。`bundle.js`は884,798バイト（Phase 2.5の880,189バイトから
+4,609バイトのみ）。VERSIONは`forge-v35`のまま。

### Phase 4: 3層データモデルでの保存 ✅

着手前に単独の計画を提示。生バイトを保存するか（(a)保存する／(b)しない）を
確認 →「a」で承認を得て実施。

**3層**: ①元ファイル（生バイト・base64）②自動解析（`FitParseResult` +
自動判定`IntervalClassifyResult`）③確認済み（本人が画面上で直した後の
`confirmedKinds`）。①②は`FitImportRecord`（新規:
`src/lib/core/fitToSession.ts`）としてそのまま保存。③だけから
`Session`/`SessionResult`を導く純粋関数`fitToSessionAndResult`は
`PastEntry`の`toSessionAndResult`と同じ考え方（確認済み層だけから機械的に
決まり、何度re-runしても同じ結果）。再導出用に`rebuildFitDerived`も用意。

メイン疾走→本、その直後から次の「メイン」または「クールダウン」の**手前まで**
を合算してリカバリー（`restAfterSec`/`restAfterDistanceM`）にする——
**クールダウンまで含めてしまうバグを一度作り、単体テストの想定値
（最後の本のrestAfterはundefinedのはず）が420秒になって発覚、直した**。
カテゴリはCFEがあれば実測ペースとGRP比較（`categoryFromTarget`。一括入力と
同じ関数を再利用）、無ければ距離だけの暫定値＋警告。日付はlapの開始時刻＋
`utcOffsetSec`、無ければUTC基準＋警告。`backfilled: true`を立てる
（過去データ遡り入力と同じ扱いで、ルールエンジン評価・自動生成上書き・
バックアップmerge保護の対象外になる。`isOwnedByAthlete`が`backfilled`を
見ているため追加コード不要）。

保存はAPI経由で新規追加（`app/api/fit-import/route.ts` /
`pwa/api-shim.ts`の両方）。**クライアントで解析・分類済みのデータをそのまま
POSTし、サーバー側では再解析しない**（本人が確認した内容と保存内容を一致
させるため。重いfit-file-parserをサーバーに持ち込む必要もない）。
`Store`に`saveFitImport`/`listFitImports`を追加（SQLite: `fit_imports`
テーブル、IndexedDB: `fitImports`配列）。バックアップの書き出し・復元
（M-12・`ID_KEYED_COLLECTIONS`）にも対応。

UIは`FitImportCard`に「この内容で登録する」ボタン。登録後はボタンを引っ込め
連打による重複だけは防ぐ（二重登録の検知そのものはPhase 5）。

**検証**: `fitToSessionAndResult`の単体テスト7件（`tests/fitToSession.test.ts`）
——**restAfterの境界バグをT-4で検出**（クールダウンも含める版に戻すと
420秒 vs undefinedの期待値で赤）。サービス層テスト6件
（`tests/fitImportService.test.ts`）——`saveResult`だけを失敗させるProxyで
「保存の途中で失敗したら先に書き込んだ分（元ファイル・Session）もロール
バックされる」ことを確認（対象2と同じ`repo.transaction()`の保護）。
**トランザクションを外すと実際に赤へ戻ることを確認して復元**（T-4）。
E2Eに新経路2件——「登録する→記録画面（練習結果タブ）に実際に反映される」。

`npm run verify`緑（**875件 60ファイル** / `ALL E2E PASS` / `UPDATE E2E PASS`、
うち6経路がFIT関連）。`bundle.js`は891,467バイト（Phase 3の884,798バイトから
+6,669バイトのみ）。VERSIONは`forge-v35`のまま。`npm audit`の脆弱性件数
（7件）は変化なし。

### Phase 5: 二重登録防止 ✅

NEXT-005（UI/UX）完了後、優先順位メモに従い保留していたこちらへ復帰
（「保留中のFIT/Garmin Phase 5・6に戻る」の指示）。

同じFITをもう一度登録すると、Phase 4までは`FitImportRecord.id`が
`fit-${Date.now()}`で毎回新規生成されるため**完全に別の記録として二重登録**
されていた。**元ファイル（`rawBytesBase64`）が既存の取込と完全一致すれば
同じidを再利用する**方式にした——`saveFitImport`/`saveSession`/`saveResult`
はどれも同じidへの`INSERT...ON CONFLICT DO UPDATE`（IndexedDBも同id上書き）
なので、再登録は新規の二重登録ではなく上書きになる。一括入力（`pe-bulk-*`）
やApple Health（`ah-*`）が内容から決まるidで自然に二重登録を防いでいるのと
同じ考え方。ハッシュ関数は使わず生バイト列の完全一致で判定（完全一致以外は
別記録として扱う。推測しない）。

**副次的に見つけた問題**: `id`が`Date.now()`だけだったため、短時間に別の
FITを続けて取り込むとミリ秒が衝突し無関係な記録を上書きしうる状態だった。
乱数を足して修正（`fit-${Date.now()}-${ランダム6文字}`）。

`importFitFile`の戻り値に`duplicate: boolean`を追加。`FitImportCard`は
duplicateに応じてメッセージを出し分ける。

**検証**: 単体テスト3件追加（同じ生バイト→id再利用・件数不変、分類を直して
再登録→内容が実際に更新される、生バイトが違えば別記録）。E2Eで実際のUI
操作から確認——同じファイルを選び直しても`input`の`change`が発火しないことが
分かり、`about:blank`経由の文書再読み込みを挟む形に修正。**T-4で一致判定を
無効化し、単体テスト2件・E2E2経路すべてが赤に戻ることを確認して復元。**

`npm run verify`緑（**878件 60ファイル** / `ALL E2E PASS` / `UPDATE E2E PASS`、
うち8経路がFIT関連）。`bundle.js`は893,128バイト（Phase 4の891,467バイトから
+1,661バイトのみ）。VERSIONは`forge-v35`のまま。

### Phase 6: 既存の計画済みセッションとの紐付け ✅

着手前に単独の計画を提示 →「いいよ」で承認を得て実施。

先に通常の記録経路`processResult`を調査——単なる保存ではなく、`status`を
`planned`→`completed`にし、**CFE更新・ルールエンジン・以降の予定への波及**
まで行うことを確認。FIT取込Phase 1〜5（`backfilled: true`）はこれらを意図的に
スキップしていたが、**計画済みセッションに紐付ける場合は、手入力で記録した
場合と同じ扱いにすべき**と判断（「今日やる予定だった練習を、手入力の代わりに
FITで正確に記録する」ことと同じため）。

`src/lib/core/fitToSession.ts`を分割: `deriveFitActuals`（lap列から実測データ
だけを導く純粋関数）／`buildBackfilledSessionAndResult`（従来の新規backfilled
セッション）／`buildLinkedResult`（既存セッションのSessionResultだけを作る。
`backfilled`は立てない）。`fitToSessionAndResult`はこの2つの組み合わせとして
後方互換のまま維持（既存テスト・`rebuildFitDerived`は無改修で通った）。

`importFitFile`に`linkToSessionId`を追加。省略時（1回目）はその日の
`status: "planned"`セッションを探し、あれば保存せず`needsConfirmation`＋
候補一覧を返す。2回目に文字列（紐付け先）か`null`（新規登録）を渡すと確定。
APIルートは`body`をそのまま渡す実装だったため、フィールド追加だけで
Next.js・PWA両方に対応できた（新規エンドポイント不要）。

計画とFITの実測内容が食い違っていても検知しない——**手入力の既存経路
（`ResultForm`）も同様に検知していない**ことを確認済みなので、ここだけ
新しく厳しくしない。RPE・主観的しんどさはFITから取れないため、backfilledと
同じ既定値を使う（後で記録画面から直せる）。

**検証**: 単体テスト5件（計画済みが無ければ従来通り／あれば確認だけで
何も保存しない／複数候補を全部返す／紐付けで`processResult`が実際に走り
`backfilled`は立たない／新規登録を選ぶと計画済みセッションはそのまま残る）。
E2Eで実際に計画済みセッションを作り、同日のFIT取込→確認画面→紐付け選択→
`status`が`completed`になることまで確認（テスト専用に足したセッションは
後続の間隔違反テストへの影響を避けるため終了時に削除）。**T-4で計画済み
セッションの検出処理、`processResult`経由の保存をそれぞれ無効化し、両方とも
赤に戻ることを確認して復元。**

E2E構築時の落とし穴2件: ①IndexedDBの保存は250msデバウンスされており、
`fetch`直後に文書を読み直すと直前の書き込みが消えることがある（待ち時間を
追加）。②同じFITファイルを選び直す再現には文書ごとの再読み込みが要る
（Phase 5と同じ問題）。

`npm run verify`緑（**883件 60ファイル** / `ALL E2E PASS` / `UPDATE E2E PASS`、
うち9経路がFIT関連）。`bundle.js`は896,020バイト（Phase 5の893,128バイトから
+2,892バイトのみ）。VERSIONは`forge-v35`のまま。`npm audit`の脆弱性件数
（7件）は変化なし。

これでFIT/Garmin取込（Phase 1〜6）が完了。

### 完了条件

- [x] 先に赤、実装後に緑（Phase 1〜6とも）
- [x] `npm run verify` 緑
- [x] Garmin共有仕様を実際に確認した上で実装方針を決定
- [x] FIT field番号を実際にエンコード・デコードして確認（推測に頼らない）
- [x] bundleサイズ対策（動的import・コード分割）
- [x] ラップ→区間の自動分類（ルールベース・信頼度・手動修正表示）
- [x] 3層データモデルでの保存（元ファイル・自動解析・確認済み）
- [x] 二重登録防止（生バイト列の完全一致でid再利用）
- [x] 既存の計画済みセッションとの紐付け（通常の記録経路を再利用）
- [x] README更新
- [ ] commit・push・配信（指示により保留）

---

## NEXT-003（P0監査 対象1・2・7・6・3。全7対象すべてに着手）✅ 実装・検証済み・未コミット

第三者P0監査の7対象のうち、外部設定に依存しない2つに着手（対象4・5はNEXT-002で対応済み）。

### 対象1: PWAの保存保証

`persistState`（`pwa/memory-store.ts`）が `void` を返し、IndexedDB・localStorage
両方の失敗が呼び出し側に伝わらなかった。`Promise<PersistOutcome>` 化し、失敗理由
（`quota`/`unavailable`/`unknown`）を返す。IndexedDBアクセスは注入可能にして
（新規テストライブラリなし）テストで分岐を検証。`pwa/entry.tsx` に
`PersistFailureBanner`（失敗時のみ表示、`role="alert"`）と、`pagehide`/
`visibilitychange` での `flushPendingState()` 呼び出しを追加。

### 対象2: 安全なバックアップ復元

`isBackupFile` が `format`/`version`/`data` の存在しか見ておらず、`replace` は
検証前に `resetAll()` していた。`validateBackup`（`src/lib/core/backup.ts`）を
新規追加し、各コレクションが配列か・idが文字列かを `resetAll()` より前に確認。
さらに `Store.transaction()`（新規、SQLiteは実BEGIN/COMMIT/ROLLBACK、
MemoryStoreはスナップショット差し戻し）で実際の書き込みを包み、検証をすり抜けた
想定外のデータで途中失敗しても開始前の状態へ完全に戻す。

**実際に確認した効果**: `validateBackup` を外すと、IndexedDB側は壊れたセッション
（idが数値）を**例外すら出さずそのままpushしてしまう**（配列pushに型チェックが
無いため）。検証を先に置くことがIndexedDB経路では唯一の防御と確認できた。

### 変更ファイル

`src/lib/db/store.ts`（`transaction`追加）/ `src/lib/db/repo.ts`（実装）/
`pwa/memory-store.ts`（`persistState`・`flushPendingState`・`transaction`）/
`pwa/entry.tsx`（バナー・flush配線）/ `src/lib/core/backup.ts`（`validateBackup`）/
`src/lib/service.ts`（`importBackup`の並べ替え）/ `README.md`。
新規テスト: `tests/pwaPersist.test.ts`（7件）/ `tests/storeTransaction.test.ts`（6件）/
`tests/backupAtomicity.test.ts`（11件）。

### 検証

先に赤を確認 → 実装 → 緑。**`validateBackup`を外すと2件赤に戻ることを確認して復元**（T-4）。
`npm run verify` 緑（**835件 55ファイル** / `ALL E2E PASS` / `UPDATE E2E PASS`）。
`npm audit`: 既存の脆弱性（Next.js/PostCSS連鎖、メジャー更新が必要）のみで、
今回の変更とは無関係・対象7（依存関係）の範囲。VERSIONは`forge-v35`のまま。

### 対象7・6・3（P0監査の残り）✅ 実装・検証済み・未コミット

FIT/Garmin（NEXT-004）・UI/UX（NEXT-005）完了後、指示により保留していた
P0監査の残り3項目に着手（対象7→6→3の順）。

**対象7: 依存関係と基本セキュリティ**——`npm audit`の7件は`next@14`本体・
同梱postcss由来で、修正には`next@16`へのメジャー移行が要る（`app/`共有
アーキテクチャへの影響が大きく、P0監査のついでにはやらない。対象3の
ローカル限定待受で実露出は抑制済み。理由をREADMEに明記）。`vite`系3件は
devDependencyのみで配信物に含まれず実害なし。秘密情報のハードコードは
無し。`.gitignore`に`.env`系を追加。バックアップJSON・Apple Health XML
取込にFITと同じ考え方のサイズ上限を追加
（`BACKUP_MAX_BYTES=50MB`／`HEALTH_XML_MAX_BYTES=500MB`）。

**対象6: 危険なトレーニング提案の防止**——既存の`checkPastEntry`
（実測の妥当性チェック）と同じ閾値を再利用した`checkTargetPaces`/
`checkSessionPlausibility`（`src/lib/core/sanity.ts`）を新規追加し、
`addSession`・`editSession`（**forceでも越えられない**）・`regeneratePlan`
（生成ロジックのバグで出た枠だけを除外し件数報告）の3箇所に配線。
手入力の経路（カレンダー＋／編集シート）で実際に桁の打ち間違いを拾う
ことを確認。

**対象3: Next.js APIの認証・認可**——`next dev`/`next start`が既定で
`0.0.0.0`待受だったため、`-H 127.0.0.1`に固定（同じLAN上の別端末から
`/api/backup?download=1`で全データを読めていた問題を解消）。加えて
`middleware.ts`（新規）で`FORGE_API_TOKEN`環境変数設定時のみ全APIに
共有シークレットを要求（未設定時は無効＝ローカル開発の摩擦を増やさない）。
配信されているPWAはNext.jsサーバーを使わないため無関係。

**検証**: 対象7は単体テスト＋T-4。対象6は単体テスト＋service層テスト＋
T-4（`addSession`/`editSession`それぞれ）＋E2Eで既存の手動編集・プラン
生成フローに回帰が無いことを確認。対象3は実際に`npm run dev`を起動し
ホスト表示とcurlでのトークン認証（401/401/200）を手動確認——**Next.js
サーバー機能そのものであり配信PWAには影響しないため、これを継続的に
検証する自動テストは無い**（手動確認のみ）。

`npm run verify`緑（**897件 60ファイル** / `ALL E2E PASS` / `UPDATE E2E PASS`）。
VERSIONは`forge-v35`のまま。

これでP0監査7対象すべてに着手完了（対象1・2・4・5・7・6・3）。

### 完了条件

- [x] T1〜T7相当のテストが先に赤、実装後に緑
- [x] `npm run verify` 緑
- [x] 両方失敗する状況で画面に保存失敗が出る
- [x] 不正バックアップのreplace後、既存データが残っている
- [x] 既存の正常バックアップが読める（回帰テストで固定）
- [x] README更新
- [ ] commit・push・配信（指示により保留）

---

## NEXT-002（完了・commit d997ddb・forge-v35配信済み・実機確認済み）

**Supabase 設定、Google OAuth、PWA 同期の調査・修正**

`docs/FORGE_BACKLOG.md` の項目2。

### 今回やったこと（Phase 2-1: 統合の安全性）✅

**統合（merge）で、この端末の練習がクラウドの古い予定に上書きされていた。**

`/sync` の pull（`app/sync/page.tsx:160`）と、競合時の「両方を残す（統合）」は
どちらも `importBackup(..., "merge")` を通る。その `mergeById` が同じIDを
**無条件で上書き**していたため、クラウドに残っていた自動生成予定が、
この端末の**完了済み・本人編集・固定枠・手動追加・遡り入力**を消していた。

AGENTS.md の「完了済み・手動編集・固定予定を上書きしない」に反しており、
「両方を残す」と書いてあるボタンが片方を消す状態だった。

- `mergeById` に `keepExisting` を足し、残した件数を `kept` で返す
- `importBackup` の sessions で `isOwnedByAthlete` を適用（`replace` では適用しない。
  本人が「クラウドを優先」を選んだ経路なので）
- **守ったことを黙らない。** `RestoreReport.kept` と `warnings` を、
  データ管理画面と同期画面の両方に出す
  （`/api/backup` の応答は `{ ok, report }`。`report.warnings` の取り違えで空になっていた）

検証: 追加18件が**修正前16件赤 → 修正後緑**。E2E を1経路追加し、
**保護を外すと3つとも落ちること**を確認して復元（T-4）。`npm run verify` 緑
（**801件 52ファイル** / `ALL E2E PASS` / `UPDATE E2E PASS`）。

### Phase 2-3: Storage の利用者分離 ✅

**保存先が全利用者共通のままだった。** `forge/snapshot.json` は固定パスで、
Google OAuthでサインインできる誰か（想定外の第三者を含む）が同じファイルを
読み書きできる状態だった。利用者が1人でも、公開URL・OAuthを使う以上
「認可は不要」と判断しない、という方針（`docs/FORGE_BACKLOG.md` 項目11）どおり対応。

- 保存先を `forge/<uid>/snapshot.json`（利用者ごと）に変更
- `uid` は `accessToken`（SupabaseのJWT）の `sub` クレームから取り出す
  （`src/lib/core/sync.ts` の新規 `jwtSubject`。既存の `jwtRole` と同じ
  デコード基盤を共有するようリファクタ）
- `uid` が取り出せない場合は**通信せず**明確なエラーにする（共有パスへの
  黙ったフォールバックは分離の意味を失わせるため実装していない）

検証: `tests/sync.test.ts` に4件、`tests/supabaseConnection.test.ts` に2件追加
（先に赤を確認）。E2E は per-user パスへの経路に更新し、**旧共有パスに戻すと
ユニット3件・E2E3件とも落ちること**を確認して復元（T-4）。`npm run verify` 緑
（**810件 52ファイル** / `ALL E2E PASS` / `UPDATE E2E PASS`）。

**外部設定（本人対応・完了）**: Supabase の SQL Editor で RLS ポリシー4本
（SELECT/INSERT/UPDATE/DELETE、`(storage.foldername(name))[1] = auth.uid()::text`）
を適用。当初 `docs/FORGE_REQUIREMENTS.md` に用意したSQLに
`alter table storage.objects enable row level security;` を含めていたが、
実機で `ERROR: 42501: must be owner of table objects` になったため削除
（`storage.objects` は SQL Editor の `postgres` ロールの持ち物ではなく、
Supabaseのプロジェクトでは作成時点でRLSが既に有効なため、この行は不要だった）。
`create policy` 4本のみで適用成功。ドキュメントは訂正済み。

**「未解明点」は解消した**（下のPhase 2-4を参照）。RLS適用直後に古いコードで
成功したのは謎のままだが、実害があったのはPhase 2-4のバグの方だった。

### Phase 2-4: 個人用パスへの初回pushが失敗する不具合を修正 ✅

`forge-v34` 配信後、本人が実機で「いま同期する」を実行したところ、次のエラーが出た。

```
クラウドの読み取りに失敗しました（HTTP 400）。
Supabase: NoSuchKey / not_found / Object not found。
「forge」バケットの存在と、authenticated向けSELECT・INSERT・UPDATEポリシーを確認してください。
```

**根本原因**: 個人用パス（`forge/<uid>/snapshot.json`）は誰も一度も書き込んでいない
ため、実際のSupabaseは初回pushで `code: "NoSuchKey"`（S3互換のオブジェクト未存在
コード）を含む本文を返した。`isMissingSnapshot`（`app/components/supabase.ts`）は
`payload.code` を最優先で見て文字列 `"not_found"` とだけ比較していたため、
`"NoSuchKey"` と一致せず「初回同期」と判定できなかった。`error` フィールドには
従来どおり `"not_found"` が入っていたが、`code` を優先していたため見ていなかった。
テスト環境のダミー応答には `code` フィールドが無かったため、この不具合はユニット
テストでは検出できず、**実機の初回pushで初めて顕在化した。**

- `isNotFoundCode` を追加し、`code` と `error` のどちらかが `"not_found"` または
  `"nosuchkey"`（大小文字問わず）であれば未存在と判定するよう緩和
- `message`（`"Object not found"`）は引き続き主な決め手として必須のまま

検証: `tests/supabaseConnection.test.ts` に1件追加（実機で見た実際のペイロード
そのまま）。**修正前ロジックに戻すとユニット1件・E2E2件が落ちること**を確認して
復元（T-4）。`npm run verify` 緑（**811件 52ファイル** / `ALL E2E PASS` /
`UPDATE E2E PASS`）。**その後 `forge-v35` として commit / push / gh-pages配信まで
完了済み**（2026-07-30）。

### 完了条件

- [x] Phase 2-0〜2-3 実機で確認済み
- [x] `npm run verify` 緑
- [x] commit `4364d1e` → push → gh-pages配信（`forge-v34`）
- [x] RLSポリシー適用済み（本人確認）
- [x] Phase 2-4: 個人用パスへの初回push不具合を発見・修正
- [x] commit `d997ddb` → push → gh-pages配信（`forge-v35`）
- [x] **`forge-v35` 配信後の同期動作を実機で再確認** — 本人確認済み（2026-07-30）

**NEXT-002 はこれで全条件を満たし完了。**

### 今回やったこと（Phase 2-0 + 2-2）✅

**実機診断で切り分けた。** 接続テストは `種別: ok ／ HTTP 200` — Supabase プロジェクトは
実在し、URL・Publishable Key とも正しい。「プロジェクトが無い」という当初の推測は外れた。

サインイン後にホームへ戻る症状を実機で再現し、**同期画面を開き直すとサインイン済みと
表示される**ことを確認した。つまり**トークンは受け取れているが、画面遷移だけが起きない**。

**根本原因（確認済み）**: `app-shell.tsx` のトークン受け取りが、戻り先に `?sync=1` という
クエリが残っていることを前提に「同期画面へ戻るか」を判断していた。このクエリは
自前では守れない。Supabase の **Redirect URLs** にこのアプリのURLを登録していないと、
Supabase は指定した `redirect_to` を無視して **Site URL** へ飛ばすため、クエリごと落ちる
（横取り耐性のための仕様で、FORGE側の不具合ではなく設定依存の外部要因）。
その結果、トークンは保存されるのに画面だけホームに居座っていた。

**本人に確認済み**: 症状が起きていた当時、Supabase の Redirect URLs には
`https://rio01100801-gif.github.io/trainmiddle/**` のワイルドカードエントリが
**入っていなかった**（今回の相談中に追加）。上記の推測は当て推量ではなく、
時系列と設定状態の両方で裏が取れている。

**直したこと**: `captureAuthRedirect` が何か拾えた時点で、それは必ず
`signInWithGoogle` が発行した `redirect_to` からの戻りである（他にこのURLへ来る経路が
無い）。そこで `?sync=1` の有無を問わず同期画面へ戻すようにした。判断は
`src/lib/core/sync.ts` の `authRedirectLanding`（新規・純関数）に集約し、
`app-shell.tsx` はそれを呼ぶだけにした。

検証: `tests/sync.test.ts` に3件追加（先に赤を確認）。E2E に
「`?sync=1` が欠けた復帰でも同期画面へ戻る」経路を追加し、**判定を元に戻すと
タイムアウトで落ちること**を確認して復元（T-4）。`npm run verify` 緑
（**804件 52ファイル** / `ALL E2E PASS` / `UPDATE E2E PASS`）。
**その後 `forge-v33` として commit / push / gh-pages配信まで完了済み**（2026-07-30）。

**症状2（iPhone「サーバに接続できません」）も、`forge-v33` 配信後の実機確認で
再現しなくなったことを本人に確認済み**（2026-07-30）。

ただし**根本原因は確定していない。** 私が直接コード上の原因を特定して直したのは
症状1（`?sync=1` 依存）だけで、症状2については以下のどれで直ったか切り分けていない。

- 元々の報告どおり「最初にURLを打ち間違えた」ことによる一時的な状態で、
  正しいURLへ直した時点で既に解消していた可能性
- Redirect URLs にワイルドカードを追加したことによる副次的な解消
- 症状1の修正コードが症状2にも効いていた可能性

**推測で「直った理由」を断定しない。** 現時点で言えるのは「本人の実機で再現しなくなった」
という事実のみ。今後また同様の症状が出た場合は、あらためて実機で切り分けが必要。

**外部設定は対応済み。** Site URL は `https://rio01100801-gif.github.io/trainmiddle/`、
Redirect URLs に `https://rio01100801-gif.github.io/trainmiddle/**` を含む3件が
登録されていることを本人のスクリーンショットで確認した（2026-07-30）。
コード側の修正（`?sync=1` に依存しない）と合わせて、二重の安全策になっている。

### なぜ次がこれか

BUG-02〜04 は**実機 iPhone でしか再現確認できない**。実機の結果を待つ間に進められて、
かつ他の多くの項目の前提になるのが同期。ただし下の「先に片付かないと進まないもの」に注意。

### いま実装されているもの（コード上の事実。**検証済みという意味ではない**）

| 置き場所 | 中身 |
| --- | --- |
| `src/lib/core/sync.ts` | ネットワーク非依存の判断。`decideSync` / `metaOf` / `normalizeSyncConfig` / `validateSyncConfig` / `oauthRedirectTo` / `googleAuthorizeUrl` / `authRedirectLanding`（新規） |
| `app/components/supabase.ts` | 実通信と端末側の保存。設定の読み書き、`signInWithGoogle`、`testConnection`、`getSyncDiagnostics`、`parseAuthRedirectHash` / `captureAuthRedirect`、`fetchSnapshot` / `putSnapshot` |
| `app/components/app-shell.tsx` | OAuth復帰の受け取り。着地判断は `authRedirectLanding` に委譲 |
| `app/sync/page.tsx` | `/sync` 設定画面。未設定でも成立する |
| `tests/sync.test.ts` / `tests/supabaseConnection.test.ts` | 42件・緑 |
| `pwa/e2e.mjs` の S-11 | 同期設定・接続診断・OAuth復帰（`?sync=1` あり／無し両方）・Storage RLS診断・クラウド保存・設定のみ削除 |

同期の中身は `exportBackup` / `importBackup` の payload をそのまま使う。
**保存形式を変えると同期にも波及する。**

### 未解決として報告されている症状

1. ~~**PC**: Google サインインまで進めるが、そのあと FORGE のホーム画面に戻る~~
   → **Phase 2-2 で対処済み（原因確定・実機再現済み）**
2. **iPhone（PWA / Safari）**: 「サーバに接続できません」が出る。**未再現**。
   最初に URL を打ち間違え、その後修正した経緯あり。症状1とは別原因の可能性がある

### コード上で分かっている穴

- **Storage の置き場所が全利用者共通**（`app/components/supabase.ts:537-538` の
  `BUCKET = "forge"` / `OBJECT = "snapshot.json"`）。利用者IDでパスが分かれていない。
  RLS の設計とセットなので、`docs/FORGE_BACKLOG.md` の項目11とも重なる
- ~~`importBackup` の統合が同じIDを無条件で上書きする~~ → **Phase 2-1 で対処済み**。
  完了済み・本人編集・固定枠・手動追加・遡り入力は統合で残し、残した件数を画面に出す
- ~~OAuth復帰が `?sync=1` の残存を前提にしていた~~ → **Phase 2-2 で対処済み**

### 先に片付かないと進まないもの（🔑 リポジトリ外）

`HANDOFF.md` 下部の「外部設定待ち」の6件。**これが終わるまで実データの往復は検証できない。**
コードだけで進められるのは、設定の正規化・診断・判断ロジック・ハッシュ経路の扱いまで。

### 着手時に決めること（症状2に着手する場合。**この節を埋めてから実装に入る**）

- 症状2を**先に実機で再現**する。再現しないものを直さない（NEXT-001 の教訓）
- 失敗するテストを先に書く。ネットワークを使わない層（`sync.ts`）で書けるところまで書く
- Next.js と PWA の両経路 / SQLite と IndexedDB の両保存層を確認する
- `exportBackup` の形式に触るかどうかを最初に決める（触るなら影響範囲が一段広がる）
- 秘密情報を扱う。**token・key・健康データ本文をログや報告に出さない**

詳細要件は `docs/FORGE_REQUIREMENTS.md` の 2.2 に書く（現在は未記入）。

---

## NEXT-001 の完了記録（2026-07-30・commit fd15365 / forge-v33 配信済み）

**目標レースのボーダータイムが再表示時に消える** → ✅ 対応済み

- **報告された症状は着手時点で既に直っていた。** 往復を見る7件のテストは修正前から緑
- **実際に直したのは別の欠陥**: `Number.isFinite(0)` が `true` のため `0`／負のボーダーが
  保存を素通りし、`planHeatPace` の `?? ` も `0` を nullish と見なさないので、
  予選の通過目安が **−0.5秒** になる。**画面には値が出たままで気づけない**。
  `importBackup` は `saveGoalAndRaces` を通らないので、そちらにも同じ規則を通した
- 変更: `src/lib/service.ts`（`normalizeRaceBorders`）/ `tests/goalRaces.test.ts`（新規12件）/
  `pwa/e2e.mjs`（1経路）/ `README.md` / `pwa-dist/bundle.js`。`app/goal/page.tsx` は変更なし
- 検証: 修正前に4件赤 →修正後12件緑。E2E は**壊すと落ちることを確認**して復元（T-4）。
  `npm run verify` 緑（783件 51ファイル / `ALL E2E PASS` / `UPDATE E2E PASS`）。
  当時は VERSION 未更新だったが、**その後 `forge-v33` としてビルド・commit・push・
  gh-pages配信まで完了済み**（2026-07-30）

**残り**: 実機 `forge-v33`（配信済みの最新版）で元の症状が再現するかの確認。
再現するなら、直したのとは別の原因が残っている。

根拠と閾値の理由は `docs/FORGE_REQUIREMENTS.md` の 2.1 と `README.md` にある。

---

---

## READMEから移した時系列記録

この節は、READMEを主題別の設計資料へ戻した際に移した旧記録。版・日付・実施内容を失わないため、移動時の文章をそのまま保持する。現在の設計理由はREADME、未完了はBACKLOGを見る。

## UI改修指示書v2 への対応

### A-4 / B-2: 改修前の配置と改修後の到達先（対応表）

改修の原則は「移動は可、削除は不可」。下の表のとおり、旧ホームの8カードと
旧ハンバーガーの12項目はすべて到達可能なまま残っています。
唯一の削除は E-4 で明示された**重複表示**のみです。

#### 旧ホーム画面のカード（8種）

| 改修前のカード | 改修後の到達先 |
|---|---|
| 今日のメニュー ＋ セッション準備度 | ホーム TODAY（統合）。準備度の内訳リングは `/session` へ |
| 今週のスケジュール | ホーム 週ストリップ（TODAY直下・固定） |
| CFE（推定800mタイム） | ホーム PERFORMANCE 主指標 |
| 週間800m転移度 | ホーム PERFORMANCE 副指標 |
| 高乳酸頻度（28日） | ホーム PERFORMANCE 副指標 |
| 今週の走行距離 | ホーム PERFORMANCE 副指標 |
| 疲労シグナル・総合疲労 | ホーム RECOVERY 主指標（1枚に統合） |
| 今週の負荷（ACWR） | ホーム RECOVERY 副指標 |
| 次のレースまで | ホーム RACE 主指標 |
| シーズンフェーズ | ホーム RACE 副指標＋ヘッダー右に常時表示 |
| ラウンド構成を見る | ホーム RACE 内リンク |
| ルール警告（件数一覧） | 当日・翌日ぶんのみ TODAY。それ以外はカレンダーの該当日バッジ |
| 警告の詳細（全文再掲） | **削除**（E-4）。全文は `/warnings` に集約し、TODAYとカレンダーから遷移 |

新規表示: 故障ログの未回復件数（RECOVERY 副指標）、前回ポイント練習からの経過日数（同）。

#### 旧ハンバーガーメニュー（12項目）

| 改修前のメニュー項目 | 改修後の到達先 |
|---|---|
| ホーム / カレンダー / 記録 / 分析 | 下部タブに既存。メニューからは削除 |
| レース分析 | 分析タブのセグメント「レース」に統合（`/race` 単独でも到達可） |
| 大会モード | ホーム RACE セクションから起動。レース前日・当日は TODAY に自動昇格 |
| 過去データ | カレンダーの日付タップからの入力に統合。一括入力は設定内に残す |
| メニュー設定 | 設定画面 |
| 暑熱順化 | RECOVERY セクションの詳細リンク／設定画面 |
| 目標・レース | RACE セクションから編集／設定画面にも配置 |
| プロフィール | 設定画面 |
| データ管理 | 設定画面 |

ハンバーガーは廃止し、ヘッダー右の設定アイコン（44×44pt）1つに置き換えました。

#### P-5 増えた機能をどう見つけられるようにするか

タブを増やさずに機能だけが増えた結果、2つの問題が出ていた。

**分析タブのセグメントが6つ。** iPhone幅では1つ約59pxで「バランス」「現在地」が詰まり、
どれが選ばれているかも読み取りにくかった。数を減らすために機能を消すのではなく、
**問いの単位でまとめて3つ**にした。

| セグメント | 中身 | 答える問い |
| --- | --- | --- |
| 現在地 | 制限因子・600m通過・接地時間・週次レビュー | いま自分はどこにいるか |
| 推移 | CFE・経済走RPE・日次負荷・ACWR・転移度バランス | どう変わってきたか |
| レース | レース分析 | レースに向けて何をするか |

中身のカードは縦に並ぶだけなので、まとまっても目的のカードは見つかる。
E2Eがセグメント数（3つ）とラベルが幅に収まっていること（`scrollWidth ≦ clientWidth`）を
実測している。増やすときはここが落ちる。

**設定画面が名前だけの一覧だった。** 開いたことのない画面は何をするところか分からず、
存在に気づけないまま素通りになる。画面を増やさず、用途で3つに分けて1行ずつ説明を添えた
（最初に決めるもの／練習の決まりごと／データ）。到達先そのものは1つも減らしていない。

### D-3「前回と同じ」の設計

直近の**同じカテゴリ**の記録を初期値として読み込みます。距離や本数が違っていても
「前回の入力値」として使う価値があるので一致条件は厳しくしていません。
代わりに事故を防ぐ仕掛けを2つ入れてあります。

1. **どのセッションから読んだかを必ず画面に出す**（例: 「07-28 高乳酸セッション の内容を読み込みました」）。
   出典が見えないまま登録されると、そのままCFEの更新に流れてしまうため。
2. **実施タイムだけは読み込まない**。本数・距離・レスト・RPEは前回の値を入れますが、
   実施タイムは「前回の結果」であって今日の結果ではないので、必ず空のままにします。

カテゴリが違うものは絶対に候補にしません（高乳酸の記録を経済走に持ってこない）。

### D-3 メニューのテンプレート保存

記録画面の「この内容をメニューとして保存」で、**自作メニュー（3-2）として登録**されます。
テンプレート専用のストアは作っていません。同じ「よく使うメニュー」を2箇所で持つと
どちらが正なのか分からなくなり、生成側がどちらを見るかも曖昧になるためです。
保存したものは「メニュー設定」から確認・一時停止・削除できます。

### 実装できなかった項目

現時点ではありません。指示書のフェーズA〜Eはすべて実装済みです。
アプリ全体としての未実装は `BACKLOG.md` を見てください。文書に一覧を書き写すと、
実装したあとに片方だけ古くなります（実際にここが「クラウド保存は未実装」のまま残っていました）。

### 設計上の判断（指示が競合した箇所）

FORGEブランド指針は下部タブを Home / Calendar / Analysis / Settings と定義していますが、
改修指示書は 記録タブの再構成（フェーズD）を前提にしています。
両立させるため、**下部タブは ホーム / カレンダー / 記録 / 分析 の4つ**とし、
**設定はヘッダー右のアイコン**（B-1の指示）に置きました。
タブ数は4つのままで、記録への到達がメニュー階層に沈まないことを優先しています。

## 改修指示書v3（F〜I）への対応

### F-1 時間・ペースの相互補完

練習日誌には「51分 平均4:40」のように距離が書かれていない。距離を必須にすると
電卓を叩くか入力を諦めることになるので、**距離・時間・ペースのうち2つあれば
残り1つを埋める**。3つとも入力されている場合は上書きせず、3%以上ずれていれば
警告だけ出す（どれが正しいかはこちらでは判断できないため）。

時間は `51` `51:30` `1:05:00` `51分` を、ペースは `4:40` `4:40/km` `@3:50` を受け付ける。
単位なしの数値は、時間なら「分」、ペースなら「秒/km」として読む（実態に合わせた非対称）。

### F-2 一括入力の解釈規則

完全ルールベース。同じテキストを貼れば必ず同じ解釈になる。
実際の練習日誌（タブ無し・半角スペース1個区切り・実施タイムが次の行・
括弧に設定と区間ラップが混在）をそのまま通せる。

- **行の切り出し**: 先頭トークンだけを日付として切り、残りは本文として扱う。
  タブや2連スペースを列区切りに要求すると、実際の日誌は1列も切り出せない
- **継続行**: 先頭が日付でない行は前の行の続きとして結合する
  （`42 1:26 1:25` のように実施タイムだけの行がよくある）
- **予定と実績の分離**: 本文にタブ・全角空白・2連スペースがあれば後ろを「結果」とみなし、
  そちらを優先する。`ジョグ40〜50分（予定）／51分（実績）` で予定の50分が実績を
  押しのけるのを防ぐ
- **処方の除去**: `300(42)＋600(1:26)` の 300 や 600 は距離であって実施タイムではない。
  除去せずに数値を拾うと、距離と設定が実施タイムの列に紛れ込む
- **レスト記述**: `r15min` `r200jog` `R12min`。r と数字の間に空白を許してはいけない。
  許すと `R12min 41.6` の `R  41` まで飲み込み、実施タイムの1本目が消える
- **カテゴリ**: **設定タイムがあれば一意に決まる**。
  設定ペース ÷ 現在のGRP の比率で分類する
  （〜102% は高乳酸/モデリング、〜112% は経済走、それ超は CV/閾値）。
  同じ 1000m×4 でも設定が3:15/kmならCV、2:50/kmなら高乳酸になるので、
  距離だけでは決まらないが設定があれば決まる。設定が無い場合だけ人間に選ばせる
- **複合区間**: `300(42)＋600(1:26)＋600(1:26)` は本数の多い距離（600m）を代表とし、
  そのぶんだけを能力推定に使う。混在していることは画面に明示する
- **補助の分離**: `8000mペース走＋流し4本` の流しは主セッションから切り離す
- **オフ**: 休養日として記録する。能力推定には使わない
- **レースの括弧**: `1:56.0(56.0-60.0)` の括弧は区間ラップ。
  レース配分シミュレータの唯一の材料なので落とさない

読めなかったものは**空欄のまま**にし、理由を添えて人間に確定させる。
確定していない行は登録経路に乗らない。

### J 読めてしまった間違いの検出（sanity）

読めなかった行は画面に理由が出るので気づける。危ないのは**読めてしまった間違い**で、
これは何も言わずにCFEまで届く。

例: 300mの実施タイム 40.0 を 4.00 と打つ。素直に読めば800m換算は約11秒になり、
承認した瞬間にCFEが飛ぶ。CFEは全カテゴリの設定ペースの親なので、練習全体が壊れる。

`src/lib/core/sanity.ts` が登録前に検査する。error が1つでもあれば登録させない
（画面には「要確認」）、warn は登録できるが表示する（「注意」）。

- 1本のペース 0.095〜0.60秒/m の外（100mを9.5秒／10:00/km相当を超える）は error
- 持続走 2:30〜10:00/km の外、距離0.4〜60km・時間3〜360分の外は error
- 平均心拍 80〜210 の外は error（桁の打ち間違い）
- 400mPBから見て速すぎる1本（200m以上の区間で12%超）は warn
- レース記録がPBを3%以上上回る場合は warn（PB更新か入力ミスかを本人に確認させる）
- 区間ラップの合計が記録と合わない場合は warn
- **本数と実施タイムの数が合わない場合は warn**。パーサは不自然な数値を捨ててから
  渡してくるので、捨てられた事実はここでしか分からない。黙って本数が減ると、
  平均も垂れ幅も別のセッションの数字になる

「ありえないが本当」という値は中距離の練習データには無いので、迷ったら止める側に倒す。

### K 補強の取り込み

実際の日誌には「体幹」「プライオ」「ウェイト」が普通に混ざる。従来はカテゴリ未確定で
全部落ちていた。

補強は `SessionCategory` を増やさず、既存の `StrengthSession` へ流す。
`SessionCategory` はルールエンジン22本・GRP比率・転移度・負荷計算がすべて参照している型で、
ここに `strength` を足すと「補強は質練習に数えるのか」「ACWRにどう乗るのか」を
既存の全ルールで決め直すことになる。補強には接地回数など走練習とは別の規則が既にあるので、
そちらに寄せた。走練習側の経路に入れると ACWR で二重計上される。

判定語は走練習の語より**後ろ**に置いてある。`ジョグ40分＋体幹` を補強として登録すると
ジョグ40分が有酸素量から丸ごと消えるため、走った分を残す方を優先する。
`体幹30分` だけの日は走の語が無いので補強に落ちる。

接地回数（`接地120回` / `120接地`）はプライオの週間管理に使うので拾う。

### L 表記辞書

一括入力が読めない語は必ず出る。所属チーム固有の呼び方があるからで、
組み込みルールをいくら足しても追いつかない。

読めなかった行を人が直したあと、その場で「この書き方を覚えさせる」と押せば
語とカテゴリの対応が保存され、次から自動で通る。
**本人の登録は組み込みルールより優先する**（「セット走」を閾値走と呼ぶチームも
CV走と呼ぶチームもあるので、こちらが決めてはいけない）。
登録済みの語は「覚えた書き方」から確認・削除できる。

LLMは使っていない。同じテキストを貼れば必ず同じ解釈になるという性質を壊さないため。

### G 同一処方の経時比較

カテゴリ・1本の距離・本数が一致したものを同じ処方とみなす（レストは一致条件に含めず、表に併記）。

**平均タイムと垂れ幅を必ず別々に判定する。** 平均が速くなっていても垂れ幅が
広がっていれば、それは1本目を突っ込んだだけで後半維持は悪化している。
耐乳酸型の選手は1本目を速く入りやすいので、この2つを1文にまとめると判断を誤る。

### H CFEの予測レンジ

「1:51.0、信頼度0.48」と出しても、その0.48を使って判断はできない。
`1:50.2 〜 1:51.8` と幅で示す。信頼度が低いほど広く、実測どうしのばらつきが
大きいほど広くする。**表示専用**で、プラン生成や設定ペースの計算には使わない
（計算に使うと「幅のどこを採るか」という新しい恣意性が入る）。

ホームのCFE推移グラフは軸を反転しない（下がるほど良いことは「下がるほど良い」の一文と数値横の矢印で示す）。
分析タブのCFE推移（LineChart）も反転していないため、Sparkline側だけ反転すると同じデータが画面によって上下逆に見えてしまう。見え方が画面ごとに変わる害の方が大きいと判断した。

### I レース配分シミュレータ

目標タイムと**本人の過去レースの実測落ち幅**から3案を出す。
実測どおり／落ち幅を0.5秒詰める／0.5秒広げる、の3つ。

過去レースの区間ラップが2本未満のときは**案を出さない**。
一般論の配分を出しても、本人の落ち幅が分からないまま信じてレースを壊すだけになる。
代わりに「何本必要か」「どこに入れるか」だけを伝える。

---

## 改修指示書v4（M-1〜M-12）への対応

### M-1 練習結果の保持と上書き

記録画面は保存後も同じセグメントに留まり、入力済みの値を表示したままにする。
既に登録がある場合は初期値として読み込み、ボタンを「上書きして保存する」に変える。

**再保存は上書き。** 新しいidで積むと記録が二重に残り、負荷も達成度も二重に数えられる。
さらに CFE が同じ練習で2回動き、「1回の更新は±1.5秒まで」というガードレールが実質±3秒になる。
そのため `processResult` は既存の記録のidを引き継ぎ、
`revertCfeForSession` で前回この練習が動かしたぶんを取り消してから入れ直す。
取り消しは後続の更新の差分（after − before）を保ったまま行うので、
古い記録を直しても以降の履歴が壊れない。

### M-2 直近の状態に応じた設定の調整

**CFEと設定ペースを分ける。** CFEは「能力の推定」、設定ペースは「今日出せる値」。
実行できなかったことは能力低下とは限らない（暑かった、寝ていない、設定が高すぎた）。
どれも CFE を下げる理由にはならないので、設定だけを動かす。

判断材料は4つとも使う。

- 直近3回の同カテゴリの実行状況（設定との乖離・垂れ幅・打ち切りの有無）
- その日のコンディション（睡眠・疲労・安静時心拍）
- ジョグの心拍。同じペース帯（中央値±20秒/km）で平均心拍が +4bpm 以上なら疲労とみなす。
  ジョグを「消化した記録」ではなく「状態の測定値」として扱う
- ポイント練習の心拍（Q-1）。同じ距離で、直近2本の平均心拍が
  それ以前より +4bpm 以上なら疲労とみなす。こなせてはいても状態は落ちている、を拾う

閾値と上限（`src/lib/core/adaptive.ts`）:

- 直近3回の平均乖離が **+2.0秒/本** を超えたら緩める
- 緩める上限は **3%**、締める上限は **1.5%**。
  締める側を保守的にしているのは、速く走れた事実は能力の主張だから
- その日の状態による調整は最大 **2%**
- 黄信号は既存ルールに合わせ、強度を維持したまま量を30%減らす
- 暑熱フラグの付いた実測は判断材料から外す（環境要因は M-9 が別に扱う）

提案は保存しない。何をどれだけ動かしたかを出して、本人が適用するか辞退するかを決める。
辞退は記録され、同じ提案を出し直さない。

#### S-10 画面で何を言うか

「設定の調整案」と見出しを出して増減の秒数だけ並べても、**何をどうしたいのかが伝わらない**。
差分を読ませるのではなく、次の3つを順に出す。

1. どちらに動かすのか（見出しで言い切る。「今日の設定を ゆるめる提案」）
2. **変更前と変更後の処方を、文面そのままで並べる**（`beforePrescription` / `afterPrescription`）
3. そう判断した材料（`reasons`）と、**CFEには触れないこと**

3つ目は必ず書く。設定とCFEの混同はこのアプリで最も起きやすい誤解で、
「暑かった・寝ていない・設定が高すぎた」はどれも能力が落ちた理由にならない。

ポイント練習の日以外は、その日の枠ではなく**次のポイント練習**の案を出す。
今日の枠に固定していたため、ジョグの日には調整案が一切見えていなかった。
ジョグと休養には設定が無いので、固定しても出るものが無い。

#### Q-1 ポイント練習の心拍の扱い

1本ごとの平均心拍（`RepResult.avgHr`）は**任意項目**。時計から拾える人だけが入れるので、
無いほうが普通という前提で扱う。無くても比較もM-2も今までどおり動く。

- 記録画面では既定で欄を出さない。常時2欄にすると1行に6欄並び、
  iPhone幅（390px）で実施タイムの欄まで潰れる。出すときは3列→2列に落として幅を確保する
  （E2Eが最小幅を実測している）。
- 心拍と「何本目か」の対応は、実施タイムを間引く**前**に取る。
  先に間引くと、途中の本が空のときに心拍が1本ずつずれる。
- **心拍が上がっていても、タイムが 0.3秒/本 以上速くなっていれば疲労とみなさない。**
  速く走れば心拍は上がる。上がったこと自体を悪い材料にすると、
  速く走ったのに疲労と判定されることになる。
- 判定のしきい値は用途で分ける。M-2の疲労判定は **4bpm**、
  同一処方の比較で「傾向あり」とみなすのは **3bpm**。
  比較側が緩いのは、そちらは表示だけで設定を動かさないため。
- 一括入力のパーサには読ませていない。日誌に1本ごとの心拍が書かれることは稀で、
  読める書き方を増やす代償のほうが大きい。

#### R-1 心拍が実際に何に効いているか

**心拍は補助。無くても全機能が動く。** 心拍が無いことを理由に何かを止めたり、
値を推測で埋めたりしない。判定できないものは判定できないと出す。

| 心拍 | 使っている場所 |
| --- | --- |
| ジョグの平均心拍 | `jogEfficiency`（M-2の材料） |
| 1本ごとの平均心拍 | `qualityHrTrend`（M-2）／同一処方の比較の `hrTrend` |
| 暑熱ブロックの心拍 | 順化判定（同一ペースでのHR低下） |
| 最大心拍 | 相対強度の基準（`hrMaxReference`） |
| 心拍の差 | 暑熱の切り分け（`heatHrEvidence`） |

`src/lib/core/heartRate.ts` に置いた判断:

- **最大心拍を年齢の式（220−年齢など）から推定しない。** 個人差が±10拍以上あり、
  推定を基準にすると強度の判定がその誤差ぶん動く。本人が入れた実測値が無ければ
  記録の中の最高値を使い、**それが実測の最高値であること**（本当の最大とは限らない）を明示する。
  どちらも無ければ相対強度を出さない。
- **1本が2分未満の練習では、心拍から強度を判定しない。**
  心拍が定常に達するまで1.5〜2分かかる。300mを42秒で走る練習の平均心拍は
  実際の負荷よりはっきり低く出るので、そのまま読むと「強度が足りていない」と誤読する。
  短い練習の心拍は、同じ設定どうしの比較（`qualityHrTrend`）にだけ使う。
  高乳酸・モデリング・神経系はこの理由で強度帯を持たせていない。
- 暑熱の切り分けは、**同じ設定・同じかそれより遅いペースで心拍が5bpm以上高い**ときだけ
  環境要因の裏づけとして扱う。心拍が変わらなければ「暑さのせいだと言い切る材料は無い」と書く。
  速く走ったことによる上昇は裏づけにしない。

「使われているか」は分析タブの「現在地 → 心拍の使われ方」で実際の記録に対して出している。
保存されているだけで誰も読んでいない項目を作らないための枠でもある。

### M-3 中止基準

処方に必ず打ち切り条件を付ける。

- 高乳酸・モデリング: 許容 +2.0秒、**1本でも**超えたら打ち切り
- それ以外: 許容は設定の1.5%（最低2.0秒）、**2本連続**で超えたら打ち切り

打ち切りは失敗ではない。`SessionResult.aborted` を立て、
CFE の SKIP-06（中断を未達として反映）を**適用しない**。
「2本連続で超えたら止めろ」と指示しておいて、止めたことを未達として響かせるのは筋が通らない。
打ち切りは設定側（M-2）が受け取り、2回続けば設定が高すぎると判断する。

予定した距離の途中で止めた本も捨てない。たとえば `500m×3` の最終本を400mで
止めた場合は、`RepResult.distanceM=400` と実測タイムを保存し、元の予定500mと設定は
`plannedDistanceM` / `plannedTargetSec` に残す。比較・週次レビュー・次回補正では
予定距離相当へ比例換算し、400mの56秒を500mの56秒として扱わない。
FIT等の丸めで中断扱いにならないよう、予定との差が0.5m未満なら同距離とみなす。
これは競技上の許容幅ではなく、保存値のサブメートル誤差を吸収する境界である。

根拠: 設定から大きく外れた本数を消化しても狙った代謝刺激は入らず、疲労だけが残る。
耐乳酸型は1本目を突っ込んで後半が垂れやすく、垂れた状態の消化は糖枯渇練習に変わる。

### M-4 セッション中の入力

`/run?sessionId=...`。1本入れるごとに続行か中止かを返す。
画面に出すのは3つだけ（直近の1本・設定との差・続行か中止か）。
レストの間に読める情報量には限りがある。

入力途中は kv に保存するので、画面を閉じても消えない。
終了時にそのまま `SessionResult` になり、二度打ちにならない。
走行中は誤タップの元になるので FAB を出さない。

### M-5 予定の編集・移動

カレンダーの操作は3つ。

- **日付の行をタップ** → その日の記録画面へ（行全体がリンク）
- **✎** → 編集シート（内容・設定タイム・所要時間・移動・削除）。セッションの長押しでも開く
- **＋** → その日に練習を足す（同じ日に午前・午後の2本を残すときにも使う）

長押しだけにしていない理由: iOSの長押しは取りこぼすことがあり、
リンク上ではプレビューが割り込む。押せる場所を用意しておく。
編集・追加のシートは日付リストの**上**に出し、開くときに画面上部までスクロールする。
日付が何十行も並ぶ画面なので、下に出すと画面外に出てしまい「押しても何も起きない」ように見える。

**確定前に必ずルール検査を通す。** セッションを差し替えた状態でルールエンジンを走らせ、
必ず元に戻してから結果だけを返す。新しく ERROR が出る変更は既定で適用せず、
何が壊れるかと、前後7日で置ける日（最大3件）を出す。
本人が選べば強行できるが、強行したことを変更履歴に残す。

プラン全体は作り直さない。作り直すと手で直した内容が消える。

### M-6 レース前の変則調整（テーパー）

原則は「量を落として強度は維持」。強度まで落とすとレースペース感覚と神経系の出力が落ちる。

| 段階 | 内容 | 週の総量 |
| --- | --- | --- |
| 14日前〜 | 高乳酸の本数を減らす（設定は変えない） | 100% |
| 10日前〜 | ジョグの量を落とす | 80% |
| 7日前〜 | 高乳酸を外す（回復が間に合わない） | 65% |
| 3日前〜 | 刺激のみ | 40% |
| 前日 | 流しと動き作り | 20% |

固定セッション（チーム練習）は動かさない。動かせない前提で他を削る。
調整は差分として出し、適用するまで予定は変わらない。辞退もできる。

**M-2との干渉を切ってある。** テーパー期は「直近の出来が悪いから落とす」のではなく
「意図的に落とす」期間なので、両方を掛けると二重に落ちる。
テーパー期は M-2 の量の調整を止め、設定ペースの調整（実行可能性の担保）だけを残す。

### M-7 制限因子の自動判定

400mPBと1500mPBで800mの現在地を両側から挟む。

- 400m側の妥当域 = 400mPB×2 + 9〜12秒
- 1500m側の妥当域 = (1500mPB − 15〜20秒) ÷ 2

遅れが大きい側が「使い切れていない資源」。差が0.5秒を超えたところで判定する
（1秒にすると両側1秒以内の選手が全員「バランス」になり、判定が働かない）。

伊藤選手（400m 49.0 / 1500m 3:56 / 800m 1:49.51）:
400m側の中央1:48.5に対して1.0秒遅く、1500m側の中央1:49.25に対して0.26秒遅い。
→ **制限しているのは後半の維持**。
目標1:48.90に必要な換算差は10.9秒で標準（9〜12秒）の範囲内なので、
400mを速くする必要はない。

判定は表示だけでなく**生成の配分に反映する**。重み1未満のカテゴリの枠を、
重みが最大のカテゴリに振り替える（維持が制限なら CV → 経済走）。
振り替えた日付・元・先は `limiterSwaps` として返し、必ず本人に見せる。

#### Q-2 足りていないカテゴリの提案

ルールエンジンは「多すぎる・詰まりすぎ」だけを見ていて、
**「足りていない」側は誰も見ていなかった**（RULE-16 の ACWR 低下は総負荷の話で、
カテゴリの偏りは検出しない）。固定曜日設定を使っていると、その枠に収まる範囲でしか
メニューが出ないため、4週まとめて見ると特定のカテゴリが薄いまま進む。

`src/lib/core/trainingBalance.ts` が月曜始まりの現在週と直前3週を共通集計し、
`coverage.ts` がカテゴリ不足の提案と組み合わせる。

- 予定距離/予定負荷と、結果から得た実施距離/実施負荷を別々に持つ。
- 週ごとの高負荷日、高乳酸・解糖系、中距離特異的、有酸素高強度、ロング走、
  完全休養・回復、実施率、変更/中止/期限超過を表示する。
- 実施4件未満では傾向を断定せず、「記録不足」と次の行動だけを出す。
- 前週比急増、高負荷3週連続、高乳酸の直近10日集中、回復不足、実施率低下を
  根拠の件数・日付と一緒に出す。
- 保存カテゴリは変更せず、警告/集計用に
  `高乳酸・解糖系 / 中距離特異的 / 有酸素高強度 / 神経・スプリント /
  低〜中強度有酸素 / 回復 / 筋力・補強` へ写像する。

- **基準の回数は別表で持たない。** 生成器の週テンプレート
  （`categoryCountsPerFourWeeks`）をそのまま4週ぶん数える。
  別表にすると、生成の中身を変えたときに提案の基準だけが古くなる。
- 制限因子（M-7）の重みを掛ける。維持が制限なら経済走の基準が ×1.3 になる。
- **不足2回から提案する。** 1回の差は週テンプレートの巡り合わせで普通に出るので、
  毎週なにか言われる状態になり、提案そのものが読み飛ばされる。
- 数える対象は**実施したものだけ**。過去日に予定のまま残っているものは数えない
  （あるつもりで足りない、を防ぐ）。過去データ入力ぶんは**含める**
  （実際にやった配分を見たいので、入力経路で回数が変わってはいけない）。
- 置き換え候補はジョグの枠から選ぶ。ポイント練習を別のポイント練習に替えると、
  減らした側が今度は足りなくなる。
- **ロングランは他に候補が無いときだけ出し、代償を必ず文章にする。**
  有酸素の土台は数週間かけて落ちるので、連続で潰すと別の問題が出る。
- 固定曜日設定そのものは書き換えない。動かすのはその週の予定1件だけ。
  適用するかは本人が選ぶ（M-2 / M-6 と同じ作法）。
- 置き場所はホームではなく分析タブの「現在地」。4週の振り返りは「今日やること」ではないので、
  ホームの原則を崩さない。

#### S-12 何の画面かを先に書く

表と不足回数だけでは、**何を見て何をすればいいのか分からない**。順番を変えた。

1. 何のための画面か（固定曜日の枠に収まる範囲でしか出ないので、薄いまま進む種類がある）
2. **おすすめを1つだけ**（「経済走を あと3回 増やす」）。表を読んでから考えさせない
3. 内訳の表（「やった / 目安 / 不足」。「実施 / 基準 / 差」から言い換え）
4. 入れ替え候補

**M-2（今日の設定）との違いを画面に書く。** 「今の状況に応じた強度」という同じ言葉で
両方を指せてしまうので、こちらは1か月の組み立ての話だと明記する。

カレンダーにも1行だけ出す（`CoverageStrip`）。予定を組み替えるのはカレンダーなので、
そこで気づけないと意味が無い。内訳と入れ替えは分析タブに任せ、
カレンダーには結論1行と導線だけ置く（表まで置くと日付が画面から押し出される）。

### M-8 600m通過を主指標にする

目標1:48.90の基準線: 400m通過 54.5 ／ 600m通過 1:21.5 ／ 残り200m 27.4。

材料は800mレースの区間ラップ、600m単発、300+500のような分割走。
200m刻みのラップがあれば通過はそのまま足す。400+400しかない場合は
後半400の48%で按分し、**推定であることを画面に出す**（単純な半分では速く出る）。

材料が2本未満のときは指標を出さない。1本しかないものを線にすると、
たまたまの1本が現在地に見える。

### M-9 WBGTから設定を自動で緩める

WBGT区分ごとの基礎補正（注意1% / 警戒2% / 厳重警戒3.5% / 中止5%）に、
暑熱耐性の係数（low ×1.5 / high ×0.7）を掛ける。

補正は**表示上の設定にだけ**掛かる。実測をCFEに戻すときは `normalizeForHeat` で差し引く。
これをやらないと夏場の実測がすべて「能力低下」として記録される。
WBGTも気温・湿度も無ければ補正しない（推測で埋めない）。
有酸素系は設定をCFEから作っていないので対象外。

### M-10 接地時間の疲労指標化

同じペース帯（中央値±20秒/km）で、基準（8日以上前の中央値）から
**+5%以上**の状態が3日続いたら疲労として出す。
主観や安静時心拍より早く出ることがあるので、故障の予兆としても使う。
CSVは列名を見ず「日付, 接地時間(ms), ペース」の位置で読む（時計ごとに列名が違うため）。
データが無ければ何も出さない。

### M-11 週次レビューの自動生成

定型文の組み合わせで作る。LLMは使わない。同じ週の同じデータからは必ず同じ文章が出る。

数字は必ず実測を引用する（「調子が良かった」ではなく
「設定41.5秒に対して平均42.0秒、垂れ幅+1.0秒」）。
平均と垂れ幅は別々に評価する（平均が設定どおりでも垂れているなら入りが速すぎる）。
打ち切りは「中止基準にしたがって止めたもので、失敗ではありません」と書く。

### M-12 データの書き出しと復元

全データをJSON1ファイルに書き出す。復元は「上書き」と「統合」を選べる。
統合はidで突き合わせ（日次コンディションは日付で）、重複を作らない。
14日以上書き出していなければホームで促す。

iPhoneのPWAはストレージが消えることがあり、
数か月ぶんの実測が消えると現在地の推定が全部やり直しになる。
派手さは無いが、失ったときの損失は他のどの機能より大きい。

### v4 で足した保存先

単独のテーブルを立てるほどの構造を持たないものは kv（キー・バリュー）に置く。
セッション途中の入力、辞退した提案、接地時間、最後に書き出した日時。
検索キーが要るものはここに置かない。

### 対象1: PWAの保存保証

以前は `persistState`（`pwa/memory-store.ts`）が `void` を返し、IndexedDBと
localStorageの両方が失敗しても呼び出し側に何も伝わらなかった。画面は
「保存した」つもりのまま、実際は端末に何も残っていないことがありえた。

`persistState` を `Promise<PersistOutcome>` にし、失敗理由（`quota` /
`unavailable` / `unknown`）まで返すようにした。IndexedDBアクセスは
`StateIndexedDb` として注入可能にしてある——IndexedDB自体がテスト環境
（Node/vitest）に存在しないため、`supabase.ts` の `fetchImpl` と同じ形で
「成功」「失敗」を明示的に表現したフェイクを注入し、実際の分岐を検証できる
ようにするため（新しいテスト用ライブラリは入れていない）。

両方失敗した場合は `pwa/entry.tsx` の `PersistFailureBanner` が画面上部に
文言で表示する（色だけに頼らない。`role="alert"` / `aria-live="assertive"`）。

`pagehide` と `visibilitychange`（`hidden`）で `flushPendingState()` を呼び、
250msのデバウンス待ちでアプリ終了直前の更新が失われないようにした。

### 対象2: 安全なバックアップ復元

以前は `isBackupFile` が `format`・`version`・`data` の存在しか見ておらず、
`replace` モードは検証前に `resetAll()` していた。壊れたバックアップで
「全消去してから壊れたデータで復元を試みる」＝データ消失になりえた。

`validateBackup`（`src/lib/core/backup.ts`）を追加し、各コレクションが配列
であること・idキーの型が正しいことを、`resetAll()` より前に確認する。
距離・時間などドメインの値そのものは検証しない——読めなかった値を推測で
埋めないのと同じ理由で、過剰に断定的な検証をすると、将来フィールドが
増えた正常なバックアップまで拒否しかねないため。

さらに実際の書き込み（`resetAll()` を含む）を `Store.transaction()` で包み、
検証をすり抜けた想定外のデータで途中失敗しても、開始前の状態へ完全に戻す。
SQLite（`Repo`）は実際の `BEGIN`/`COMMIT`/`ROLLBACK`（3つの実体すべてが
`exec()` で同じ生SQLに対応するため、新しい抽象を足していない）。IndexedDB
（`MemoryStore`）はJSが単一スレッドであることを利用し、実行前の状態を
スナップショットしておいて例外時に差し戻す方式にした（差し戻し後に
`touch()` を呼び直さないと、差し戻し前の状態を指したままの永続化予約が
そのままIndexedDBへ書き込まれてしまう点に注意）。

実際に確認した効果: `validateBackup` を外した状態で壊れたセッション
（`id` が数値）を復元させると、**IndexedDB側は例外すら出さず、配列へ
そのままpushしてしまう**（配列pushには型チェックが無いため）。検証を
`resetAll()` より前に置くことが、単なる「順序の好み」ではなく、
IndexedDB経路では唯一の防御であることを確認した。

---

## 改修指示書v5（N-1〜N-3）への対応

### N-1 1文字打つたびにキーボードが消える

`ResultForm` の中でラベル用のコンポーネント `L` を定義していた。

```
function ResultForm(...) {
  const L = ({ label, children }) => (<label>...{children}</label>);   // ← これ
  return (... <L label="RPE"><input .../></L> ...);
}
```

`L` は再描画のたびに別の関数になるので、Reactは「別の種類の要素」とみなして
中身の `<input>` を作り直す。作り直された入力欄はフォーカスを失い、iOSではキーボードが閉じる。
1文字ごとに再描画が走るので、1文字ごとに閉じる。19か所で使っていたため、
練習結果のほぼ全部の欄が該当した。`L` をモジュール直下に出して解決。

**この不具合は画面に何も出ない。** 見た目は正常なので、実機で打ってみるまで気づけない。
そのためE2Eで「1文字ずつ打ってフォーカスが保たれること」を機械的に見張っている。
壊れた状態に戻して、この検証が実際に落ちることも確認済み。

なお `app/setup/page.tsx` の `F` は `{F("名前", "name")}` という**呼び出し**で、
要素の種類が変わらないため同じ問題は起きない。
判定基準は「コンポーネントの中で定義した関数を `<X>` としてJSXで使っていないか」。

### N-3 メニュー本文からカテゴリを判定する

`src/lib/core/prescription.ts` の `parsePrescription`。

**中身は一括入力の `parseRow` に日付を足して渡しているだけ。** 独自に読み直さない。
同じ「300(42)×5 r5min」が一括入力では高乳酸、編集シートでは別の何か、
ということが起きると、どちらの数字を信じればいいのか分からなくなる。
表記辞書も同じものを通す。

判定は断定しない。根拠（「設定 42.0秒/300m はGRPの98%」「表記辞書「セット走」」）を出し、
その場で直せるようにする。設定タイムが書かれていなければカテゴリは一意に決まらないので選ばせる。
保存前に M-5 のルール検査を通すので、カテゴリが変わって高乳酸の間隔に触れれば止まる。

**`@設定` 表記に対応した。** このアプリが生成する処方は
`300m × 5 @40.9〜41.7秒 r5分` と書くので、これを読めないと
自分が出したメニューを自分で「カテゴリ未確定」と判定することになる。
ただし距離×本数が書かれているときだけ設定として扱う。
ジョグの `@5:00〜5:20/km` を1本の設定と読むと種別を取り違えるため。

### N-2 本文に合わせて入力欄を組み立てる

読み取った構造に沿って入力欄そのものを組み替える。
編集シートは1本ごとの**設定タイム**（`300(42)+600(1:26)+600(1:26)` のように
区間ごとに設定が違うメニューがあるので、1つの設定欄では表せない）、
記録画面は1本ごとの**実施タイム**（貼り付け用に、まとめて入れる方式も残して切り替えられる）。

**入力中に欄を作り直さないこと。** 素直に実装すると N-1 と同じ不具合を自分で作る。

- 解釈は入力が止まってから走らせる（300ms のデバウンス）
- 欄の作り直しは `shape`（種別・距離・本数の指紋）が変わったときだけ。
  設定タイムは `shape` に含めない。設定だけ直したときに欄を作り直すと入力中の値が消える
- 本数が増えたら空欄を足すだけ。減っても値は捨てない（`resizeValues`）。
  打ち間違いで一時的に「×3」になったときに4・5本目を消すと、入れ直しになる
- 入力欄の `key` は「何本目か」で固定する。本文や解釈結果を key に使わない
- 本数の上限は30。打ち間違いで「×99」と入っても欄を99個作らない
- 読み取れない本文では欄を変えない。空にもしない

記録画面の欄の数は「処方の本数」「入力した本数」「既に入れた値の数」の最大を採る。
処方が3本でも実際に5本やったなら5本ぶん入れられる必要がある。

#### S-2 距離・時間・平均ペースは2つで足りる

3つのうち2つ入れれば残り1つは決まる。計算は一括入力と同じ `completeRunTriple` を使う
（同じ数字の関係を2か所で実装しない）。以前は「距離＋時間 → ペース」の向きしか無く、
時計で平均ペースを見た人は距離を自分で割る必要があった。

3つとも入っていて食い違うときは、**どれが正しいかを決めずに知らせるだけ**にする。
勝手に上書きすると、どれが実測でどれが計算かが分からなくなる。

`paceOverridden` を立てるのは、距離と時間が揃っているうえで手入力のペースを採ったときだけ。
ペースから距離を出した場合、そのペースは上書きではなく実測そのもの。

#### S-4 レストは本ごとに違うことがある

`IntervalDetail` のレストはセッションに1つしか持てないが、
`300+600+300` のような複合セッションでは区間ごとにレストが違う（300のあと6分、600のあと10分）。
`RepResult.restAfterSec` に**その本のあとの**レストを持たせた。任意項目で、
全部同じなら入れなくてよい。表示は `perRepRestNote` が違うときだけ並べる。

`400(56)+300(42)+200(24) r次の距離walk` は3本の別区間として扱い、設定を
56秒・42秒・24秒、区間後の距離レストを300m walk・200m walkとして展開する。
距離レストは既存の`RepResult.restAfterDistanceM`へ保存するためmigrationは不要。
結果保存後に実際のカテゴリを直した場合は`Session.category`を同じ保存処理で更新し、
CFE・負荷・4週間集計・警告・次回処方を新しい分類から再計算する。

心拍と同じく、**実施タイムを間引く前に「何本目か」で対応させる**。
先に間引くと、途中の本が空のときにレストが1本ずつずれる。

入力欄は既定では出さない。3欄（タイム・心拍・レスト）を横に並べると
iPhone幅で潰れるので、**3欄になったら1本1行に落とす**（E2Eが最小幅を実測。105px）。

#### S-3 メニューの入力方法を1つにする

自作メニューの登録だけ本文のテキスト1つで、1本の距離を手で入れ直す作りだった。
記録画面・編集シート・追加シートと同じ `PrescriptionFields` を使うようにして、
**本文を打てば欄が組み上がり、距離・本数・レストは解釈から取る**。
同じ「メニューを打ち込む」操作が画面によって違う手数になるのをやめた。

**編集シート・追加シート・ホームのTODAYは実装を共有する。**
本文に合わせた入力欄は `app/components/prescription-fields.tsx`、
メニューの変更シートそのものは `app/components/session-edit-sheet.tsx` が唯一の実装。
画面の外枠（ヘッダー・タブバー・FAB・下端の余白）は `app/components/app-shell.tsx` で、
Next.js の `layout.tsx` と PWA の `entry.tsx` の両方がこれを使う。
以前は枠を2か所に書いていたので、片方だけ直しても片方の実行環境では直らなかった。

同じ組み替え規則を2か所に書くと、片方だけ直したときに同じ本文が画面によって違う欄になり、
どちらの数字を信じればいいのか分からなくなる（解釈を `parseRow` の1か所に集めているのと同じ理由）。
共有できていることはE2Eで確認している。組み替え規則を1つ壊すと両方の画面のE2Eが同時に落ちる。

### P-4 下端の余白（タブバー・FABに隠れない）

下部タブバーとFABは `position: fixed` なので、スクロール領域が自分でそのぶんの
余白を持たないと、最下部の要素が裏に入る。**スクロールしきったように見えるので気づけない。**

余白は `--main-bottom-pad`（globals.css）で1回だけ計算し、`app-shell.tsx` の `main` が
それだけを見る。画面ごとに padding を足して回ると、足し忘れた画面だけ隠れる。
内訳はタブバーの高さ（セーフエリア＋55px）＋FABの逃げ（78px）。
**セーフエリアを定数で見込まないこと。** iPhoneでは34px あるので、
「だいたい96px」と置くと実機だけ足りなくなる。

`calc()` をユーティリティ側（`pb-[calc(...)]`）に直接書かないこと。
Tailwind がクラスを生成しないことがあり、生成されなくても画面には出ず、
最下部が隠れるだけなので気づけない。CSS変数にしてから `pb-[var(--main-bottom-pad)]` と書く。

E2Eの「P-4」が全画面で最下部の要素とタブバー上端を実測して比べている。

## UI/UX・iPhone操作性・アクセシビリティ改善

`BACKLOG.md` の「iPhone・アクセシビリティ」項目のうち、実機が無くても
対応・検証できるものから着手（実機依存の項目は📱のまま残す）。

### 320px幅（iPhone SE相当）での横スクロール検証

既存の横はみ出しE2E（`pwa/e2e.mjs`）は390px幅（iPhone 12〜14相当）でしか
検証していなかった。同じチェックを320px幅でも回すようにした
（全画面を2つの幅で二重に確認する。追加の実装は無し、検証範囲を広げただけ）。

### タップ領域44×44の統一

CSSクラスレベルでは44px下限が効いていたが（`.btn-volt`/`.btn-ghost`/入力欄）、
個別に上書きしている箇所に抜けがあった:

- `.seg button`（ホーム3セクション切替・記録タブなど）が40px → 44pxへ
- [app/results/page.tsx](app/results/page.tsx) 故障ログの「+記録する」トグルが32px → 44pxへ
- [app/plan-settings/page.tsx](app/plan-settings/page.tsx) 自作メニューの「+登録する」トグルが36px → 44pxへ
- カレンダーの期間送り矢印（`←`/`→`）は高さは44pxあったが**幅**が1文字ぶんしか無かった
  （`min-w-[44px]`を追加。`aria-label`も無かったため合わせて付与）

E2Eに実測チェックを追加（セグメントタブの高さ・期間送りボタンの幅）。
T-4で数値を戻して赤に戻ることを確認して復元。

FIT取込のlap一覧（`FitImportCard`）の各行`<select>`（32px）は対象外にした——
1画面に何行も並ぶ密なレビュー用リストであり、ネイティブ`<select>`自体のタップ
挙動はOS側が担保するため、主要な導線のボタンと同列には扱わなかった。

### ConfirmDialogのアクセシビリティ

破壊的操作の前に出る`ConfirmDialog`（`app/components/ui.tsx`）に、モーダルとしての
基本的な振る舞いが無かった:

- `role="dialog"` / `aria-modal="true"` / `aria-labelledby`（タイトルとの関連付け）
- 開いた瞬間にダイアログ内へフォーカスを移す
- **Escapeキーで閉じる**（`onCancel`を呼ぶ）
- **Tabキーがダイアログの外へ抜けない**（フォーカストラップ。最後の要素でTabすると
  先頭へ、先頭でShift+Tabすると最後へ戻る）
- 閉じたときに、開く前にフォーカスがあった要素（呼び出し元のボタン等）へ戻す

`onCancel`はほとんどの呼び出し元がインライン関数で渡すため、`useEffect`の依存配列に
そのまま入れると親の再描画のたびにeffectが走り直し、開いている間もフォーカスが
先頭ボタンへ引き戻されてしまう。**最新の関数はrefで保持し、effectの依存は`open`だけ**
にした（値が変わるたびに走ってほしいのは開閉のタイミングだけで、関数の中身の
変化には反応しなくてよい）。

`ConfirmButton`はこの`ConfirmDialog`を内部で使っているため、修正は自動的に反映される。

**検証**: 単体テストの土台（jsdom・testing-library）がこのプロジェクトには無く、
既存のテスト方針が「コアロジックは単体テスト・画面はE2E」で統一されているため、
E2E（`pwa/e2e.mjs`。自作メニュー削除の確認ダイアログを流用）でrole/aria-modal・
Escape・フォーカス復帰・フォーカストラップの4点を実際のキーボード操作で確認。
T-4で以下を個別に壊し、それぞれ赤に戻ることを確認して復元:
role/aria-modal属性の削除、Tabのラップアラウンド処理の無効化。

### iOSキーボード対策（interactive-widget=resizes-content）

編集フォーム（`SessionEditSheet`・カレンダーの追加シート）を調べたところ、
保存ボタンは`position: fixed`ではなく通常のドキュメントフロー内にあり、
ネイティブのフォーカス時スクロールで対応できる作りだった——**固定位置の保存
ボタンがキーボードに隠れる、という当初想定した形の問題は無かった。**

一方、下部タブバー・FABは`position: fixed`で、iOSはキーボード表示中も
レイアウトビューポートを縮めない（`visualViewport`は縮むがレイアウトは
縮まない）ため、固定要素がキーボードの裏に取り残われる可能性がある。
`visualViewport`を監視する独自JSを書く代わりに、**標準の
`interactive-widget=resizes-content`をviewport metaに追加**した
（iOS 17.4+ / Chrome 108+。対応していないブラウザは未知のトークンとして
無視するだけなので安全）。これによりキーボード表示中はレイアウト
ビューポート自体が縮み、`position: fixed`要素も自動的にキーボードの上に
再配置される。

`pwa/index.html`と`app/layout.tsx`（Next.jsの`viewport`export。
`viewportFit: "cover"`も併せて追加——PWA版には元々あったが、Next.js側に
無かった既存の抜けだったため揃えた）の両方に追加。

**検証**: E2Eでmetaタグの内容を確認（`interactive-widget=resizes-content`
が含まれること）。**実機での最終確認は別途必要**（Chromiumはキーボードに
よるビューポート縮小を再現しないため）。

### 状態表現を色だけに頼らない（StatusText）

赤・amber文字で警告・エラーを示す箇所（17ファイル・85箇所）のうち、
アイコンも`role`も無い**色だけの箇所が24個**あった。うち21個は
`<p style={{color:"var(--red|amber)"}}>{message}</p>`という同じ形だったため、
共通コンポーネント`StatusText`（`app/components/ui.tsx`）を作り機械的に
置き換えた。アイコン・色の組み合わせは既存の`ViolationList`（`LEVEL`定数：
ERROR=⛔/red、WARN=⚠/amber）に揃え、`role="alert"`（error）/
`role="status"`（warning・success）を付ける。

残り3箇所（`sync/page.tsx`の`<b>`によるインライン強調、`calendar/page.tsx`の
`topSignal`表示2箇所）は、太字という色以外の視覚的な区別が既にあるため
対象外にした。`app/page.tsx`の`Notices`（テーパー予告・書き出し催促の
1行通知）は項目ごとに色が違う配列を素朴にマップしているだけで、片方
（書き出し催促）は元々ニュートラルな灰色であり警告色ではないため、
StatusTextでは無く警告色の項目だけに条件付きでアイコン・roleを足した。

**検証**: 単体テストの土台が無いため（前項と同じ理由）、E2E（S-6の他選手
メニュー換算。PBの差を10秒超にして`converted.notes`が実際に出るケースに
変更）で`StatusText`が`role`とアイコンつきで描画されることを確認。
`StatusText`は1箇所の実装なので、ここで確認できれば他の20箇所の呼び出しも
同じ実装を経由していることの裏付けになる。**T-4でrole/アイコンの出力を
削り、赤に戻ることを確認して復元。**

## 実運用で見つかった5件の修正（2026-07-31）

利用者が実際に使っていて見つけた5件。着手前に各項目を実コード調査で
裏付けてから修正した（推測で直さない）。

### 1. カレンダーの✎ボタンが2件目以降のセッションに届かない

`app/calendar/page.tsx`の`DayRow`は、1日に複数セッションがあるとき
`editable = sessions.find((s) => !s.isFixed)`で**最初の1件しか**✎ボタンの
対象にしていなかった。長押しは行ごとに正しく対象を拾えていたが、iOSでは
長押しが取りこぼされることがあるとコード内に既にコメントがあり、
確実な代替導線（✎ボタン）がここだけ効かなかった。

`editableSessions = sessions.filter((s) => !s.isFixed)`に変え、非固定
セッションの数だけ✎ボタンを出す（複数あるときはカテゴリ色をボーダーに
つけて、どの行に対応するか分かるようにする）。1件のときの見た目は変わらない。

### 2. 実施タイム（カンマ区切り）欄でカンマが打てない

`app/results/page.tsx`の「実施タイム（本数分をカンマ区切り）」欄に
`inputMode="decimal"`が付いていた。JS側の文字除去処理は無く、原因は
iOS等の数値専用キーボード（カンマキーが無い）だった。この欄はカンマ区切りの
**文字列**を受け取る欄で単一の小数ではないため、`inputMode="decimal"`の
指定自体が誤りだった。属性を外して通常のテキストキーボードにした。

### 3. FIT取込でランニングダイナミクスを取得する

`src/lib/core/fitParse.ts`は心拍以外（ピッチ・ストライド・上下動・
接地時間・気温）を一切読んでいなかった。`fit-file-parser`の生成プロファイル
（`node_modules/fit-file-parser/dist/garmin_profile.generated.js`）を実際に
確認し、session/lap/recordメッセージのフィールド番号・スケール・単位を
特定した上で追加（`avg_cadence`=session18/lap17、`avg_temperature`=
session57/lap50、`avg_vertical_oscillation`=session89/lap77(scale10=mm)、
`avg_stance_time`=session91/lap79(scale10=ms)、`avg_step_length`=
session134/lap120(scale10=mm)。record単位は`cadence`=4、`temperature`=13、
`vertical_oscillation`=39、`stance_time`=41、`step_length`=85）。

**cadenceは片脚分（rpm）で記録される**というGarmin FIT SDKの既知の仕様に
従い、2倍して歩数/分（spm）として持つ（`RUNNING_CADENCE_LEGS`）。湿度は
FITの標準recordフィールドとして存在しないため対象外（引き続き手入力のみ）。

集計は`fitToSession.ts`の`deriveFitActuals`で、インターバルは**「メイン」
区間だけ**の平均、持続走は**セッション全体**（無ければlap平均）を使う——
ウォームアップ・クールダウンの遅いピッチを混ぜると実際の巡航ピッチより
低く出るため。温度は既存の`SessionResult.weatherTempC`に載せ、暑熱判定
（WBGT・`heatFlagged`）にもFIT由来の気温がそのまま使えるようにした
（新しい重複フィールドを作らない）。表示はFIT取込確認カードと同一処方
比較表（ピッチのみ、列を増やしすぎない）に留め、新しい分析画面は作っていない。

**検証**: `tests/fitParse.test.ts`・`tests/fitToSession.test.ts`に
先に赤→ 実装 → 緑。cadenceの2倍処理・メイン区間限定平均をそれぞれ
T-4で無効化し赤に戻ることを確認して復元。E2Eにも実際のFITメッセージで
ピッチ・気温が表示されることの確認を追加。

### 4. 「ジョグ＋坂ダッシュ」等の複合メニューを自動生成の時点で分割する

`src/lib/core/periodization.ts`の`hillSprints()`・`strides()`・テーパー期の
「刺激入れ（流し）」は、ジョグと刺激（坂ダッシュ／流し）を**1つの
prescription文字列**に結合していた。以前の対応（forge-v40）は複合本文の
検出・警告表示だけで、生成そのものは直していなかった。

週生成ループ（`generatePlan`）は1日につき`sessions.push`を1回しか呼ばない
設計だが、カレンダー側（`sessionsByDate`）・保存層は元々1日に複数
セッションを問題なく扱える（手動追加の「同じ日に午前と午後」機能が
既に動いている）。そこで`DayTemplate`に`combinedJogMin?: number`を追加し、
設定されていれば（かつ自作メニューを使っていない日は）ジョグ部分を
`jog()`の別セッション（`timeOfDay`を主セッションと分ける）として自動生成
するようにした。`hillSprints`/`strides`/「刺激入れ（流し）」の文面からは
ジョグの記述を削除し、別セッションの文言と役割が重ならないようにした。

**曜日の優先設定で「神経系」を固定する場合も同じ経路を通る**
（`templateForSlot`の`"neural"`分岐が`strides(6)`を返すため）。
ユーザーから「曜日設定でジョグ＋神経系のような複数種目に対応してほしい」
という要望もあったが、新しい型やUI（複数枠選択）を追加しなくても、
既存の単一選択のまま「神経系」を選ぶだけでジョグが自動的に付くことを
確認できたため、そちらは見送った（`SLOT_LABELS`の表示だけ
「神経系（ジョグ込み）」に変更して分かりやすくした）。

**検証**: `tests/periodization.test.ts`・`tests/weekTemplate.test.ts`に
先に赤→実装→緑。分割ロジックをT-4で無効化し、両ファイルとも赤に戻ることを
確認して復元。`npm run verify`実行で978→979件に増加。既存の全テスト
（週次サマリー・ACWR・カバレッジ等、セッション件数の変化に影響されうる
箇所）に回帰が無いことを確認済み。

## リファレンスUI刷新（forge-v45 / 2026-08-01）

`FORGE_UI_IMPLEMENTATION_BUNDLE.zip`（指示書＋リファレンス画像13点）に基づく
見た目の刷新。**ドメインロジックには一切触れていない。**
画面ごとの経緯は `HANDOFF.md`、実行計画は `.claude/plans/swirling-petting-oasis.md`。

### なぜカード内のトラックはSVGなのか

リファレンスの光跡は写実的なレンダリング（モーションブラー・ブルーム・
アスファルトの粒状感）で、これをSVGフィルタで描き起こすのは以前失敗している。
そこで最初は**アプリアイコンの実素材をカードに敷いた**が、文字が読めなくなった。

拡大して確認したところ、**リファレンスのカード内トラックは写真ではなく
細い光の弧**だった。アイコンやスプラッシュの写実的な絵とは別物。
そのため用途で分けている。

- カード・セッション実行画面 → `app/components/brand/forge-track.tsx`（SVG）
- アプリアイコン → リファレンス画像そのもの（`pwa/icon-master.png`）
- スプラッシュのワードマーク → リファレンスから字形を抽出（後述）

### 画像パスをCSSに寄せている理由

`app/globals.css` の `url("./brand-wordmark.png")` で参照している。
JS側でパスを組み立てていないのは、**Next.jsとPWAでURLの基準が違う**ため。

CSSの `url()` は「そのCSSファイルの位置」を基準に解決されるので、
Next.js（`app/globals.css` 基準）でもPWA（`pwa-dist/styles.css` 基準）でも
同じ記述で正しく解決される。JS側で組み立てると必ずどちらかで壊れる
（PWAはハッシュルーティングで配信先がサブパス、Next.jsは絶対パス）。

実体は `app/` と `pwa/` の2箇所に置く。`scripts/build-static.mjs` の
コピー配列と `pwa/sw.js` の `ASSETS` の**両方**に登録が要る。
片方だけだとオフラインで画像が出ない。

### ワードマークを画像にしている理由

指示書が「斜体をかけた普通のテキストを最終実装にしないこと」と明示している。
実際、リファレンスの字形（幅広・角落とし）はフォント指定では再現できない。
`00-app-icon.jpeg` から白い字形だけを `alpha = min(R,G,B)` で抜き出している
（緑の光条はB成分が低いので、この式で消える）。CSSの `mask` として使うので
色は `currentColor` に従う。

**副作用**: 文字としての「FORGE」が画面から消える。読み上げと自動確認のために
見えないテキストを別に残してある（この後退はE2Eが検出した）。

### リファレンスと変えた4点（本人と合意）

1. **下部タブは4つのまま**（リファレンスは5つ）。`CLAUDE.md` の明記ルールを優先。
   リファレンス自身もアイコン一覧では4つしか描いておらず内部矛盾している
2. **カレンダーは月グリッドにしない**。本人がカレンダーでやるのは行ごとの
   編集・削除・追加であり、1日＝点1つのグリッドではどれもできない。
   「強度を一目で」はACWR・カバレッジ・ルール警告が既により正確にやっている。
   リストのまま chrome を削り、強度マーカー（■高/●中/▲低/—休）を足した
3. **分析のPERFORMANCEは置き換えではなく追加**。置き換えると制限因子・600m通過・
   同一処方比較・CFE推移・ACWR・カバレッジ・レース分析が全部消える。
   当初は4つ目のセグメントにしたが、E2Eが既存ルール（セグメントは3つ／
   iPhone幅で詰まる）違反を検出したため「推移」の先頭に置いた
4. **セッション実行画面にタイマーを作らない**。自分の時計で測って入れる、という
   既存の使い方のまま。`NEXT REST` も出さない——レスト時間は `SessionProgress` が
   持っておらず、持っていない値を画面に作らないため

### 表示名を変えた箇所

リファレンスは記録サマリーの3枚目を `AVG PACE` としているが、インターバルで
平均するのは「1本のタイム」でありペースではない。数字の意味と表示名がずれると
後から何を見ていたか分からなくなるので、種別で `AVG LAP` / `AVG PACE` を
呼び分けている（`src/lib/core/sessionSummary.ts`）。

### 視覚回帰（`npm run visual`）

8画面 × 3幅（390/393/430）＝24枚を `visual/baseline/` と比べる。

**判定はハッシュ比較。** 時刻（2026-08-15＝レースまで41日）・データ・
アニメーションを固定してあるので、同じ実装からはバイト単位で同じPNGが出る
（実測済み）。そのため pixelmatch のような画像差分ライブラリを入れていない。
どこが変わったかは `visual/current/` と `visual/baseline/` を並べて見るほうが速い。

**忠実度は自動判定できない。** リファレンスはベゼル入りの合成モックアップで
解像度も縦横比も違うため、実装スクショとピクセル比較しても意味のある差分に
ならない。自動化できるのは「前と変わっていないか」だけ。

ハマりどころが2つある。スプラッシュはReactがマウントすると自分で消えるので
`bundle.js` を読ませずに撮る。また「アプリを準備しています」が無限に点滅し、
撮るたびに不透明度が変わって毎回違うPNGになるので、撮る直前に**その要素だけ**
アニメーションを止めている（全部止めると `forwards` で最終状態を保持している
入場アニメが初期状態に戻ってしまう）。

### 届いていない点

スプラッシュのトラックは、リファレンスでは遠近のついた楕円で左へ光跡の尾が
抜けていく。こちらは正面から見た楕円で尾は無い。写実的なレンダリングの再現は
SVGの範囲を超えるため、差が残っている。

---

## 実運用で見つかった4件の修正（forge-v46 / 2026-08-01）

利用者からの報告4件。FITの本数の数え違いが1件、カレンダーの操作が2件、
分析タブの読みにくさが1件。

### 1. 1本の中を刻んだlapを別々の本と数えていた

報告: `1000m×4 r200jog`（実測 3:16.1 / 3:14.9 / 3:14.2 / 3:14.7）が
**`396m×12`** として読み取られる。

原因は `deriveFitActuals`（`fitToSession.ts`）が **lap 1つ＝1本** と数えていたこと。
時計で1000mの中を 400m / 400m / 200m と刻んで押すと、FITには本のlapが3つ入る。
これを別々の本と数えると、4本のはずが12本になり、
距離は最頻値（`mode`）を取るので400m側の396mが代表になっていた。

`groupIntoReps` を足して、**間に休みのlapが無く、かつ時刻が連続している**
メインlapを1本にまとめるようにした。休み無しで次の本が始まることはありえないので、
この並びは別々の本ではなく1本の中の通過。

まとめる条件を2つにしているのは、記録を止めて再開した場合を切り分けるため。
FITの `total_elapsed_time` は一時停止も含んだ実時間なので、
「前のlapの終わり ＝ 次のlapの始まり」なら、その間に休みは無いと言い切れる
（`SAME_REP_GAP_SEC = 2` は1秒刻みの丸めぶんだけを許す幅）。
時刻が読めないFITでは隣接だけで判断する——間にlapが無い以上、
休みが記録されていないことは確かなので、これは推測ではない。

まとめると通過の内訳が消えるので、`RepResult.splitsSec` に残して
記録サマリー画面（`/summary`）に `1:18.9 - 1:18.5 - 38.7` として出す。
本人はこの形で見ているし、合計だけでは前半で突っ込んだのか最後に落ちたのかが
分からなくなる。合計は必ず `actualSec` に一致する。

### 距離はトラックの距離に丸める（forge-v48で変更）

まとめた実測は 990m（396+396+198）になる。当初はこれをそのまま出していたが、
**丸めるように変えた**（本人の判断）。理由は、同じ 1000m×4 が日によって
990 / 991 / 989 と違う距離で記録され、`samePrescription`（同一処方の経時比較）が
別の練習として扱ってしまうため。GPSの誤差ぶんで比較が切れるのは実害が大きい。

`snapToTrackDistance` が、実測が `TRACK_DISTANCES_M` のどれかから
**3%以内**（`SNAP_TOLERANCE`）ならその距離として記録する。
3%は400mなら±12m、1000mなら±30m。トラックのGPSはカーブで内側を結ぶぶん
1%前後短く出るので、その誤差は吸収しつつ隣の距離には届かない幅にしてある——
一番狭い 1500m と 1600m でも6.7%離れているので、両方の範囲が重なることはない。
どれにも近くなければ**丸めずに実測を残す**（350mは300mとも400mとも言えないので、
350mのまま記録する）。

**丸めたときは必ず警告に出す**（`990m→1000m／ずれは最大1.1%`）。
距離は本数と並んで記録の骨格になる値なので、黙って書き換えると
あとで数字を疑うときに追えなくなる。元の実測は `FitImportRecord.parse` に
そのまま残っているので、解析を変えたときに読み直せる。

### 2. 固定枠をやらなかったときにカレンダーから外せない

固定枠（チーム練習等）は RULE-15 で変更も移動も禁止されており、
カレンダーでは長押ししても断られるだけで、✎ボタンも出ていなかった。
そのため**流れたチーム練習が予定として残り続け**、カレンダーが
これからやることの一覧ではなくなっていた。

RULE-15 が守っているのは「予定の組み替え」であって、
やらなかったという事実の記録ではない。固定枠にもシートを開けるようにし、
中身は変えられないと断ったうえで「やらなかった」だけを記録できるようにした。

**消すのではなく中止（`status: "skipped"`）にする。** 理由は2つ。

- 実施率・中止回数（4週間のバランス）に残る。消すと実施率が黙って上がる
- 消すと次のプラン再生成で復活する。`regeneratePlan` は
  `occupiedSlots`（日付＋時間帯）が埋まっていない枠に生成分を入れるので、
  中止として枠に残っていれば作り直されない

固定枠は後ろ倒ししない（`processSkip`）。日時が決まっているから固定枠なので、
2日ずらした「チーム練習」は実在しない予定になる。理由が「予定」でも
その日のまま中止にする。

中止した予定はカレンダーの一覧から外れ、行の下に取り消し線つきで
`中止 <名前> [戻す]` として残る。`restoreSkippedSession`（`DELETE /api/skip`）で
予定に戻せる——固定枠は手で作り直せないので、押し間違いを戻せないと詰む。
記録が入っているものは戻さない（中止なのに結果がある状態を作らない）。

### 3. カレンダーの表示期間が覚えられない

既定を2週間から**1週間**に変えた。1週間ぶんが1画面に収まって
「今週なにをやるか」が最初に目に入る。長い範囲は見たいときに選ぶ。

そのうえで、選んだ期間を端末に覚えさせる（`app/components/view-pref.ts`）。
これまでは画面の `useState` に持っていたので、記録タブに行って戻ると既定に戻っていた。

Store（練習データ）には入れない。これは「どう見たいか」であって練習の記録ではないし、
端末ごとに違ってよい。書き出し・同期の対象からも外れるべきもの。
読み込みはマウント後の `useEffect` でやる——`useState` の初期値で
localStorage を読むと、Next.js側（サーバーで一度描く）と食い違って
hydration エラーになる。保存された値が許可されている値でなければ既定に戻す
（想定外の値を近いものに寄せない）。

### 4. 分析タブが文字の壁になっていた

同じ言い回しの一文が日数ぶん縦に並び、どこを見ればいいのか分からない状態だった。
**文を消してはいない。** 判断に使う部分を図にし、文は畳んで残した。

**心拍の使われ方**: 「平均心拍 157bpm は最大の 79%（狙いは 65〜78%）。
狙いより強く走っています。…」という一文が8日ぶん並んでいた。
狙いの帯を線で描き、その上に実測の位置を打つ形にした（`HrRow`）。
外れている日は目で分かる。軸は %HRmax の 60〜100 だけを描く——
0から描くと帯（65〜97%）が右端に潰れて、外れているかどうかが見えない。
判定できない日は理由だけを短く出す（`blockedReason`）。文は行を開くと出る。

**制限因子**: 「400mから見た妥当域 1:48.00〜1:51.00」という数字が2つ並び、
その下に同じ数字を言い直した長い文があった。数字の並びからは、
PBがその域のどこにいるのか、目標が域の内側なのかが読み取れない。
2つの妥当域とPB・目標を**同じ数直線**に置いた（`LimiterScale`）。
軸は表示する値の実測の最小・最大から取る——妥当域は数秒の幅しかないので、
固定幅にすると全部が同じ位置に潰れる。根拠の文は畳んだ。

**週次レビュー**: 指導者に渡すための文章なので、この画面で毎回読むものではない。
畳んで、開かずに「コピーする」だけで用が足りるようにした。

### 取り込み済みFITの作り直し（forge-v49で追加）

ラップの読み方を直しても、**すでに取り込んだぶんは古い解釈のまま残る**。
変換は取り込んだ瞬間にしか走らないため。実際、`1000m×4` の読み違いを直したあとも
画面には `400m×12` が残っていた（本人からの報告）。

作り直す関数 `rebuildFitDerived` は前から実装されていたが、
**APIにも画面にもつながっていなかった**（呼び出し元がゼロ）。
`POST /api/fit-import { rebuild: true }` と、データ画面の「FIT取込を作り直す」を足した。

元ファイル（`FitImportRecord.rawBytesBase64` と `parse`）を丸ごと保存してあるのは
まさにこのためなので、読み直せる。実測そのもの（タイム・心拍）は元ファイルの値なので
作り直しても変わらない。変わるのは解釈（本数・距離・カテゴリ）だけ。

紐付け済み（計画済みセッションに結び付けた）ぶんは、新規セッションを作らず
元のセッションのまま更新する。紐付け先が消えていた場合は作り直さず件数だけ報告する
（勝手に新しい予定を生やさない）。

### RULE-06が常時点灯していた問題（2026-08-04）

RULE-06（VLaMax上昇リスクの警告）は、21日間に高乳酸系セッションが**3回以上**あると
WARNを出す仕様だった。Specific期の意図した処方は「高乳酸 週1」（`periodization.ts`、
仕様書4-6の表どおり）で、週1を守っている限りどの3週の窓を切り取っても高乳酸は
ちょうど3回になる。つまり**計画通りに進んでいるだけで常時点灯していた**。

以前は「既知の注意点」に運用上の注記（意図的に出ます、と）を書いて済ませていたが、
これは信号として機能していない。CLAUDE.mdの「きつい練習＝良い練習という評価軸を
作らない」の裏返しで、「計画通り＝警告」も同じくらい良くない——常に点灯している
警告は、本人が慣れて無視するようになるだけで、頻度が本当に積み上がったときの
シグナルを埋もれさせる。

閾値を**4回以上**（`RULE06_THRESHOLD`）に変更した。週1を続ける限り3週の窓は
常に3回で止まるので、4回に達するのは「どこかで週1を超えて詰め込まれた」場合
（自作メニューの重複・生成側の不具合・本人が手で追加した場合など）だけになる。
週1を6週続けても鳴らないこと、3週で4回（週の途中にもう1本入る）はWARNになることを
テストで確認した。

他のルールにも同じ構造（閾値が生成エンジン自身の意図した値と一致していて
常時点灯する）が無いか、一巡だけ機械的に確認する余地がある
（RULE-17は週間走行距離増加を15%で警告するが、生成側は10%を上限にし
`RULE-17の15%注意域へ一度の操作で入らないため`と明記しており、この問題は
起きていないことを確認済み）。

### ルール評価の重複計算（forge-v56 / 2026-08-04）

**症状**: 記録が増えるほどホームと分析タブが重くなる。実測（デスクトップ）:

| セッション件数 | runRuleEngine | dashboard |
|---|---|---|
| 150 | 11ms | 25ms |
| 400 | 63ms | 206ms |
| 800 | 250ms | 750ms |

件数5.3倍でdashboardが30倍。ルール評価がセッション数に対しておよそ**二次**で
伸びるうえ、`weeklySummary` が内部で `runRuleEngine` を呼ぶため、
**同じ計算を何度も繰り返していた**——ホームは2回（自分＋週サマリー）、
分析タブは5回（自分＋4週ぶんのサマリー）。

`runRuleEngine` を `RuleContext` 単位でメモ化した（`ruleEngineCache`、WeakMap）。
ルールは ctx の純関数で、ctx は `buildRuleContext` で組み立てたあと変更されない
（リクエストごとに新しいオブジェクトになる）。WeakMapにしているのは、
ctxが不要になればキャッシュも一緒に回収されるようにするため。

返すのは毎回**複製**。キャッシュした配列をそのまま返すと、呼び出し側が
並べ替えたり足したりした瞬間に別の画面の表示が壊れる。

実測（デスクトップ・測定時点のテスト全件）:

| 経路 | 前 | 後 |
|---|---|---|
| ホーム（dashboard） | 750ms | 521ms |
| 分析タブ（4週サマリー） | 1000ms超 | 251ms |

**検証**: メモ化を外すと落ちるテストを置いた（`tests/ruleEngineMemo.test.ts`）。
挙動が同じことだけを見るテストはメモ化を外しても通ってしまい、
あとで誰かがキャッシュを消しても気づけない（待ち時間が戻るだけで画面に何も出ない）。
初回と2回目の時間差で判定するが、実測で1000倍以上開くので境界は5倍と緩くしてある。

**残っている課題**: `runRuleEngine` 自体が二次で伸びる点は直していない。
多くのルールがセッション列を入れ子で走査しているためで、20以上のルールを
横断する変更になる。安全側の判定ロジックなので、必要になってから
（記録が500件を超えて体感に出てから）まとめて扱う。
現在の実データは137件（約25ms）なので急がない。

### 接地時間がFIT取込と繋がっていなかった（forge-v57 / 2026-08-04）

FIT取込は `SessionResult.avgGroundContactTimeMs` を前から保存していた。
しかし判定側の `contactTimeStatus` は **CSV取り込み専用の保存領域
（`contact:samples`）しか読んでいなかった**。

```
FIT取込 → SessionResult.avgGroundContactTimeMs → （どこにも繋がっていない）
CSV取込 → contact:samples → assessContactTime → 分析タブ
```

その結果、分析タブは「接地時間のデータがありません。**時計から書き出すと**、
主観より早く疲労を拾えます」と出し続けていた。すでにアプリの中にある値について、
手作業でCSVを書き出せと案内している状態だった。
`rebuildFitDerived` と同じ「実装はあるのに配線されていない」型の欠落。

`contactSamplesFromResults`（`contactTime.ts`）を足して、記録から材料を作る。
`allContactSamples`（service.ts）がCSVぶんと合わせて判定に渡す。案内文も直した。

**ペースを必ず添える。** この指標は同じペース帯どうしでしか比べられない
（ジョグとインターバルの接地時間を混ぜて平均を取っても何も分からない）。
持続走は平均ペースをそのまま、インターバルは疾走部分の合計距離・合計時間から出す
——レストを含めると「その接地時間で走っていた速さ」とずれるため。

**本人未確認のFIT結果は入れない**（`trustedResults`）。確認前の値を分析へ流さない
という既存の切り分けに合わせる。

**同じ日・同じ値は片方だけ数える。** 同じ練習をCSVとFITの両方から入れると二重になり、
判定に必要な件数（6件）を実際より早く満たしてしまう。

接地時間が入っていない記録は材料にしない。対応していない時計では undefined のまま
来るので、0で埋めない。

**検証**: 配線を外すとサービス層のテスト3件が落ちる（T-4で確認）。
`tests/contactTime.test.ts` は core の抽出と service の合流の両方を見る。

### 実運用で見つかった3件（forge-v58 / 2026-08-11）

#### 1. Q-2「〜に替える」が反応しないように見える

ボタンは動いていた。押すと `POST /api/coverage` まで届き、`editSession` が
**ルール違反を理由に適用を拒否していた**（ジョグを高乳酸に替えると RULE-01（間隔）・
RULE-03・RULE-04・RULE-06 に同時に触れる、というのが普通に起きる）。

問題は返し方だった。結果は `msg` という1つの状態に入れ、**カードの末尾に灰色の
一文で出していた**。入れ替え候補は数件ぶん縦に並ぶので、押したボタンと結果の
表示位置が画面2つぶん離れる。しかも文面は「この変更はルールに反します。内容を
確認してください」だけで、どのルールなのかも、次に何ができるのかも無い。
本人からは「押しても何も起きない」に見える。

直したのは3点。

- 結果を**押した候補の内側**に出す（`outcomes` を候補ごとに持つ）。
  カード全体で1つの状態にしていたのが距離を生んでいた。
- 止まった理由を `RULE-xx: 内容` の形でそのまま出す。
  カレンダーの移動（M-5）が既にこの出し方をしているので、それに合わせた。
- **「承知のうえで替える」を同じ場所に置く。** `applyCoverageProposal` に
  `force` を足して `editSession` へ渡す。M-5 の移動と同じ考え方で、
  止めるのは既定であって、本人の判断を塞ぐためではない。

**なぜ検査を抜けたか。** E2Eが `fetch("/api/coverage")` を直接呼ぶだけで、
ボタンを押していなかった。APIは正しく動くので通ってしまう。
押す経路に変え、**候補の内側**だけを読んで結果を確かめるようにした
（カード全体を読むと、末尾に出す旧実装でも通ってしまい何も見ていない検査になる）。
そのために候補の枠に `data-candidate` を付けている。

#### 2. カレンダーの週がスクロールしただけで飛ぶ

横スワイプ判定（C-3）が**指の横移動しか見ていなかった**。
`Math.abs(dx) < 60` だけで、縦の動きも所要時間も見ていない。
この画面は日付が縦に何十行も並ぶので実際の操作はほぼ縦スクロールで、
指が弧を描いて横に60px流れることは普通に起きる。それが全部週送りになっていた。

横送りと判定するのは次を全部満たしたときだけにした。

- 指が1本（ピンチ・二本指スクロールを除く）
- 横に60px以上（誤差では届かない距離）
- **横が縦の1.5倍以上**（縦スクロールのついでの横流れを弾く。これが本体）
- 600ms以内（ゆっくり指を這わせる動きはスワイプではない）

E2Eでは合成TouchEventを2種類投げる。横80px・縦440pxの縦スクロールで
**週が変わらないこと**と、横200px・縦20pxの素直なスワイプで
**1週間ぶん送れること**の両方を見る。片方だけだと、誤判定を潰したつもりで
スワイプ自体を殺しても気づけない。

#### 3. 表示期間が4週間で開く／月表示の「→」が効かない

**4週間で開く**のは移行漏れ。既定を2週間→1週間に変えたとき、端末に保存済みの
値（`forge.view.calendar.weeks`）はそのまま残るので、コードの既定を変えても
端末は4週間で開き続ける。保存キーを `calendar.weeks.v2` に変えて一度だけ
既定に戻した。変えたあとの選択はこれまで通り覚える。

**月送り**は日数計算が原因。`addDays(monthStart(a), dir * 30)` は
8月1日 + 30日 = 8月31日 で、その monthStart がまた8月1日になる。
31日ある月（8月・10月・12月など）では「→」を押しても永久に進まなかった。
`addMonths` を足して、日数ではなく月そのものを動かす。
戻る方向は -30日で前月に入れてしまうため、症状が出ておらず気づきにくかった。

## 確定範囲 — メニュー生成をどこまで「決まったこと」として出すか（forge-v59 / 2026-08-11）

### 何が問題だったか

生成はレース日まで全週を一度に作る（`periodization.ts` の `while (w <= raceDate)`）。
そのとき設定ペースは**生成した瞬間のCFE**で焼き込まれる。ところが更新は
M-2 が14日先までしか届かず、再生成は手動なので、2か月先のセッションは
古いCFEで作った数字を持ったまま残り、それが明日の練習と同じ顔で表示されていた。

これは「読めなかったものを推測で埋めない」に反する。
2か月先の設定ペースは推測なのに、決定事項として出ていた。

さらに悪いことに、更新側にズレがあった。`repaceFutureSessions` は
`targetPaces` だけを書き換えて処方の文面を放置していた。文面には
`400m × 3 @400m 52.5〜53.6秒 r6分` のように設定ペースが埋まっているので、
**CFEが2.5秒動いたとき34枠すべてで「画面には52.5秒、実際の設定は51.6秒」**という
状態になっていた。1秒近い差を、本人が気づけない形で持っていたことになる。

### なぜ「1〜2週ずつ生成する」ではないのか

未来の予定が存在することを前提に動いている機能が、実際に効いている。

- ルールエンジンの事前警告（「次の高乳酸は間隔を空け」は未来の予定を見ている）
- Q-2 の入れ替え候補（未来のジョグ枠から選ぶ）
- テーパー計画は21日先、統合タイムラインは28日先を読む

生成を短く切るとこれらが材料切れで黙る。**レース直前ほど効く機能**なので、
切るのは生成範囲ではなく**確定範囲**にした。骨組みは長いまま残す。

なおピリオダイゼーション自体は都度生成でも壊れない。フェーズはレース日からの
逆算（`phaseForDate`）なので、いつ生成しても同じ位置に入る。判断の分かれ目は
そこではなく、上に挙げた「未来の予定を材料にしている機能」のほうだった。

### 決めたこと（`src/lib/core/horizon.ts`）

**確定範囲は14日**。M-2（`adaptiveProposals` の既定 days=14）と揃えた。
M-2 は直近の実行状況とその日の状態から設定ペースを調整する仕組みで、つまり
14日先までは設定に手を入れられる。**手を入れられない範囲の数字に責任を持つとは
言えない**ので、責任範囲＝調整できる範囲にする。
ここを動かすときは `adaptiveProposals` の既定日数も一緒に見ること。
片方だけ伸ばすと「確定と言っているのに誰も更新しない範囲」が生まれる。

**判定は保存しない。** 日付から毎回計算する。保存フラグにすると、日が進んで
確定範囲に入っても古い印が残り、明日の練習が素案のまま表示される事故が起きる。

**素案では本数もレストも出さない。** 設定ペースだけ隠せば済む話ではなかった。
本数とレストは `buildSessionSpec` が直近の実行状況・疲労
（trend / loadHigh / recentFatigueSignal）から決めているので、2か月先の
「400m × 3」も同じく推測でしかない。半分だけ本当の文章を出すと、
どこまでが決まっているのか判別できなくなる。
残すのは種目・狙い・volume（週テンプレートとフェーズから決まり、CFEでは動かない）。

**本人が書いたものは素案にしない**（`userEdited` / `origin`）。
確定範囲が守っているのは「生成が推測で置いた数字を決定事項として見せない」ことで、
本人が決めた内容はそもそも推測ではない。ここを分けずに実装したところ、
2か月先の予定に自分でメニューを書くと定型文に差し替わって消えたように見え、
既存のE2E（カレンダーで保存したメニュー本文が一覧へ反映される）が落ちた。

### 更新は生成と同じ経路を通す（`refreshNearHorizon`）

文面側の数字を置換して辻褄を合わせることも考えたが、採らなかった。
処方の文面は `progression.ts` / `periodization.ts` が組み立てており、
表示のためにもう1か所で解釈すると「同じ文字列が場所によって違う意味になる」。
**生成と同じ経路で作り直す**ことにして、文面と設定が構造的にズレないようにした。
`repaceFutureSessions` は廃止し、呼び出し元3か所を差し替えている。

作り直すのは確定範囲だけ。その先は素案として設定ペースを出さないので、
古い数字を持っていても表示されず、確定範囲に入る前にここで作り直される。
触らないものは `regeneratePlan` と同じ（実施済み・中止済み・本人編集・固定枠・
自動生成でないもの）。**片方を直したらもう片方も確認すること。**

負荷条件（`loadSensitivity`）を生成と作り直しで共有しているのは、条件が違うと
同じ日付なのに作り直すたびに内容が変わってしまうため。

### 検証

- `tests/horizon.test.ts` … 境界・素案の中身・本人編集の除外・日が進めば確定に変わること
- `tests/nearHorizon.test.ts` … 文面と設定ペースの一致、触ってはいけないものを触らないこと
- E2E … 素案の詳細画面に設定ペースが出ないこと、素案から開始できないこと、
  確定範囲の中で文面と設定が食い違わないこと

**壊した状態で落ちることまで確認済み。**
処方の更新を止めると `tests/nearHorizon.test.ts` が「222 と 217.0 が一致しない」で落ちる。
詳細画面の素案判定を外すとE2Eが3件（ペースが出ている／いつ決まるか出ていない／
そのまま開始できる）で落ちる。

なお最初に書いたE2Eはカレンダー本文を読む形で、**壊しても通ってしまった**。
カレンダーの既定が1週間表示なので、14日より先の枠はそもそも描画されていなかった。
日付に依存しないセッション詳細を読む形に変えている。

### アプリアイコンの差し替え（forge-v60 / 2026-08-12）

黒地＋スラッシュ入りワードマーク＋`800M PERFORMANCE` のロックアップに変更。
以前のトラック写真＋FORGEから、文字だけの構成にした。

原本の元画像は縦長（1122x1402）で、ロックアップは中央の一部にしか無い。
そのまま正方形に切ると小さすぎるので、ロックアップの位置を実測して
（bbox x316..822 / y614..864）中央に置き直したうえで `pwa/icon-master.png` を作った。

**ロックアップ幅を原本の70%にしている理由。** `build-icons.mjs` が
さらに110%（上下左右5%ずつのブリード）で切るため、最終的なアイコン上では約77%になる。
maskable（inset 9%）でも約63%に収まり、円形に切り抜かれても文字が欠けない。

32pxでは `800M PERFORMANCE` は読めず、緑の線として見える。
ワードマークだけの別原本を用意すれば32pxは読みやすくなるが、
原本を2つに分けると「どちらが正か」が生まれるので、1つのままにした。
ホーム画面で実際に使われる180/192/512では問題なく読める。

アイコンURLには `?v=${VERSION}` が付いているので、**VERSIONを上げないと端末のアイコンは変わらない。**

### 画面側のワードマークもロゴに揃える（forge-v61 / 2026-08-12）

アイコンだけ新ロゴにすると、アプリ内のワードマークと字形が食い違う。揃えた。

**ワードマークはCSSの mask として使う**（色は `currentColor`）。つまり必要なのは
シルエットで、白＝不透明・黒＝透明のアルファ画像。したがって
**緑のタグライン `800M PERFORMANCE` は画像に含められない**——マスクでは色が
失われ、ワードマークと同じ色で塗られてしまう。ロゴのうちワードマークだけを
切り出し（実測 bbox x316..822 / y614..797、緑を除いた白画素のみ）、
輝度をアルファに変換して作った。JPEGのノイズで黒地にも輝度5〜20が乗るので、
下40・上215で切ってから伸ばしている（そのままだとマスク全体が薄く霞む）。
ロゴの斜めの切れ込みは元画像で黒なので、そのまま「抜け」になる。

タグラインは元から**テキストとして**存在していた（スプラッシュの `.tag`、
ヘッダーの副題）ので、そちらに任せればロックアップとして成立する。

**縦横比が変わるので定数も直す。** 旧 1120x205（比5.46）から
新 1012x366（比2.77）へ。`forge-logo.tsx` の高さ計算と `pwa/index.html` の
`aspect-ratio` の両方にある。スプラッシュの幅を430px→360pxに下げたのは、
幅を据え置くと高さが約2倍になり画面の中で浮くため。

**ヘッダーは skewX をかけた太字テキストをやめ、`ForgeLogo`（マスク）にした。**
以前のコメントには「アイコンのワードマークが斜体なので画面側も同じ傾きにする」
とあったが、新しいロゴは斜めに切れ込みが入った専用の字形なので、
テキストを傾けても似せられない。`ForgeLogo` は実装だけあって未使用だった。

**検査を足した理由。** テキストだったころは崩れても文字は出ていた。
画像に変えたことで、マスクのURLが解決できないと**正しい大きさの空の箱**になる。
エラーも出ないので画面を見ないと気づけない。要素の大きさだけを見る検査では
これを通してしまうので、ワードマークの範囲を切り出して
**明るい画素の割合**を数えている（実測約33%、空欄なら0%）。

壊した状態で落ちることは確認済み。`styles.css` のマスクURLを存在しない名前に
差し替えると `FAIL: R-3: ヘッダーのワードマークが空欄（明るい画素 0.0%）` で落ちる。
なお `brand-wordmark.png` を配信物から消す壊し方だと、Service Worker の
install が先に失敗してオフライン検査が落ちる（原因が分かりにくいだけで、
配信物から漏れること自体は検出される）。

### ロード表示を線と3点にする（forge-v62 / 2026-08-12）

400mトラックを1.45秒かけて描き、ゴールにフレアを出す演出をやめた。
細い線と3つの点だけにして、点を順に光らせる。

**理由。** 毎日開く道具に、起動のたび2.5秒の見せ場を入れるのは重い。
FORGEの原則は「数値を主役にする」で、起動画面の主役は演出ではなく
**レースまでの日数と目標との差**（R-2）のほうだった。
線は「ここまでが読み込みの枠」、点は「まだ生きている」ことだけを示す。

あわせて次を直した。

- **最低表示時間を2800ms→2000ms。** 2800msだったのは描画1.45秒＋フレア1.68秒を
  途中で切らないため。演出を変えたので、その値を守る理由が消えた。
  一度1200msまで詰めたが、実機で「速すぎて何が出ていたか分からない」となったので
  2000msに戻している。ワードマークと情報が出そろうのが約0.85秒なので、
  そこから約1.15秒は動かない画面が残る（点の点滅は1周1.35秒なので1周半ぶん）。
  0にしないのは、読み込みが速いときに一瞬光って消えるチラつきになるため。
- **出だしの遅延を詰めた**（ワードマーク1.78s→0.12s、情報2.18s→0.42s）。
  どちらもトラックが描き終わるのを待つ値だった。
- **背景の緑のにじみを外して黒一色に。** あれはトラックの発光を受けるための
  ものだったので、演出と一緒に落とす。
- **Next.js側（`launch-splash.tsx`）も同じ形にした。** 片方だけ変えると、
  同じアプリなのに実行環境で起動画面が変わる。ついでにこちらだけ残っていた
  skewXの太字テキストのワードマークも、画像（マスク）に揃えた。

**検査。** R-2はトラックのレーン本数を数えていたので、線と点を見る形に変えた。
要素の有無だけでは「3つ並んでいるが全部潰れている」を通してしまうので、
線の幅と点の大きさまで見る。点を消すと
`FAIL: R-2: 読み込み表示の点が3つでない` で落ちることを確認済み。

## 時間が経ってから開くと白飛び→かくかく（forge-v64 / 2026-08-12）

実機で「ちょっと時間たってから開くと、画面が白くなってからカクつく」。
原因は3つ別々にあり、どれも**コールド起動のときだけ**出る。
メモリに残っている間の再開ではリロードが起きないので、普段は気づけない。

### 1. iOSの起動画像が無い（白飛びの本体）

`manifest.webmanifest` に `background_color: #0a0a0a` は入っているが、
**これを見るのはAndroid/Chromeで、iOSは見ない。**
iOSのホーム画面PWAは `apple-touch-startup-image` を使い、無いと白い画面を出す。

`scripts/build-splash-screens.mjs` で12機種ぶん生成した。
**実物のスプラッシュをそのまま撮っている**（bundle.jsを止め、reduced-motionで
フェードの途中を撮らないようにして開く）。静止画を別に組むと本物とわずかにズレて、
「起動画像 → 本体」の切り替わりで画がガタつくため。
iOSはサイズが完全一致したときだけ使うので、機種ごとに1枚要る。

### 2. アプリ本体がネットワーク優先（白飛びが長引く原因）

`sw.js` は `index.html` / `bundle.js` / `styles.css` をネットワーク優先で返していた。
手元のキャッシュに完動品があっても、まず通信を試して、失敗して初めてキャッシュに落ちる。
`bundle.js` は約1MBあるので、電波が悪いと起動のたびに白い時間が数秒伸びる。

**stale-while-revalidate に変えた。** キャッシュを即返し、取得した新版はキャッシュへ入れて
次回起動で反映する。

ネットワーク優先だったのには理由があって、**VERSIONを上げ忘れて再配信しても届く**
という保険だった（`e2e-update.mjs` が見張っている）。この保険は1回遅れで残る。
VERSIONを上げた場合は install が新しいキャッシュを作るので、これまで通り即座に切り替わる。

`e2e-update.mjs` の契約もそれに合わせた。1回目のリロードで**旧版が出ること**まで
見ているのは速さの回帰を防ぐため——ここで新版が出るなら、キャッシュを即返さず
通信を待っている証拠になる。

### 3. 点のアニメが色を動かしていた（かくかくの原因）

```css
@keyframes splash-dot { 0%,60%,100% { background: rgba(...) } 25% { background: var(--forge) } }
```

**色のアニメーションはコンポジタに乗らない。** 毎フレーム、メインスレッドでの
描き直しが要る。ところがこの画面が出ている最中のメインスレッドは、約1MBの
bundle.js の解析と最初の描画で埋まっている。結果、点だけが取り残されてカクついた。

点を2層にして、上に重ねた緑の層の `opacity` だけ動かす形にした。
opacity なら合成側で回るので、メインスレッドが詰まっていても滑らかに動く。
同じ画面の `.brand` / `.info`（opacity・transform）と `.wait`（opacity）が
滑らかだったのは、元からコンポジタで動いていたため。

### 検証

壊した状態で落ちることまで確認済み。

- 起動画像を1枚消す → `FAIL: R-3: 起動画像 ./splash-1170x2532.png が配信物に無い`
- 1回目のリロードで新版が出る（＝通信待ちに戻る）→ `FAIL: 1回目のリロードで新版が出た`
- 2回目でも旧版のまま（＝裏の更新が効かない）→ `FAIL: 2回目のリロードでも旧版のまま`

### ヘッダーの400mトラックのマークを外す（forge-v65 / 2026-08-12）

ヘッダー左上にあった緑の400mトラック（`ForgeMark`）を削除し、ワードマークだけにした。

**理由。** アプリアイコンを新しいロゴに差し替えたとき、新ロゴが**文字だけの構成**に
なり、対応するシンボルが存在しなくなった。無いものに形を寄せることはできない。
トラックを残すと「アイコンには無い記号がアプリの中だけにある」状態になる。
緑はタブと数値の側に集約する（黒白グレー90%・アクセント10%の配分に沿う）。

下タブの TODAY アイコンは同じトラックの形のまま残している。あちらは
ブランドの記号ではなく**タブの機能を示すアイコン**（カレンダー・同心円・棒グラフと
同じ役割）なので、ロゴの変更とは切り離す。

**E2Eの検査はここで消した。** 以前は `header svg path` を4本数えていたが、
マークを外したあともヘッダーには設定の歯車のSVGが残る。検査を残すと
**歯車を掴んで通ってしまい、マークが消えていても気づけない**。
代わりにワードマークの有無を見る（描画の中身は既存の画素チェックが見ている）。
ワードマークをただの太字テキストに戻すと
`FAIL: R-3: ヘッダーにワードマークが無い` で落ちることを確認済み。

### ロゴを差し替え、ワードマークをマスクから画像に変える（forge-v66 / 2026-08-12）

アプリアイコン・ロード画面・ヘッダーのワードマークを、新しい角落としのロゴに差し替えた。

**マスクをやめた。** これまでワードマークはCSSの `mask` で使い、色は `currentColor` に
従わせていた。新しいロゴは**緑の縦線がロゴの一部**なので、マスクにすると緑が
白に潰れる。実際の使用箇所（ヘッダー・スプラッシュ）は全部白で、色を変えている
場所は無かったので、色を持った透過PNGをそのまま出す形にした。
**色を変えたくなったら画像を分けること**（もう currentColor では変わらない）。

透過への戻し方は、元画像が黒地に描かれた（＝黒に合成済みの）JPEGなので

```
a   = max(R,G,B)
RGB = 元の色 / a
```

で非合成化する。輝度をアルファにすると緑が薄く出るので max を使う。
割り戻さないと、文字の縁が灰色に濁る。

**縦横比が毎回変わるので、寸法は全部見直す。**

| | 比 | スプラッシュ幅 | ヘッダー幅 |
| --- | --- | --- | --- |
| 1120x205 | 5.46 | 430px | — |
| 1012x366 | 2.77 | 360px | 46px |
| 1124x918 | **1.22** | **260px** | **34px** |

ほぼ正方形になったので、幅を据え置くと高さが約3倍になる。
`forge-logo.tsx`（高さ計算）・`pwa/index.html`（`aspect-ratio`）・
`globals.css`（`.launch-wordmark`）・`nav.tsx`（ヘッダー幅）の4か所にある。
**ロゴを差し替えたら4か所とも直す。**

**起動画像も作り直す。** `splash-*.png` は実物のスプラッシュを撮ったものなので、
ワードマークが変われば中身が古くなる。`npm run build:splash` → `npm run build:all` の順。

**検査。** ヘッダーの画素チェック（明るい画素の割合）はマスクでも画像でも同じように
効く。`styles.css` の画像URLを存在しない名前に差し替えると
`FAIL: R-3: ヘッダーのワードマークが空欄（明るい画素 0.0%）` で落ちることを確認済み。

## CFEを実測タイム基準に作り直す（forge-v67 / 2026-08-12）

本人から「CFE（推定800mタイム）が感覚とかなりズレる」。計測したら**構造的な偏り**だった。

### 何が起きていたか

旧式は `暗黙の800m = 現CFE + ΔRPE×0.4 + 未達幅` で、未達幅は

```js
shortfall = Math.max(0, (actualPerM - targetPerM) * 800);
```

**速く走った分は `Math.max(0, ...)` で捨てていた。** つまりタイムが速いことはCFEに
一切入らず、改善が入る経路はRPE（主観）だけだった。CFE 1:51.0 / 設定 400m×3 @55.5秒
で実測したところ:

| 実測 | RPE | 旧 | 新 |
| --- | --- | --- | --- |
| 52.0秒（設定より3.5秒/本 速い） | 6 | −0.17秒 | **−0.38秒** |
| 52.0秒（同じタイム） | 9 | **+0.08秒（悪化）** | **−0.32秒（改善のまま）** |
| 55.5秒（設定どおり） | 8 | 0 | 0 |
| 58〜59秒（未達） | 9 | +1.34秒 | +1.50秒 |

改善と悪化で効き方が8倍違い、**同じタイムでもRPEの入れ方で符号が反転していた**。
続けるほど遅い側へ寄る作りで、初期値も `PB + 1.5秒`（12週以内にレースが無い場合）。
感覚と合わなくなるのは当然だった。

### 直し方

`impliedFromInterval`（過去データからの現在地測定で既に使っている実装）を
通常の練習結果にも使う。実測平均 ÷ カテゴリのGRP比率 × 800 で800m相当を出す。
**換算をCFE側に書き直さない**——同じ実測から画面ごとに違う推定が出てはいけない。

- **速い側・遅い側が対称に効く**
- **RPEは補助に降格**（係数 0.4 → 0.15）。符号を反転させない。
  0にしないのは、レストを削った・向かい風だった等がRPEにしか出ないため
- **RPE6未満（全力でない）の実測は使わない**。能力の下限しか分からないため
- **設定ペースを一切見なくなった。** 副次効果として「目標タイムが設定に混ざって
  CFEに逆流する」問題（`goalTargetTimeSec` で打ち消していたもの）が構造的に消えた
- **中断は値を盛らず、その結果の信頼度を半分にする**。実測基準では完走した本の
  タイムがそのまま入るので、未達を足すと二重に罰することになる

**いちばん大事な性質**: 生成器の設定どおりに走ると implied = CFE ちょうど（delta 0）。
生成と推定が同じ比率表（`GRP_RATIOS`）を使っているため。ここがずれると
全セッションでCFEが漂流するので、テストで固定してある。

ガードレールは全部そのまま（±1.5秒／レースは±3.0秒／暑熱・疲労時は反映しない／
14日で鈍化／aerobic・neuralは使わない）。

**実測が無い記録ではCFEは動かない。** RPEだけ入れても動かないので、
既存テストで `actualLapsSec: []` のまま「RPEでCFEが悪化する」を見ていたものは
実測を入れる形に直した。

## 複合メニューの入力（forge-v67）

`1000×4＋200×3 r3min-5min-200jog` が読めなかった。正確には**読めずに黙って捨てていた**。

- `parseRepSpec` は先頭の "距離×本数" しか返さないので `＋200×3` が消えていた
- レストの正規表現も先頭の `3min` だけを拾い、`5min` と `200jog` は消えていた

どちらもエラーが出ないので、本人には「無かったこと」になる。CLAUDE.mdの
「黙って本数が減らない」に反していた。

`parseRepBlocks` を足して全部のかたまりを拾い、2つ以上あれば区間に展開して
**既存の複合セット処理にそのまま乗せる**（代表距離の決定・混在の警告・本数の対応づけ）。
新しい分岐を作らないのは、同じ本文が一括入力と編集シートで違う意味になるのを避けるため。
1かたまりのときは展開しない——`300m×6` まで複合セット扱いになってしまう。

レストは表記を全部 `restNote` に残し、構造化する値（負荷とCFEのレスト補正）は
先頭を代表に採る。1本間のレストが実施強度をいちばん左右するため。
**代表を採ったことは本文で伝える**（黙って1つに丸めない）。

## 機能検索（forge-v67）

「どこに何の機能があるか分からない」。実際、CFEがズレたときに直す手段
（過去データ → 現在地の測定）があるのに、その存在自体が埋もれていた。

`src/lib/core/featureSearch.ts` にカタログと検索を置く。**LLMは使わない**ので
同じ入力からは必ず同じ結果が出る。なぜ一致したか（`matchedOn`）も返す。

カタログは手で持つ。画面から自動収集しないのは、**本人の言葉で引けないと意味が無い**ため
——「タイムがずれてる」「バックアップ」「コーチに見せる」「ガーミン」で辿り着けること
が目的で、画面名を知っている人しか引けないなら問題は解けていない。`keywords` が
その言い換えを入れる場所。

入口は設定画面の先頭。下部タブは4つから増やさない規則があり、
設定はヘッダーの歯車から全画面1タップで開けるため。

画面の**中**にある機能（現在地の測定・4週間のバランス・週次レビュー）は
`where` に「行った先のどこか」まで書く。画面名だけ出しても辿り着けない。

**検証**: E2Eは「出ること」で終わらせず、押して実際に移動するところまで見る。
リンク先が間違っていても一覧に出た時点では気づけないため。
該当が無いときに無理やり何かを出さないことも確認する。

### 実測基準CFEの取りこぼし2件（forge-v68 / 2026-08-12）

本人から「ちゃんと結果入力できてるのか確認したい」。実機の入力
（1000m×4＋200m×3、レストが本ごとに違う）を通しで再現したところ、
**保存は完全だったが、CFE側に2件の取りこぼしがあった。**

保存のほうは問題なし（`tests/compoundResult.test.ts` で固定）。
7本ぶんのタイム・1本ごとの距離・1本ごとのレスト（時間と距離が混ざっていても）が
そのまま残る。

#### 1. 距離が混ざった本を全部まとめて平均していた

実測基準に変えたとき、`repDistanceM` に先頭の距離を、`repTimesSec` に全部の
タイクを渡していた。複合セットでは **190秒（1000m）と30秒（200m）を平均する**
ことになり、換算が完全に壊れる。

いちばん本数の多い距離だけを使うようにした。一括入力（`parseRow`）が
「代表として1000mぶんだけを能力推定に使います」と言っているのと同じ規則。
**片方を直したらもう片方も確認すること。**
代表を採ったことは `guardrailNotes` で伝える（黙って捨てない）。

#### 2. CV・閾値がCFEを一切動かさないのに、理由が嘘だった

`GRP_RATIOS` は modeling / high_lactate / race_economy / neural しか持たない。
**CVと閾値には800m相当への換算比率が無い。** それなのに `SESSION_CFE_PARAMS` には
両方が載っていたため、実測基準に変えた時点で無言で「更新されない」状態になり、
しかも出る理由が「距離・タイムが足りない」で嘘だった。

比率を勝手に足すことはしない——1000mのCV走から800m能力を出す根拠が無いため
（読めなかったものを推測で埋めない）。代わりに理由を正確にした。

```
ガードレール: cv は800m相当への換算比率を持たないためCFEに使わない
（負荷とLT推定には算入します）
```

旧実装ではCVもRPEと未達幅でCFEを動かしていたが、あれは実測ではなく主観由来の
揺れだったので、動かさないほうが正しい。タイム自体は負荷（ACWR）・LT推定・記録・
同一処方の比較には従来どおり使われる。

## 保存内容の確認（forge-v69 / 2026-08-12）

本人から「ちゃんと入力できてるか確認できる機能ほしい」。
保存直後の「補正結果」はCFEの前後しか出さないので、
**本ごとのタイムとレストが正しく残ったかを確かめる手段が本人側に無かった。**

記録画面に「保存内容の確認」を足す。直近5件について、本ごとの
距離・タイム・レストを並べ直し、あわせて**その記録が何に使われたか**を出す。

### 判定はここで書き直さない

「CFEに使われたか」を画面用に判定し直すと、実際の処理と説明が食い違ったときに
**説明のほうが正しく見えてしまう**。`updateCfeFromResult` は純粋関数で保存もしないので、
そのまま呼んで `applied` と `guardrailNotes` を受け取るだけにしてある。

CFEに使われなかった場合も、負荷（ACWR）と同一処方の比較には算入されることを併記する。
これを出さないと「CFEに使われない＝入力が無駄だった」と読めてしまう。

### 途中で見つけた自分の誤り

最初に書いた `tests/compoundResult.test.ts` は、本ごとのレストを
`restSec` / `restDistanceM` という**実在しないフィールド名**で書き込み、
同じ名前で読み返していた。SQLiteはJSONをそのまま往復させるので**テストは通る**が、
アプリが実際に使う `restAfterSec` / `restAfterDistanceM` は見ていなかった。
型を通した時点（`resultAudit` の実装）で発覚。

正しいフィールド名に直して再確認したところ、**保存自体は元から正しかった**。
ただしテストとしては何も見ていなかったので、直して固定した。
`interval.restSec`（セッション共通）と `restAfterSec`（その本のあと）は
名前が似ていて取り違えやすい。

## 2部練習に対応する（forge-v70 / 2026-08-13）

土台は元からあった（`timeOfDay`、同日2本の高負荷を弾くRULE-03、真夏の2部を警告する
RULE-11、負荷の日次合算、カレンダーと記録の同日複数対応）。
足りなかったのは**設定・生成・ホームが1日1本前提**だったこと。

### 午前は指定した曜日だけ

`WeekTemplate.amSlots` を足した。既存の `slots` は**午後（主練習）**の意味のまま
——`pmSlots` に改名すると保存済みのデータが読めなくなる。

午後の枠と違い、午前には「auto（自動生成に任せる）」を置かなかった。
**黙って練習を増やさないため。** 午前を自動で埋めると、本人が頼んでいない量が
勝手に乗る。800mで効くのは量ではないので、増やすなら本人が明示する。
未設定・"auto" はどちらも「午前なし」として扱う。

午後が休養の日には午前を置かない。休養日が半日つぶれると回復日が消える。
主練習が既に午前にある日にも置かない（同じ時間帯に2本入ってidが衝突する）。
午前枠を置いた日は、坂ダッシュ＋ジョグの分割を行わない——分割先も午前なので同じ衝突が起きる。

### 危ない組み合わせは生成前に言う

午前・午後とも高負荷なら、テンプレートの検証でERROR。
RULE-03 は生成後に同じことを言うが、そのときには「なぜそう置いたのか」が
もう分からない。このファイルの既定方針（固定曜日は枠であって免罪符ではない）に従う。

### ホームにもう1本を添える

`todaySession` は優先度のいちばん高い1本しか返さない。それだけを出していたので、
午前ジョグ＋午後ポイントの日は片方がホームから消えていた。
判断ゲート（カードの主役）は1本のままにして、残りを名前と時間帯だけ添える。
両方を同じ大きさで出すと「今日どっちをやるか」の判断が2つに割れる。

### 検査を2回書き直した

最初は「2本ある日の数」を数えた。**午前の生成を止めても通ってしまった**
——坂ダッシュ＋ジョグの分割が元から2本を作るため。
次に「午前枠を入れる前後の差」で見たが、これも 0→1 で通った（分割がたまたま火曜に当たる）。
カテゴリで区別する案も効かない。分割で入るジョグも `aerobic` だから。

最終的に **「午後がある火曜には必ず午前がある」** で見ている。
午前の生成を止めると `1/6 日しか2本になっていない` で落ちることを確認済み。

## CFEが感覚とズレる本当の原因は鈍化だった（forge-v71 / 2026-08-13）

実測基準に作り直したあとも「CFEが1:54.2で感覚とかなりズレる」。
PB 1:49.51 に対して4.7秒遅い。式ではなく**鈍化の累積**だった。

```
初期値 PB+1.5秒 = 111.01（1:51.01）
  ＋ 63日ぶんの鈍化（+0.4秒/週）
  = 114.21（1:54.21）  ← 実機の値と一致
```

### なぜ鈍化だけが進んだのか

鈍化の判定は `cfe.lastUpdated`（＝**CFEが動いた日**）を見ていた。
ところがCVと閾値は800m相当への換算比率を持たないためCFEを更新しない。つまり

> CV走を2か月続ける → CFEは一度も更新されない → 「結果が無い」扱いで鈍化だけ進む

**練習して記録しているのに罰されていた。**
鈍化の目的は「練習していない期間の楽観的なCFEを放置しない」ことであって、
練習しているのに下げることではない。

`applyStaleness` に `lastResultDate`（**カテゴリを問わない**最後の記録日）を渡し、
記録があるなら鈍らせないようにした。補強も記録として数える。

この誤りは、実測基準に変えたときに CV/閾値がCFE更新から外れたことで**顕在化した**
（旧実装ではRPEでCVもCFEを動かしていたので、鈍化が相殺されて見えなかった）。
片方を直すともう片方が露出する例なので、`SESSION_CFE_PARAMS` と `GRP_RATIOS` の
対応がずれていないかは、今後も両方まとめて確認すること。

## 2部の午前メニューを状況に合わせる（forge-v71）

午前は種別によらず固定だった（ジョグは常に40分、流しは常に6本）。
ペース帯だけは本人のCFE/LTから出ていたが、**量はフェーズも午後の内容も疲労も
見ていなかった**。午後が高乳酸の日も回復ジョグの日も同じ40分では、
2部にする意味が薄いうえ危ない。

午前は**午後を殺さないための補助**と定義して、量を次で決める。

| 状況 | 午前ジョグ |
| --- | --- |
| 通常 | 40分 |
| 午後が高負荷 | 30分（脚を残す） |
| Modeling・Taper期 | 25分（総量を落とす局面で午前を積まない） |
| 疲労の実測あり | さらに×0.7 |

**上げる方向には振らない。** 量で伸ばす種目ではないため。

実装上の落とし穴: `templateForSlot` は `"aerobic"` に対して `jog(40)` を
**固定で返す**ので、そこを経由すると決めた長さが捨てられる。
ジョグだけは通さずに組み立てている。

## 午前枠の助言（forge-v72 / 2026-08-13）

2部の午前の種目を、制限因子から**自動で決めることはしない**。
噛み合っていないときだけ一言出して、決めるのは本人に残す。

### 自動化を採らなかった理由

1. **制限因子は既に午後（主練習）で効いている。** M-7 の `categoryWeights` が
   枠を振り替えている。午前にも同じ判定を当てると、1つの信号を二重に数える。
2. **午前に置ける幅が狭い。** 同日2本の高負荷はERRORで弾くので、午前は低負荷に
   限定される。自動化してもジョグ・流し・補強のどれかに収束し、決め打ちのコードを
   増やす割に出てくる答えが変わらない。
3. FORGEの規則（自動で変えたことは理由とセットで出し、本人が却下できるようにする）と、
   静かに種目を決める機能は相性が悪い。

判定は `assessLimiter` / `categoryWeights` の結果をそのまま使い、
`amSlotAdvice.ts` で別の基準を作らない——同じ制限因子から画面ごとに違う助言が
出てはいけない。

### 出さない条件のほうが重要

**毎回何か出ていると、本当に噛み合っていないときに気づけなくなる。** 黙る条件:

- 2部にしていない
- PBが足りず制限因子を判定できない（推測で言わない）
- 既に流しが入っている
- 「維持が制限」のとき——増やしたいのは経済走・モデリングだが、どちらも高負荷なので
  午前には置けない。置けないものを勧めない

出すのは「スピードが制限なのに午前が全部ジョグ」のときだけ。断定せず可能性として言い、
制限因子の判定根拠を必ず添える。

### 検証で2回引っかかった

- **同じハッシュへの `goto` では画面が作り直されない。** ハッシュルーティングなので
  遷移が起きず、最初に読み込んだ助言が残る。`reload()` に変えた。
- **保存がIndexedDBへ落ちる前にリロードすると書き込みごと消える。** PBを変えて
  すぐリロードしていたため、助言が出ない側に倒れていた。待ってから読み直し、
  さらに**PBが実際に保存されたか**を先に確認してから助言を判定するようにした
  （書き込みが消えたのか助言が壊れたのかを区別できないと、原因を追えない）。

E2Eは黙る側と出す側の**両方**を通す。出さないことだけ確認して終わると、
機能が丸ごと壊れていても通ってしまう。
画面側で助言を捨てると `FAIL: 午前枠の助言がAPIには出ているのに画面に出ていない` で落ちる。

## 「現在地の測定」が動かない理由を言えるようにする（forge-v73 / 2026-08-13）

本人の端末で過去データが23件入っているのに、CFEが 1:54.2 のまま直せなかった。
実データの形（ジョグ・持続走22件＋CV 1000m×4 が1件）で通したところ、
**推定の材料が0件**だった。

- ジョグ・持続走 → 除外（有酸素系は800m能力の推定に使わない）
- CV → 除外。ただし理由が **「カテゴリ・距離・タイムのいずれかが不足」** と出ていた

後者が問題。入力は揃っている。落ちたのはCVに800m相当への換算比率が無いからで、
あの文面だと**入れ方が悪いように読める**。何を足せば測れるようになるのかが
分からないので、同じ種類の入力を増やし続けることになる。

理由を分けた。

```
cv は800m相当への換算比率が無いため能力推定に使えません（負荷とLT推定には算入）
```

材料が0件のときの案内にも、使える種類と使えない種類を名指しで書いた。
「実測がありません」だけだと、ジョグを何十件足しても状況が変わらないことが伝わらない。

これは `updateCfeFromResult` で直したのと**同じ型の誤り**（落ちた理由と表示が食い違う）。
`GRP_RATIOS` を持たないカテゴリの扱いは、CFE更新・現在地の測定の両方に出る。
片方を直したらもう片方も確認すること。

### CFEの測り直しをホームからも開けるようにした

「現在地の測定」は過去データ画面の中にしか無かった。
`assessCurrentFitness` の入力が `PastEntry` なので、入力する場所の隣に置いた
**実装都合の配置**で、CFEを見ている場所から直せないのはおかしい。
ホームのCFEカードに「実測から測り直す」を足した（信頼度が足りているときも出す
——足りていても感覚とズレることはある）。

## 現在地の測定が過去データしか見ていなかった（forge-v74 / 2026-08-13）

本人から「CFEに反映されているのが7/26までなのはなぜ？」。
過去データ画面の最新が7/26で、それ以降の練習は**記録タブ**に入れていた。

`assessFitness` は `repo.listPastEntries()` だけを読んでいた。
記録タブで入れた結果（`SessionResult`）は入っていない。つまり

> 同じ高乳酸のセッションでも、**過去データ画面から入れたか記録タブから入れたかで
> 現在地の測定に入るかどうかが変わっていた**

同じ入力から同じ結果が出るという原則に反する。実機では直近3週間ぶんが
測定の材料から丸ごと落ちていた。

記録タブの結果も同じ材料として扱うようにした（`resultsAsPastEntries`）。

- **過去データ由来の結果は除く。** `addPastEntry` は PastEntry と SessionResult の
  両方を作るので、入れると同じ練習を二重に数える。`past-s-*` の接頭辞で判別する
- **距離が混ざった本は、いちばん本数の多い距離だけを使う。**
  `updateCfeFromResult` と同じ規則（190秒と30秒を平均すると換算が壊れる）
- 暑熱の除外が効くように環境も引き継ぐ。`SessionResult` 側は `weatherTempC`
  で、`PastEntry` の `tempC` と名前が違うので取り違えに注意

### 同じ型の誤りが続いている

CFE更新・現在地の測定・除外理由の表示で、**同じ判断が別々の場所に書かれていて
片方だけ直っている**という誤りが4回続けて出た。
`GRP_RATIOS` を持たないカテゴリの扱いと、距離が混ざったときの代表距離の決め方は、
いま次の場所にある。**片方を直したら必ず全部を確認すること。**

- `updateCfeFromResult`（cfe.ts）
- `impliedFromInterval` / `assessCurrentFitness`（backfill.ts）
- `resultsAsPastEntries`（service.ts）
- `parseRow` の複合セット処理（bulkImport.ts）

## 処方の土台をPBより速くしない（forge-v75 / 2026-08-13）

現在地の測定をやったら 1:54.2 → **1:48.3**。800mPB（1:49.51）より1.2秒速い。
本人が「少し速すぎる気もする」。調べたら**ガードが無かった**。

### 何が起きていたか

材料が1件のとき、推定値はその1件の換算結果**そのもの**になる。

```js
estimated = Σ(implied × weight) / Σweight   // 1件なら = implied
```

`weight`（信頼度×直近性）は**信頼度の表示にしか効かず、値には効かない**。
さらに `applyAssessedCfe` はその値をそのまま保存する。上限も収縮も無い。

そして設定ペースはCFEから計算されるので、**信頼度が低くても処方はその速さで組まれる**。
信頼度は表示されるだけで、何も守っていなかった。

### 直し方（CFEは触らない）

FORGEには元から「CFEは能力の推定、設定ペースは今日出せる値」という分離がある。
今回の問題はその分離が基準タイムのところで効いておらず、推定がそのまま処方へ流れていたこと。
**守るのは処方のほう**にした。

`guardedBaseTime` に800mPBを渡し、土台がPBより速くならないように頭打ちにする。

- CFEは「練習からは1:48.3と出ている」と正直に持つ（情報を殺さない）
- 実際に出す設定はPBペースを超えない
- **PBは定義上「これまでで一番速く走れた記録」**。それを超えるペースはまだ一度も
  出したことがない速さなので、そこを土台に処方を組むと実行できない設定が出続ける
- レースかTTでPBを更新すれば上限も上がる。自己修正する
- 速い側へ寄せるのは目標タイムの寄与が担当する（そちらには非現実的な目標を弾く判定が
  別にあるので、二重に効かせない）

頭打ちにしたことは必ず理由で言う（黙って遅くしない）。
文面は生成の `selectionReasons` に乗るので、セッション詳細から追える。

```
推定（1:48.3）が800mPB（1:49.5）より速いため、処方の土台はPBで頭打ちにしています。
PBを超える設定は、レースかTTでその速さを出してから使います
```

**検証**: CFE 1:48.3 と PB 1:49.51 で生成した計画が、CFE=PB で生成した計画と
同じ設定（200m 26.01秒）になることを確認。頭打ちを外すとテスト2件が落ちる。

### 採らなかった案

- **件数に応じて現CFEへ収縮**: 寄せる先（1:54.2）が鈍化で汚れていたので逆効果
- **1件では適用させない**: 正しいが「直せない期間」が生まれる
- **幅で出して選ばせる**: 原則には合うが、毎回の判断コストが釣り合わない

## 曜日設定を分かりやすくする（forge-v76 / 2026-08-13）

本人から3つ。「午前が分かりにくい」「その他がほしい」「ジョグ＋坂ダッシュ／
ジョグ＋WSを入れたい」。調べたら**2つは既にあるのに届いていなかった**。

### 午前／午後を対で見せる

主枠にラベルが無く、下の行だけ「午前」と書いてあった。
上が何なのか分からないのは当然で、両方に時間帯を書いた。色帯の幅も揃えて
2つが対だと分かるようにしている。

### 坂ダッシュは実装があったのに選べなかった

`hillSprints`（坂ダッシュ8本＋ジョグ30分）は**自動生成の週テンプレートにしか
出てこず、曜日設定からは選べなかった**。`neural` を選ぶと必ず流しになる。

`WeekdaySlot` に `"hill"` を足した。これはカテゴリではなく**内容の指定**
——坂ダッシュと流しは同じ neural だが別物なので、選び分けられる必要がある。
`isPointSlot("hill")` は false（低負荷なのでポイント練習に数えない）。
`isSpecificCategory` に渡らないよう、カテゴリを見る箇所では除外する。

### ラベルが中身を表していなかった

`neural: "神経系（ジョグ込み）"` では何をやるのか伝わらない。
実物は「150m流し×6 ＋ ジョグ30分」なので、そのまま名前にした。

| 旧 | 新 |
| --- | --- |
| 神経系（ジョグ込み） | ジョグ＋流し（WS） |
| （選べなかった） | ジョグ＋坂ダッシュ |

### 「その他」は自作メニューだった

一覧に無い内容は自作メニューに登録すれば、同じカテゴリの枠に当たった日で
自動的に使われる（`pickCustomMenu`）。仕組みは元からあったが、
**曜日設定の画面からその存在が見えなかった**ので「その他が無い」と受け取られていた。
一覧の下に案内を足した。選択肢に自由入力を増やすより、既にある仕組みへ繋ぐほうが
解釈の置き場所が増えない。

### 320px幅で押し出された

時間帯のラベルをセレクトの**横**に置いたら、iPhone SE幅でセレクトが押し出されて
18px はみ出した（E2Eが検出）。ラベルを行の**上**に移して解消。
横に並べる要素を増やすときは、いちばん狭い端末で確かめること。

**検証**: `"hill"` の割り当て先を流しに戻すとテスト2件が落ちる。

---

## アプリの中で相談できるようにする（forge-v77 / 2026-08-13）

「なんでCFEがこの値なの」「なんで今日これなの」は、これまで**開発側に聞くしかなかった**。
アプリはその答えを全部持っている（CFEの更新履歴・生成器が残した選定理由・ルールエンジンの判定）が、
画面のあちこちに散っていて、本人が自分で組み立てて読むのは現実的でなかった。

設定 → 分からないとき → 相談（AI）に置いた。

### LLMに数値を触らせない

CLAUDE.md の「LLMを使わない。同じ入力からは必ず同じ結果が出ること」は**変えていない**。
変えると、あとで数値を疑ったとき、どこまでがルールでどこからがLLMか追えなくなる。

```
決定的なコア（これまでのまま）
  CFE・設定ペース・ルール判定・メニュー解釈
       ↓ 読むだけ
コンテキスト生成（`src/lib/core/assistantContext.ts` 純関数・テスト済み）
       ↓ これだけを送る
LLM ── 応答は文章のみ。数値を書き戻す経路を実装していない
```

`buildAssistantContext` は純関数で、時刻もランダムも読まない。
同じ入力からは必ず同じ文字列が出るので、あとで答えを疑ったときに何を渡したか再現できる。

### 画面に出す文字列と送る文字列を同じ実体にした

データが端末の外へ出る。「何が出ていくか」を本人が確認できないまま送ってはいけない。
ただし「送る前に見せます」と書くだけでは、実装がズレたときに誰も気づけない。

そこで `AssistantContext.text` を **`sections` から組み立てる形にした**。
画面は `sections` を出し、送信は `text` を送るので、片方だけ変えることが構造的にできない。
`tests/assistantContext.test.ts` がこの一致を固定しており、
`text` を別に作るように壊すと19件中8件が落ちる（確認済み）。

送るのは訓練の数値だけ。氏名は `AssistantContextInput` に項目そのものが無い。

### 同意は鍵の設定とは別に取る

APIキーを入れることは「使えるようにする」操作でしかなく、
**何が出ていくかを把握したこと**とは別。チェックボックスを分けた。
同意が無い間は1文字も送らない（E2Eが見張っている）。

### SDKを入れずRESTを直接叩く

`app/components/supabase.ts` と同じ判断。理由も同じで、
PWAのバンドルに数百KBのSDKを入れると起動時間に直接効く（`ci:perf-budget` が見張っている）。
使うのは `POST /v1/messages` 1本だけで、SDKの大半が要らない。

ブラウザから直接叩くので `anthropic-dangerous-direct-browser-access: true` が要る。
GitHub Pages配信でサーバを持たないため、他に経路が無い。
このヘッダが落ちると本番で全滅するので、E2Eで実際の送信を横取りして確認している。

### オフラインでは機能ごと出さない

押しても何も起きないボタンを残すと、壊れているのか通信が無いのかを本人が区別できない。
相談だけを畳み、他の画面はこれまでどおりオフラインで動く。

### 失敗を種類で分ける

「エラーが出ました」だけでは本人が直せない。
401＝鍵、429＝待てば直る、500＝向こうの問題、時間切れ、接続不能、断られた、を別々に出す。
`stop_reason: "refusal"` は**中身を読む前に**判定する。断られた応答は途中まで文が入っていることがあり、
そのまま出すと「短い答えが返ってきた」ようにしか見えない。

`fallbacks`（断られたときに別モデルへ回す機能）は入れていない。
走練習の相談で安全分類に触れることは実質なく、
使えない鍵・使えないベータで毎回400になるほうが、この1人用アプリでは損が大きいと判断した。

### 次にやること

写真からの入力の転記（写真 → テキスト → 既存の `parseRow` → 本人が確認して確定）。
解釈はこれまでどおり `bulkImport.ts` の `parseRow` が唯一の実装で、LLMは文字起こしまでしかやらない。

---

## 写真から練習日誌を読み取る（forge-v78 / 2026-08-14）

過去データ → まとめて入力に、写真から文字を起こす経路を足した。

```
写真 → （LLM）文字起こし → 本人が目で直す → parseRow が解釈 → 本人が確定
```

### LLMにやらせるのは文字起こしだけ

意味を読み取らせない。`bulkImport.ts` の `parseRow` が唯一の解釈という前提を崩さない。

これは妥協ではなく、こうしないと成立しない。
「300(42)×3」が高乳酸なのかCVなのかは**今のCFEに対する比率**で決まるので、
写真を見ただけのLLMには本来決められない。決められないものを決めさせると、
あとで数値を疑ったときに、どこで曲がったのか追えなくなる。

指示で禁じているのは、推測での穴埋め・表記の整形・並べ替え・種類の判定。
読めない箇所は `【読めず】` と書かせる。`?` にしないのは、日誌そのものに
本人が `?` を書くこと（「1:26?」）があり、区別できなくなるため。

`cleanTranscription` がやるのは**取り除くことだけ**（コードフェンス・前後の空行）。
中身を直したら、それはもう文字起こしではなく解釈になる。

### 起こした文字は入力欄に入るだけ

保存までに本人が2回見る（起こした文字／解釈結果）。文字起こしは必ず間違うので、
この2回を飛ばさない。E2Eで「転記しただけでは1件も保存されない」ことを見張っている。

既に書いてある内容は消さずに足す。手で打った途中の行を写真1枚で消されると打ち直しになる。

### 送る前に縮める

撮ったままは4000px近くあり、そのまま送ると通信も費用も無駄に増える。
小さくしすぎると手書きが潰れ、読めない箇所が増えて直す手間が本人に返ってくるだけ。
長辺2576pxで止める（Anthropicの高解像度の上限。超えて送っても向こうで縮められる）。
大きさの計算は純関数（`fitWithin`）にしてテストしてある。canvas を使う部分は判断を持たない。

鍵と同意は相談（AI）と共通。未設定なら写真の欄そのものを出さない。

---

## 神経系の日に「進め方」カードが消えていた（forge-v78）

写真転記の検証中、`npm run verify` が S-9 で落ちた。**写真転記とは無関係の既存不具合**で、
配信済みの forge-v77（`e9f2fe7`）でも同じように落ちることを別ワークツリーで確認した。
日付が 8/13 → 8/14 に変わって、今日の練習が神経系になった日に出た。

### 原因

`ADJUSTABLE_PACE_CATEGORIES`（app/page.tsx）に `neural` が入っている。
設定ペースの調整（M-2）は神経系にも効くので、これは正しい。

一方、進め方の2案（S-9）はテンプレート（`buildSessionSpec`）から作るので、
**神経系には案が無い**。両カードは「同じセッションの話をする」ために対象を揃えてあるので、
今日が神経系だと両方が神経系を対象にし、調整案は出るのに進め方カードだけが消える。

消えても画面は何も言わない。本人からは不具合なのか「今日は出ないのが正しい」のか区別できない。

### 直し方

対象に案が作れなかったときだけ、次のポイント練習へ**1回だけ**移す。
移した先にも無ければ出さない（それは正しい空）。移した場合は見出しを
「次のポイント練習の進め方」に変え、日付も出す。どの練習の話か分からないまま
2案を並べるほうが危ない。

`ADJUSTABLE_PACE_CATEGORIES` から `neural` を外す案は採らなかった。
神経系の設定ペース調整は実際に動いており、それを消すのは別の機能後退になる。

### 検証

- 食い違いそのものを `tests/sessionVariants.test.ts` で固定した。
  どちらかを直すなら、このテストが先に落ちる
- E2Eのカード特定を文言からボタン（「この進め方にする」）に変えた。
  見出しは対象によって変わるうえ、調整案カードも同じ文言を出すことがあり、
  文言ではどちらを見ているのか曖昧だった
- 移す処理を外すと E2E が再び落ちることを確認した（通ることだけの確認にしない）

---

## 表記辞書の候補出し（forge-v79 / 2026-08-14）

一括入力で読めなかった行について、辞書に入れる語の案を出せるようにした。
設定 → 過去データの一括入力 → 解釈する → 読めなかった行に「読み方の候補を出す」。

### なぜこれを選んだか

**使うほどAIを呼ばなくなる**方向に働く唯一の案だったから。

辞書は一度登録すれば以降 `bulkImport.ts` が決定的に読む。だから候補出しが動くのは
「まだ知らない書き方に初めて出会ったとき」だけで、同じ語には二度と呼ばれない。
チーム固有の呼び方は有限なので、使い続けると呼び出しは自然に減っていく。

これまでの `guessPhrase()` は「数値を落とした残りで一番長い語」を拾うだけだった。
組み込みの語彙をこちらで足し続けてもチーム固有の呼び方には追いつかない、
という元々の問題は変わっていない。追いつかせる側を本人＋AIに寄せた。

### 出すだけ。登録するのは本人

押しても入力欄が埋まるだけで、辞書は増えない。
これまでどおり「この書き方を覚えさせる」を押して初めて登録される。
E2Eで「埋めただけでは辞書が増えない」ことを見張っている。

行ごとの分類はさせていない。させると同じ文字列が日によって違う意味になりうる状態ができ、
`parseRow` が唯一の解釈という前提が壊れる。ここで作るのは辞書の1エントリだけで、
中身は画面で確認・編集でき、あとから一覧で消せる。

### 入口の検査を厳しくした（`phraseSuggestion.ts`）

辞書は一度入ると効き続ける。妙な語が入ると以降その語を含む行が全部そう読まれ、
しかも本人は「なぜこう読まれたのか」を辞書まで遡らないと分からない。
入口で止めるほうが安いので、少しでも怪しければ案にしない。

通さないもの:

| 条件 | 理由 |
| --- | --- |
| その行に書かれていない語 | 言い換え・要約を辞書にしない（「読めなかったものを推測で埋めない」と同じ考え） |
| 2文字未満 | どの行にも当たってしまう |
| 数字・記号だけ | 同上 |
| 決められた種類・カテゴリの外 | 画面の選択肢とずれる |
| 理由が無い | 却下する材料が本人に残らない |

種類と合わないカテゴリ（休養なのに高乳酸など）は黙って捨て、案そのものは活かす。

「行に書かれていない語を通さない」が一番効く検査で、これを外すとE2Eが落ちることを確認した。

### 送る内容

読めなかった**1行**と、すでに覚えている語の一覧だけ。相談（AI）より大幅に少ない。
鍵と同意は相談（AI）と共通で、未設定・オフラインならボタンそのものを出さない。

---

## 起動時に読む量を半分にする（forge-v80 / 2026-08-14）

`bundle.js` が予算の96%まで来ていた。相談・写真・辞書を足した結果だが、
これらは**起動時には要らない**。それでも毎回全部読まれていた。

TODAY以外の画面を遅延読み込みにした。

| | 前 | 後 |
| --- | ---: | ---: |
| bundle.js（起動のたびに読む） | 1,035,868 | **550,588** |
| 配信物のJS全体 | 1,463,911 | 1,476,122 |

全体は少し増えている（分割の境界ぶん）。**減らしたいのは起動時に読む量**で、
そこが半分になった。JSは読むだけでなく解析・実行に時間がかかるので、
iOSでアプリがメモリから落とされたあとの起動に直接効く。

分割の仕組み自体は既にあった（FIT解析が遅延読み込み済み）。実証済みの経路に乗せただけ。

### chunkをプリキャッシュするようにした（既存の穴）

`pwa/sw.js` の `ASSETS` は静的な一覧だが、chunkのファイル名にはハッシュが入るので
そこに書けない。**書いていなかったので、chunkはプリキャッシュされていなかった。**

これまでは遅延読み込みがFIT解析だけだったので実害が小さかったが、
画面を分けるならここを直さないと「インストール直後にオフラインへ入ると、
一度も開いていない画面が開けない」ことになる。

`scripts/build-static.mjs` が、配られた `chunk-*.js` の実体名を
`pwa/sw.js` の `const CHUNKS = [];` へ差し込む。差し込み先が無ければビルドを止める
（sw.js を書き換えたときに黙って素通りしないように）。

### installはchunkの失敗で落とさない

アプリ本体（`ASSETS`）は1つでも欠けたら動かないので `addAll` のまま。
chunkは `Promise.allSettled` にした。数十件あるので、全部必須にすると
**たまたま1つ取れなかっただけで新しい版が一切届かなくなる**。
取れなかったchunkは、その画面を開いたときにfetchハンドラが取りに行く。

### 検証

- 更新E2Eに「オフラインで遅延読み込みの画面が開く」を足した。
  起動だけ見ていても、この穴には気づけない
- 差し込みを止めると、その検査が落ちることを確認した
- 予算検査に `pwa-dist/*.js`（全体）を足した。
  bundle.js だけ見ていると、分けたぶんがchunkへ逃げて全体が太るのに気づけない
- `repositoryStructure.test.ts` が静的importの文字列に固定されていたので、
  書き方ではなく「共通画面を使い回しているか」を見る形に直した

---

## 転記をカレンダー側にも置く（forge-v81 / 2026-08-14）

写真からの転記は過去データ（`/past`）にしか入口が無かった。
でも `session-edit-sheet.tsx` の本文欄も、同じ `interpretPrescription` → `parseRow` を通っている。

コーチがホワイトボードに書いたメニューを撮って、そのまま予定に入れられるようにした。
新しい能力ではなく、検証済みのものを1画面増やしただけ。過去の日誌より使う頻度は高いはず。

### 実装は1つにまとめた

`app/components/photo-transcribe.tsx` に出して、両方から使う。
撮る対象が2種類（済んだこと／これからやること）あるだけで、中身は同じ。
2つ置くと、片方だけ直して挙動がズレる。

合わせて、文字が写っていないときの合図を `練習日誌が写っていません` から
`読み取れる文字がありません` に変えた。撮る対象が日誌だけではなくなったため。

### 保存はこれまでどおり本人が押したときだけ

転記は本文欄に入るだけ。そこから先は既存の経路（本文から欄が組み上がる → 保存する）。
E2Eで「転記しただけでは予定が変わらない」ことを見張っている。

その検査は最初、**生成器が作った `300m×N` の予定に引っかかって誤検知した**。
「その本文が存在しないこと」では確かめられないので、転記の前後で件数が変わらないこと、
という形に直した。保存ボタンを押すと件数が増えて落ちることも確認済み
（増えなければ検査そのものが無意味なので）。

---

## 処方の文面を自分で読み違えていた（forge-v82 / 2026-08-14）

結果入力の画面で、次の3つが同時に起きていた（本人の指摘）。

```
処方:  300m × 3 @300m 38.7〜39.5秒 r207秒（jog）
欄:    設定(秒) 300 / レスト(秒) 300
```

1. レストが207秒という、なぜその数字か説明できない値
2. 処方は207秒なのに、欄は300秒
3. 300mの設定が300秒（実際は38.7〜39.5秒）

### 原因1: `@` の直後の距離ラベルを設定タイムとして読んでいた

`parseAtTarget` の正規表現が `@300m 38.7〜39.5秒` の **300** を拾っていた。

これは表示だけの問題ではない。設定300秒/300m はGRPの721%なので、
**パーサはこの日をCVと判定していた**。画面には「高乳酸」と「設定300秒」が同時に出ていて、
どちらが本当か本人に判断できない状態だった。

`describeSpec` のコメントには「一括入力が読み取れる書き方に揃える（同じ解釈を通すため）」と
書いてあったが、**成立していなかった**。自分が出したメニューを自分で読み違えていた。

数字の直後が `m` ならラベルとみなして飛ばすようにした。`@300`（m無し）は従来どおり300秒。

### 原因2: レストが漸進の掛け算で端数になっていた

3週目の密度上げで `restSec * 0.8` を掛けて207秒になっていた。
トラックで秒単位に刻む値ではないので精度に意味が無く、読んでも理由が分からない。

5秒刻みに丸める（`roundRestSec`）。**丸めるのは表示ではなく値そのもの。**
表示だけ丸めると、文面の210秒と保存された207秒が食い違って、どちらが本当か分からなくなる。

### 原因3: 結果入力のレストが処方を見ずに既定値へ落ちていた

`useState(String(ex?.restSec ?? ex?.restDistanceM ?? 300))` — 過去の結果が無ければ **300固定**。
処方を一切見ていなかった。

`PrescriptionStructure` に `restSec` / `restDistanceM` / `restType` を持たせ、
初回入力では処方から入れるようにした。読み取れなかったときだけ従来どおり既定値にする。

あわせて、レストの種類が括弧で後置される形（`r207秒（jog）`）をパーサが読めるようにした。
読めないままだと「完全休息」がjogとして扱われる。

### 検証: 生成器とパーサを突き合わせる（`prescriptionRoundTrip.test.ts`）

片方だけのテストではこの食い違いは見つからない。生成した処方を全部パーサに通し、
カテゴリ・設定タイム・反復距離・レストが一致することを見る。
`@` の修正を戻すと4件落ちる（確認済み）。

E2Eにも「処方の文面と結果入力の欄が同じ値」を足した。
レストの初期値を処方から入れる処理を外すと落ちる（確認済み）。

### 見つけたが直していないもの

この突き合わせで**同じ型の食い違いが2つ**見つかった。どちらも別の判断が要るので今回は直さず、
現状をテストで固定した（直したらそのテストが落ちるので気づける）。

| 食い違い | なぜ今回直さないか |
| --- | --- |
| 閾値の設定（GRPの160%）がパーサのCV帯に入る | 帯の再調整は競技的な判断を含む。片方に寄せると練習の意味が変わる |
| 複合（`500m + 300m`）の処方を持続走として読む＝欄が組み上がらない | パーサの複合対応が要る。モデリングの日の結果入力に影響するので別途 |

流し（`150m流し × 6本`）は設定が書かれていないので判定できず、パーサは
`categoryCertain: false` を返している。これは「分からない」と言えているので正しい振る舞い。

---

## 複合（モデリング）の処方を自分で読めなかった（forge-v83 / 2026-08-14）

forge-v82 で生成器とパーサを突き合わせたときに見つけた2件のうち、実害が明確なほう。

```
生成: 500m + 300m @500m 68.7〜69.4秒 / 300m 41.2〜41.6秒 r1分（walk）
解釈: kind=continuous, slots=[]   ← 持続走として読む
```

**モデリングの日に結果入力の欄が組み上がらない。** 距離も設定も出ないので、
レースの形を再現する練習をやったのに、その中身を記録できない状態だった。

### 直し方: 生成器を、本人が書く形に寄せた

パーサは元から `300(42)＋600(1:26)＋600(1:26)` の形を読める。
これは本人が日誌に書いている形そのもの（一括入力の例文にも載っている）。
生成器がそれと違う形を出していたことが原因なので、生成器側を寄せた。

```
500m(68.7〜69.4)＋300m(41.2〜41.6) r1分（walk）
```

距離と設定が並ぶので人にも読みやすい。本数ぶんは繰り返して書く
（`×2` はパーサが本数として数えないため、まとめると区間が減って伝わらない）。

パーサ側に旧形式の読み方を足す案は採らなかった。
唯一の解釈場所に、もう生成されない形式のための分岐を永久に残すことになるため。

### 端末に残っている旧形式の予定について

すでに生成済みのモデリングの予定は旧形式のままで、読めない。
ただし遠い日の予定は確定範囲（14日）の外＝素案なので、近づけば作り直される。
すぐ揃えたい場合は目標・レース画面から再生成する（手で編集した予定は保護される）。

旧形式が読めないこと自体はテストで固定してあるので、状態は追える。

### カテゴリの食い違いが1件増えた（想定どおり）

複合が区間に割れるようになったことで、パーサがモデリングを高乳酸と読むようになった。
これは**設定ペースの比率からは区別できない**もので、文面の問題ではない。
モデリングは「レースの形を再現する」構造で決まる種目なので、
区間ごとのペースだけ見れば高乳酸と同じに見える。既知として固定した。

残る既知の食い違いは2件（どちらもペース比からカテゴリを復元できない性質のもの）:

| 食い違い | 性質 |
| --- | --- |
| threshold → cv | 閾値の設定がパーサのCV帯（GRPの160%）に入る。帯の再調整は競技的な判断を含む |
| modeling → high_lactate | モデリングは構造で決まる種目。ペース比からは判定できない |

---

## 決められないカテゴリは断定せず、本人に選ばせる（forge-v84 / 2026-08-14）

forge-v82・v83 で生成器とパーサを突き合わせたとき、文面の問題ではない食い違いが2件残った。
どちらも**設定タイムだけでは元のカテゴリを復元できない**もの。断定をやめた。

### なぜ帯を調整しないのか

比率を実測して分かった。生成器が出す設定は次のようになっている。

| カテゴリ | GRP比 | 設定の出どころ |
| --- | --- | --- |
| 神経系 | 0.88 | GRP |
| 高乳酸 | 0.94 | GRP |
| モデリング | 0.99 | GRP |
| 経済走 | 1.03〜1.06 | GRP |
| CV | — | **実測LT − 6〜8秒/km** |
| 閾値 | 1.60 | **実測LT** |

CVと閾値だけ出どころが違う。GRPではなく本人の有酸素プロファイルから作られており、
しかも互いに6〜8秒/kmしか離れていない。**GRP比のどこに線を引いても、どちらかを取り違える。**
帯の数値をいじって直る話ではない。

モデリングも同じで、「レース距離を2つに割って1本ずつ」という**構造**で決まる種目なので、
区間ごとのペースだけ見れば高乳酸と区別がつかない。

### 断定をやめた領域

- **GRP比 1.12超**（CVと閾値の帯）
- **レース距離を2つに割った形**（2区間・距離が違う・合計700〜900m）をレースペースでやったもの

候補は今までどおり返す（画面の初期値になる）。断定だけをやめ、理由を添えて選ばせる。
表記辞書に登録された語があれば、そちらが先に効くのでここまで来ない。

### 広げすぎない

最初は「複合ならすべて未確定」にしたが、実データの
`300(42)＋600(1:26)＋600(1:26)`（3区間・合計1500m）まで巻き込んだ。
これは分割ではなく反復なので断定してよい。貼るたびに何行も選ばせるのでは、
入力の負担を減らすという一括入力の目的そのものを損なう。

条件を「レース距離の分割の形」に絞った結果、実データ13行で選択が要るのは**1行だけ**
（7/18 の `1000(3:15-25)×4`、GRPの137%）になった。

その1行は、以前は距離だけで「CV」と断定していた。1:49の走者にとって
1000mを3:15〜3:25は閾値の側なので、**断定は間違っていた**。
黙って間違えるより選んでもらうほうがいい。

### 検証

`tests/bulkImport.test.ts` の「実データで選択が要るのは1行だけ」が件数を固定している。
増えたら落ちるので、判定を広げすぎたことに気づける。
突き合わせ（`prescriptionRoundTrip.test.ts`）では、**断定したものは全部一致する**
ことを見るようになった（既知の食い違いはゼロ）。

---

## 複合の結果入力で「何を入れればいいか」が決まらなかった（forge-v85 / 2026-08-14）

`1000×4＋200×3` の日を結果入力で開くと、本数・距離・設定の欄が**1組しかなかった**。

```
本数 7 / 距離(m) 1000 / 設定(秒) 190.0
```

複合では距離も設定も本ごとに違うので、この3つに何を入れれば正しいのかが決まらない。
合計の7を入れるしかなく、しかも距離1000mが7本すべてに効いているように見える。

### 直し方

複合のときは1組の欄を出さない。代わりに

- **予定の構成**（`1000m×4 ＋ 200m×3`）を出す
- **合計本数**だけを直せるようにする（途中で打ち切ったとき用）

距離と設定は、もともと1本ずつの欄が
`1本目 予定1000m／設定 190秒` の形で持っていた。そちらが正しいので、
上の1組は消してよかった。**入力欄を足したのではなく、迷いのもとを消した。**

区間の並びを「距離×本数」に戻すのは `slotComposition`（純関数）。
連続して同じ距離が並んでいるものだけをまとめ、**並びは変えない**
（300→600→300 を 300×2＋600 にすると、やる順番が変わってしまう）。

### 検証

E2Eで、モデリングの日を開いて
「構成が出ている」「合計本数がある」「距離・設定の単独欄が無い」「本ごとに予定距離が出ている」
を確認している。複合の分岐を外すと落ちる（確認済み）。

## RPEをこちらで埋めない（forge-v86 / 2026-08-15）

結果入力のRPEに `7`、セッション実行の終了時に `8` が**あらかじめ入っていた**。
「メニュー入力でrpe事前に入力されてんのやめてや」で指摘された。

### なぜダメか

RPEは**きつさの感じ方**で、本人にしか分からない。
こちらが7を置いておくと、そのまま保存されたときに
「本当に7だったのか」「初期値のままだったのか」が**あとから区別できない**。

RPEは飾りではない。設定ペースの補正（`RPE_ADJUST_SEC_PER_POINT = 0.15秒/点`）に
そのまま効くので、初期値が混ざると設定が静かにずれていく。
これは「読めなかったものを推測で埋めない」を、入力欄の初期値にも当てはめたもの。

### やったこと

- `app/results/page.tsx` / `app/run/page.tsx` の初期値を空にした
- **すでに保存してある結果を開き直したときは、その値を出す**（それは推測ではなく実測）
- 空のまま／範囲外のまま保存しようとしたら止めて、理由を出す
  （`Number("")` は `0` になるので、黙って通すと0点として記録に混ざる）

範囲は1〜10。ここを外れた値を弾くのは、打ち間違い（`77` など）が
そのまま補正に流れるのを防ぐため。

### 残っているもの（未変更）

主観（`subjective`）も同じく `moderate` があらかじめ入っている。
こちらは補正の計算に直接は効かないので今回は触っていない。
同じ理由で外すべきなら言ってほしい。

### 検証

E2Eの結果入力の流れの中で、
「新規で開いたときRPE欄が空であること」「空のまま保存しようとすると止まること」を見ている。
初期値を `7` に戻すと2つとも落ちる（確認済み）。

## メニューの枠をN日周期にする（forge-v87 / 2026-08-15）

「メニュー設定を月火水木金土日じゃなくて10日周期とかにできるようにする。
その時にも、その周期で最適なメニューを生成してくれるようにしたい」

### なぜ7日をやめられるようにするか

**7日は生活の都合であって、回復に必要な日数とは関係がない。**

高乳酸のあとは中2日（72時間）、高乳酸どうしは最短5日。
これを守ったまま週2枠に押し込むと、火・金（中2日／中3日）のような並びに固定される。
中3日で組みたい局面（本数を増やす週・暑熱期・故障明け）では、
どちらかが中1日になるか、片方が消える。

10日で3本なら、間隔は3・4・3日で組める。7日周期では作れない並び。

### 決め方

`src/lib/core/cycleTemplate.ts`（純関数）が周期の形を決める。

1. **ポイントの本数** — そのフェーズの週テンプレートを**実際に数えて**週あたりの本数を出し、
   周期の長さに引き伸ばす（Specific期は週2本 → 10日で3本）。
   別表は作らない。表と生成器がずれると、曜日と周期で中身が変わってしまう
   （`categoryCountsPerFourWeeks` と同じ理由）
2. **配置** — 等間隔。間隔が中2日（3日）を切るなら本数を減らす
3. **中身** — きつい方（高乳酸・経済走・モデリング）ときつくない方（CV・閾値）の
   割合をフェーズから取り、通し番号で等間隔にばらす
4. **検証** — 周期を14回繰り返して並べ、**暦の7日窓を1つずつ数える**

残りは、ポイントの翌日＝回復ジョグ、ポイントの前日＝神経系（週テンプレートも
「木＝流し → 金＝経済走」の並びになっている）、ポイントからいちばん遠い日＝ロングラン。

### いちばん厄介だったところ

**アプリのルールが暦の週で数えている。**

RULE-04 は「同じ週に高負荷が4日以上」「高乳酸・中距離特異的が3日以上」でERRORを出す。
10日周期は暦の週と噛み合わないので、**周期の中では等間隔なのに、
暦の第2週だけ高負荷が3日**という並びが普通に出てくる。目で見て気づける形ではない。

だから配置を決めたあとに7日窓を全部数え、通らなければ
きつい方の割合を落とし、それでも駄目なら本数を減らす。
**減らしたことは理由とセットで返す**（生成完了のメッセージに出る）。

さらに、生成の途中で内容が入れ替わる経路が2つあった。

- **M-7 の制限因子による振り替え**が、CVを全部レースペース経済走に回していた。
  曜日で組んでいたころはポイントが週2枠しか無かったので表に出なかったが、
  周期にして3枠目ができた時点で毎回起きる。振り替え先がきつい方のときは、
  直前7日間にすでに2日あれば振り替えない
- **フェーズの境目**では配置そのものが切り替わる（Base 10日は2本、Specific 10日は3本）。
  周期をまたいで3日入ることがあるので、最後の保険として、
  きつい方を置こうとした日の直前7日間にすでに2日あればCVに落とす

**テーパー期には周期を当てない。** あそこはレース日から逆算した固定の手順
（8日前に最終高乳酸／7日前以降は高負荷なし／3日前から総量を削る）で、
基準にしている日が違う。周期を優先すると、レース1週間前に高乳酸が入る周が出る。

### 長さの範囲（4〜14日）

下限4日: ポイントの間隔は最低でも中2日。3日周期だと3日おきに来るので、
暦の7日間に3本入って必ずERRORになる。4日なら7日窓に入るのは最大2本。

上限14日: これ以上長いとフェーズ（Base 12週〜／Taper 2週）より周期が粗くなり、
「周期の途中でフェーズが変わる」ほうが常態になって、何を繰り返しているのか分からなくなる。

### データ

`WeekTemplate.cycle` として足した。テーブルは変えていない（保存は JSON 1件）。

**曜日の設定は消さない。** 周期に切り替えても `slots` はそのまま残す。
試して戻したくなったときに曜日を入れ直させるのは、設定を壊すのと同じ。

枠の語彙（自動／優先／固定・カテゴリ・午前枠）は曜日とまったく同じものを使い、
反映の実装も1か所（`applySlotPreferences`）に寄せた。
別々に書くと、片方だけ直したときに「同じ設定なのに曜日と周期で結果が違う」になる。

### 検証

- `tests/cycleTemplate.test.ts` — 4〜14日の全部の長さで、周期を12回繰り返した
  7日窓を1つずつ数える。間隔・高乳酸の5日・役割の割り当ても見る
- `tests/cyclePlan.test.ts` — 実際に処方まで作り、**ルールエンジンにかけてERRORが0件**であること。
  周期を使わなければ従来と1文字も変わらないことも見る
- E2E — 10日周期に切り替えて設定・保存・生成し、高負荷が周期の決まった位置にだけ来ること、
  暦の7日窓が埋まらないこと、曜日に戻したときに曜日の設定が消えていないこと

7日窓の保険を外すと単体テストが4件落ち、周期の生成経路を外すとE2Eが落ちる（両方確認済み）。

## 期分けを冬にも通す（冬季・基礎構築モード）（forge-v88 / 2026-08-15）

### すでにあったもの（レースから逆算する期分け）

目標レース日からの残り日数だけで決まる。`phaseForDaysToRace`（`periodization.ts`）。

| 残り日数 | フェーズ | 呼び名 |
| --- | --- | --- |
| 84日〜（12週〜） | Base | 基礎期 |
| 56〜83日（8〜12週） | Build | 準備期 |
| 28〜55日（4〜8週） | Specific | 専門期 |
| 14〜27日（2〜4週） | Modeling | 試合期（レース再現） |
| 〜13日 | Taper | 調整期 |

判定は**週単位**（その週の真ん中の日で決める）。週の途中でフェーズが変わると、
同じ週の前半と後半で違う組み立てになるため。

フェーズが変えているのは5つ。

1. **週の並び** `weekTemplate(phase, ...)` — Base はジョグ＋坂＋閾値＋流し＋隔週の高乳酸＋ロングラン、
   Specific は高乳酸＋経済走、Taper は流しとジョグだけ
2. **処方の中身** `RECIPE_CATALOG[カテゴリ][フェーズ]` — 同じ「経済走」でも Build と Modeling で別のレシピ
3. **設定ペースの土台** `PHASE_GOAL_WEIGHT` = 今のCFEと目標タイムの混ぜ方。
   Base 0.0 → Build 0.2 → Specific 0.5 → Modeling 0.8 → Taper 1.0。
   **基礎期は目標タイムを一切混ぜない**（今出せる値で組む）
4. **補強** `strengthForPhase` — Base は重い（スクワット5×5・デッドリフト3×5）、
   Specific は速度重視、Taper は体幹のみ
5. **テーパー固有の手順** — レース8日前に最終高乳酸1本（RULE-07）、
   7日前以降は高負荷なし（RULE-08）、3日前から総量を削る（RULE-09）

これとは別に、フェーズの中で4週サイクルが回る（`weekStep`: 入り口 → 量 → 密度 → 回復）。

### 足りていなかったもの

**目標レースが無いと生成が例外で止まっていた。**（`目標レースが見つかりません`）

冬にレースが無いのは普通のことなので、**レースが無いことを異常にしない**。

### 冬季・基礎構築モード

目標画面で「レース未定（冬季・基礎構築）」を選ぶ。`Goal.targetRaceId` を空にして保存する。

**やらないこと**

- ピーキングしない。テーパーの3手順（最終高乳酸・高負荷の停止・総量の削減）を1つも通さない。
  `raceDate` は「どこまで作るか」の区切りでしかないので、
  そこへ向かってテーパーが始まると**作った期間の終わりが勝手に軽くなる**。
  予定は出ているので画面では気づけない
- フェーズを上げない。Base のまま固定する。レース日から数える意味が無い
  （そのレースが無い）のに、週が進むほど Build → Specific と上がるのは間違い
- 設定ペースに目標タイムを混ぜない（Base の重みが 0.0 なので自動的にそうなる）。
  冬に「1:48.5で走れる前提のペース」を出しても実行できないだけ

**やること**

16週を4週×4ブロックで組み、重心を移す。`src/lib/core/offSeason.ts`。

| ブロック | 重心 | 週の並び |
| --- | --- | --- |
| 1〜4週 | 有酸素の土台 | 坂1・閾値1・流し1・ロングラン80分 |
| 5〜8週 | 筋力・坂 | **坂2**・閾値1・ロングラン70分 |
| 9〜12週 | 閾値・CVの量 | 閾値1・CV1・流し1・ロングラン70分 |
| 13〜16週 | スピードの土台 | 坂1・流し1・隔週で高乳酸かCV・ロングラン65分 |

ブロックを4週にしたのは、既存の負荷サイクル（`LOAD_CYCLE_WEEKS`）と同じ長さにするため。
ずらすと「量を増やす週」と「重心が変わる週」が別々に来て、何が効いたのか分からなくなる。

高乳酸は最後のブロックの隔週だけ。16週で最大4本。
レースが無い期間に高乳酸を積む理由が無く、積むと春に上げしろが残らない。

補強は冬季だけ**坂の日にも付ける**。「筋力・坂」のブロックは走る質が閾値1本しか無いので、
高負荷の日だけに寄せると週1回しか入らない。坂は解糖系の負債が小さいので重い補強を重ねられる。
**流しの日には付けない**——あそこは動きを出す日で、重いものを引いたあとの流しは目的から外れる。

16週で区切るのは、レースが無いと終わりが無いから。重心が一巡する長さで切って、
足りなくなったら作り直す（`horizon.ts` と同じ考え方——先の予定ほど実際の状態から外れる）。

### フェーズを増やしていない理由

処方の中身（`RECIPE_CATALOG`）・補強・ペースの土台が全部フェーズで引かれている。
`OffSeason` のようなフェーズを足すと、**そこに載っていないカテゴリが固定文面に落ちて**
漸進（量→密度→回復）が効かなくなる。
冬にやりたいのは「基礎期の中で重心を移すこと」なので、Base のままでよい。

### 冬でも変わらないもの

- 通過点レース（記録会）はそのまま効く。当日はセッションを置かず、前3日は軽くする。
  本命が無いだけで、レースが1つも無いわけではない
- N日周期（forge-v87）も効く。周期の形はブロックごとに作り直す
  （フェーズだけを鍵にすると4ブロック全部が最初のブロックの形になる）
- 結果を入れたときの確定範囲の作り直し（`refreshNearHorizon`）も動く。
  以前は目標レースが無いと諦めていたので、冬は結果を入れてもCFEが予定に反映されなかった。
  **冬こそ土台が動く時期**なので止める理由が無い

### 検証

`tests/offSeason.test.ts` —
レース無しで生成できること、ルールエンジンがERRORを出さないこと、
フェーズが Base のまま動かないこと、末尾が軽くならないこと（テーパーしていない）、
4ブロックの中身が全部違うこと、レースを設定すれば従来どおり Taper まで出ること。

E2Eは、目標画面で切り替えて保存 → 生成 → Base固定・4ブロック・末尾も落ちない、
そのあと**本命レースに戻して従来の期分けに帰れる**ところまで見る
（戻せないと、冬に切り替えた時点で春の予定が作れなくなる）。

テーパーの抑止を外すと単体テストが1件、ブロックの回転を止めると2件落ちる（両方確認済み）。

## 変更履歴がバックアップから抜けていた（forge-v89 / 2026-08-15）

機能連携の棚卸しで見つけた1件目。**優先度が一番高かったもの。**

### 何が起きていたか

`exportBackup` は17項目を書き出すのに、**変更履歴（changeLog）だけが入っていなかった**。

iOSのSafariはストレージを消すことがある。だから書き出し（M-12）がある。
その想定どおりの経路——消える → 書き出しから復元——を通ると、
**「いつ・何を・なぜ自動で変えたか」だけが消える。**

結果もCFEの値も残るので、**失われたことに気づけない**のがいちばん危ない。
あとで「この設定ペース、なぜ下がったんだ」と思ったときに、もう追えない。

「自動で変えたことは理由とセットで出し、本人が却下できるようにする」を
このアプリの原則に置いているのに、その原則が**復元経路でだけ切れていた**。

### 直したこと

- `exportBackup` に `changeLog` を足した（新しい方から2000件。古い方から捨てる）
- `importBackupData` で復元する。**記録した日時をそのまま書き戻す**
- `Store` に `restoreChangeLog` を足し、SQLite と IndexedDB の両方に実装した

`logChange` を復元に使い回さなかったのは、あれが「いま起きた変更」を積む関数で、
日時を今にするから。通すと去年の変更が全部「復元した日」になり、
変更履歴が時系列として読めなくなる（何がいつ効いたのか分からない）。

### 2000件の理由

1件あたり200バイト前後。結果1件につき数件が積まれるので、
2000件で**1〜2シーズンぶん**にあたる。
それより古い自動変更は、その頃の設定ペースを疑う場面がもう来ない。
無制限にすると書き出しが肥大するので、新しい方を優先して残す。

### merge の同一判定

変更履歴はidを持たないので、id突合（`mergeById`）が使えない。
**「同じ日時に・同じセッションの・同じ項目を・同じ理由で変えた」**ものだけを同一とみなす。
日時はミリ秒まであるので実用上ぶつからない。

ここを緩めて「内容が同じなら同一」にすると、
**同じ日に2回引き直した記録が片方消える**（暑熱でさらに引き直した、など）。
逆に厳しくすると、同じファイルを2回取り込んだときに二重になる。両方テストで固定した。

### 検証

`tests/backupChangeLog.test.ts` — 書き出しに入ること、上限で古い方から落ちること、
日時をそのまま持ち越すこと、却下の記録ごと戻ること、
同じファイルを2回mergeしても二重にならないこと、
同じ日の2回の引き直しが両方残ること、変更履歴の無い古いファイルでも落ちないこと。

**保存層の組み合わせを全部通す**（SQLite↔IndexedDB の4通り）。
端末で動いているのは IndexedDB 側なので、SQLiteでしか確かめていないと
テストは通るのに実機で消える。

書き出しから外すと13件、復元の日時を「今」にすると6件落ちる（両方確認済み）。

## 二重管理をやめる：定数・呼び名・補強テーブル（forge-v90 / 2026-08-15）

機能連携の棚卸しの2件目と3件目。どれも**同じ知識が2か所にある**という形の問題で、
片方を直しても、もう片方は静かに古いままになる。

### 4週サイクルの長さ（D-1）

`progression.ts` の `LOAD_CYCLE_WEEKS`（入り口→量→密度→回復）と
`offSeason.ts` の `OFF_SEASON_BLOCK_WEEKS`（冬季の重心が変わる間隔）が
別々に `4` と書いてあった。「そろえる前提」とコメントにあるだけで、連動していない。

ずれると「量を増やす週」と「重心が変わる週」が別々に来て、
**何が効いたのか分からなくなる**。数字を書くのをやめて参照にした。

### 負荷分類の呼び名（D-2）

`rules.ts` に `QUALITY_CATEGORIES` / `isQuality` / `isSpecific` / `isHlEquiv` という
`@deprecated` 付きの別名があり、**rules.ts の中では7か所で現役だった**。
中身は `trainingClassification.ts` の関数を呼ぶだけのラッパ。

実体は1つなので判定は競合していなかったが、
「非推奨」と書いてあるものを使い続けると、どちらが正なのか毎回確かめることになる。
呼び出しを本来の名前に置き換えてラッパを消した。テスト1372件はそのまま通る（意味を変えていない）。

### 補強のフェーズ別テーブル（C-2）

同じ知識が2か所にあった。

- `strength.ts` の `STRENGTH_PHASE_TABLE` ── 「UI表示用」と書いてあるが**出している画面が無かった**
- `periodization.ts` の `strengthForPhase` の中の別テーブル ── 実際に生成に効くほう

実際にずれていて、Build は片方が「moderate〜heavy」、もう片方が `moderate` だった。

`strength.ts` を唯一の出どころにし、生成側がそれを読む形にした。
表には生成に使う値（`load` / `type` / `exercises`）と
画面に出す説明（`strength` / `plyometrics` / `frequency`）が並んでいる。

**寄せるときに生成される補強が変わっては意味が無い**ので、
先に現状の出力を固定するテスト（`tests/strengthPhase.test.ts`）を書いてから寄せた。

### 出したうえで「今はここ」を出す

メニュー設定に「補強はフェーズでこう変わる」を追加した。
表だけ出しても**ただの資料になって読まれない**ので、現在の期を強調し、
いまの期に実際に出る種目も並べる。
現在の期は `currentPhase`（service）で判定する——生成と同じ関数を通すので、表と実物がずれない。

### 検証

- `tests/strengthPhase.test.ts` — 表の中身、生成される補強が表のとおりであること、
  補強が高負荷の日にだけ置かれること、全フェーズが実際に出ていること、
  **説明と値が食い違わないこと**（「heavy」と書いてあるのに light を生成する、を弾く）
- E2E — 画面に5期ぶん出ていること、いまの期の種目が出ていること、
  そして**生成された補強と表が一致すること**

E2Eの一致確認は最初 0件を照合して素通りしていた（`/api/analysis` は補強を素で返さない）。
**通ったこと自体は何の証明にもならない**ので、照合件数がゼロなら落とすようにした。
手で入れた補強（一括入力の「7/20 体幹30分」など）は本人の記録なので照合から外す。

生成側だけ `light` に差し替えると、単体テスト1件とE2Eが落ちる（確認済み）。

## 周期・冬季を「決めたあとも追える」ようにする（forge-v91 / 2026-08-15）

機能連携の棚卸しの4件目と5件目（B-3・B-4）。

### 何が切れていたか

N日周期（forge-v87）も冬季・基礎構築モード（forge-v88）も、
**決めた直後の画面メッセージにしか出ていなかった**。同時に3つ起きていた。

1. `limiterSwaps` と `spacingSwaps`（生成の途中で内容を入れ替えた枠）の理由が保存されず、
   **画面を離れると消える**。あとで予定を見返しても「なぜここだけCVなのか」が分からない
2. TODAY にもカレンダーにも、周期の何日目か・冬季の第何ブロックかが出ない。
   **繰り返している構造が画面から見えない**ので、周期にした意味（間隔を自分で決める）が
   本人にも分からなくなる
3. 相談（AI）に送る文脈に入っていない。
   **冬季モードで相談してもレースに向けた期分けの前提で答えが返り**、
   10日周期で組んでいても週7日前提で返っていた

### 直したこと

**入れ替えた枠を変更履歴に残す。** `logChange` に流すので、
分析画面から読めて、バックアップにも入る（forge-v89 で往復するようにした）。
`direction` は `neutral` にしてある——量や強度を動かしたのではなく
**枠の中身を入れ替えただけ**（CVを経済走に、経済走をCVに）なので、
`down` にすると「軽くした」記録として集計に効いてしまう。
保存済みの枠ぶんだけ残す（安全性の判定で捨てた枠の理由を残しても、対応する予定が無い）。

**TODAYに1行出す。** 「周期 4日目 / 10日」「第2ブロック 筋力・坂」と、
冬季ならそのブロックの狙いも添える。押すとメニュー設定に飛ぶ（違うと思ったら変えられる）。
曜日で組んでレースにも向かっているときは**何も出さない**——
そのときは「毎週これ」が当たり前なので、書いても情報が増えない。

**相談にも同じものを送る。** 画面と送信は同じ関数（`todayStructure`）から作る。
別々に組み立てると、片方を直したときに
「画面ではこう見えているのにAIは別の前提で答える」ことになる。

### ブロックを予定に持たせた理由

`Session.offSeasonBlock` を足した。**あとから計算し直せない**ため。

ブロックは生成の開始日から数えた週で決まるが、その開始日はどこにも残らない
（予定を部分的に作り直すと、いちばん古い予定の日付も動く）。
「この日はどのブロックだったのか」は、生成した時点でしか分からない。

周期の位置のほうは保存しない——起点の日付が設定に残っているので、
その場で数えられる（`cyclePositionFor`）。**計算できるものは持たない。**

### 検証

`tests/structureVisible.test.ts` —
曜日＋レースなら何も出さないこと、周期なら何日目か（折り返しも）、
冬季ならブロックと理由、ブロックが4つとも予定に残ること、
相談の文脈に入ること、**画面に出しているものと送っているものが一致すること**、
入れ替えた枠の理由が履歴に残り、記録した枠が実在すること。

E2Eは、周期に設定して再生成 → TODAYに「周期 1日目 / 10日」が出る →
相談の文脈にも「10日周期」が入る → 入れ替えた枠の理由が `/api/changes` から読める、まで見る。

TODAYの行を外すとE2Eが、相談への送信を外すと単体テスト2件が落ちる（確認済み）。

### ついでに直したもの

`limiterSwaps`（M-7の制限因子による振り替え）も同じ問題を持っていた。
棚卸しでは挙げていないが、**同じ修正で直る2件のうち片方だけ直すほうが悪い**ので一緒に入れた。

## 風・雨をどこまで効かせるか決める（forge-v92 / 2026-08-15）

機能連携の棚卸しの6件目（B-1）。**判断が要る項目**だったので、決めてから直した。

### 決めたこと

**風と雨は暑熱条件の判定に入れない。** 注意書きまでで止める。

- 効く: `environmentNote` の注意書き（「強風: 向かい風区間で1周1〜2秒の遅れ。達成度の判定を緩めて」など）
- 効かない: **暑熱条件フラグ**＝その練習を能力推定から外すかどうか。
  これは気温と湿度から出すWBGTだけで決める

入れないのは根拠が無いから。風雨は「体感が下がるので能力推定から**外すべきでない**」方向にも、
「条件が悪いので**外すべき**」方向にも効きうる。
どちらか決められないまま係数を置くと、その数字がどこから来たのか説明できなくなる。
根拠が出てから入れる。

### 直したこと

入力欄の下に、どこまで効くのかを書いた。
入力欄があるのに判定が動かないと「効いているはず」と読めてしまう。
`evaluateEnvironment` にも、風雨を**意図的に**見ていないことと、その理由を書いた。

`tests/environment.test.ts` で決めたことを固定する。
風・雨をどう変えても `isHeatFlagged` と WBGT が動かないこと、
注意書きには出ること、気温が無ければ何も判定しないこと。
**あとで根拠が出て風雨を判定に入れるなら、このテストが落ちる**——
そのとき画面の文言（「風雨は入れていません」）も一緒に直せる。

### 棚卸しの訂正: 接地時間はすでに繋がっていた

同じ棚卸しで「FITの接地時間が疲労判定に流れ込まない」と書いたが、**これは誤りだった**。

`contactTime.ts` の `contactSamplesFromResults` が `SessionResult.avgGroundContactTimeMs` を読み、
`allContactSamples` がCSV取り込みぶんと合わせて判定に渡している。テストもある。
調査のときCSV側の経路（`importContactSamples`）だけを追って、もう一方を見落とした。

残っているのは**上下動（`avgVerticalOscillationMm`）と歩幅（`avgStepLengthM`）**で、
こちらは取り込んで保存するだけでどこからも読まれない（ケイデンスは分析画面に出ている）。
小物としてまとめて判断する。

風・雨についても「入れても何も起きない欄」と書いたが、これも言い過ぎだった——
注意書きには効いている。効いていないのは暑熱条件フラグだけ。

## 予定と実際のズレを分析画面に出す（forge-v93 / 2026-08-15）

機能連携の棚卸しの8件目（C-1）。**判断が要る項目**だったので、決めてから直した。

### 何が起きていたか

`trainingBalance.ts`（341行）が**どこからも import されていなかった**。
週ごとの計画と実施の突き合わせ、負荷、カテゴリ内訳、未達・積み残しを計算する
完成したモジュールが丸ごと死んでいて、ビルドにも乗っていた。

### 決めたこと

**載せる。** 消す案もあったが、設定を守れているかは
このアプリが繰り返し使っている判断材料で（`recentTrend` が同じことをカテゴリ単位で見て、
処方の本数とレストを動かしている）、**自動で動いている判断の材料を
本人も同じ形で見返せる**ようにするのは筋が通っている。

### 役割分担

`periodSummary`（分析画面で現役）と重なるので、切り分けを決めた。

| | 見るもの | 出す場所 |
| --- | --- | --- |
| `periodSummary` | 距離・時間・強度の**合計**（WEEK / MONTH / YEAR）。伸びたか | 期間サマリー |
| `trainingBalance` | 予定に対して**どれだけ実施できたか**。守れているか | 予定どおりにできたか |

同じ週の合計を2か所に出さない。画面に出しているのは
実施/予定・達成率・高負荷・高乳酸・回復の5つで、距離と時間の合計は期間サマリー側だけ。
`trainingBalance` は内部では距離の合計も持っている（負荷の急増を見るのに要る）が、
画面の主役にはしない——同じ数字が2か所に出ると、見比べたときに食い違って見える。

週の区切りは両方とも月曜始まりでそろえた。

### 気づきの出し方

`signals`（負荷の急増・高負荷の連続・回復不足・達成率の低下など）は
**出たときだけ**出す。毎回同じ行が出ると読まれなくなる（午前枠の助言と同じ考え方）。
断定せず、対処案まで一緒に出す。

### 検証

`tests/trainingBalanceWiring.test.ts` — 4週ぶんが返ること、
**予定と実施を別々に数えること**（未実施を実績に混ぜない）、
実施が増えると達成率が上がること、レースが無くても落ちないこと、
気づきに必ず対処案が付くこと、週の区切りが月曜始まりであること。

E2Eは、APIから4週ぶんが返る → 分析の「推移」に表が出る →
**画面の「実施/予定」がAPIの数字と一致する**まで見る。
一致まで見ないと「表は出ているが別の値を映している」に気づけない。

カードを外すとE2Eが3件落ちる（確認済み）。

## 棚卸しの小物をまとめて片付ける（forge-v94 / 2026-08-15）

機能連携の棚卸しの最後（C-3・C-4・B-5・B-7・C-5）。

### レースを消せるようにした（C-3）

保存層には前から `deleteRace` があったのに、**呼ぶ側が無かった**。
間違えて登録したレースが残り続け、目標から外しても記録としては消えなかった。

レース画面に導線を足した。ただし消せるほうより**消せないほう**が大事で、2つ守る。

- **本命レース** — 消すと目標が指す先が無くなり、黙って冬季モードに落ちる。
  先に目標側で外す（あちらには「レース未定」の導線がある）
- **結果を入力済みのレース** — それは競技記録で、間違いではない。
  `processRaceResult` が有酸素マーカーを作っているので、消すと現在地の根拠が欠ける

断る理由は画面に**押す前に**書く。押してから断られると理由が分からない。

### 効かない欄に「効かない」と書いた（C-4・B-5）

入力できるのに何にも効かない欄は「効いているはず」と読まれる。

- **身長・骨格筋量** → 「(記録用)」と名前に入れた。どの計算にも使っていない
  （体重は暑熱の計算に使う）。消さないのは、体組成の推移を残しておくと
  あとで「あの時期は絞れていた」を照合できるため
- **最大心拍** → 分析の相対強度にだけ効く。メニューの生成・調整には入れていない
  ——心拍で処方を動かすと、暑熱や睡眠不足の影響を能力の変化と取り違える

### 3000m/5000m のPBは型からも消した（C-4）

`Athlete.pb3000m` / `pb5000m` は**型にあるだけで、入力欄も読み手も無かった**。

有酸素プロファイルに繋ぐ案もあったが、やめた。
実測は「記録」画面の**有酸素マーカー**として入れる形がすでにあり、
あちらは日付つきで、直近8週の重み付け・暑熱条件の除外・用途（閾値/CV/レース）の
区別まで効く。静的なPBを別に持つと**LTの出どころが2つになる**——
このリリース群でずっと潰してきた形そのもの。

設定画面には「3000m/5000mを走ったら有酸素マーカーとして入れる」と置き場所を書いた。
「使わない」だけ書いて置き場所を書かないと、入れる場所を探して迷う。
使われていなかった `PersonalBest` 型も消した。

### 選手タイプの手動設定（B-7）

型のコメントが「自動診断との差異を通知する」と約束していたが、実装が無かった。
調べると**画面から設定する導線も無い**（APIとCLIだけ）。
設定する場所が無い以上、差異を通知する場面も無いので、
通知を作るのではなく**約束のほうを実態に合わせた**。

### 未使用exportの整理（C-5）

棚卸しで挙げた候補を、**自ファイル内での使用も数えて**確かめ直したところ、
半分以上が誤りだった（最初の走査が「他ファイルからの参照」しか見ていなかった）。

本当にどこからも使われていなかった4つを消した——
`describeRaceAnalysis`・`periodLengthDays`・`isWithin`・`describeCycleShape`。
最後のひとつは forge-v87 で自分が書いて、結局使わなかったもの。

自ファイル内でだけ使われているもの（`applyWeekPreferences` ほか）はそのままにした。
export の粒度の話であって不具合ではなく、10ファイルを触る価値が無い。

### 検証

`tests/deleteRace.test.ts` — 通過点は消せること、目標の一覧からも外れること、
**本命は消せないこと**、**結果ありは消せないこと**、断ったときに一覧を触らないこと。

E2Eは、画面に導線と断る条件が出ていること、APIが本命を断ること、
通過点は実際に消えること、プロフィールに「記録用」と置き場所が書かれていること。

本命の保護を外すと単体テスト1件とE2Eが落ちる（確認済み）。

## RPEをスライダーにする／入力方式を内容に合わせる（forge-v95 / 2026-08-15）

### なぜ数値入力をやめたか

テンキーだと `77` のような打ち間違いがそのまま入る。
RPEは設定ペースの補正（`RPE_ADJUST_SEC_PER_POINT = 0.15秒/点`）に直接効くので、
**打ち間違いが静かに設定へ流れる**。段階しか選べない形にすれば、そもそも起きない。

**保存する値は数値のまま**（1〜10の整数）。判定・補正・CFEはすべて数値を見ている。
変えたのは入れ方だけ。

### RPEスライダー

`src/lib/core/rpe.ts` に段階と説明を置き、画面はそれを引くだけにした。
説明文を画面に直書きすると、補正が効く数字と本人が読んでいる言葉が別々に動く。

- 1目盛りずつ止まる（`step=1`。range の既定の挙動で、自前で丸めない）
- 動かしている間ずっと数値と説明が変わる
- **既定値を置かない。** 未入力は「—」と「未入力」で出す（forge-v86 と同じ理由）
- 帯の色は 1〜3 青 / 4〜5 緑 / 6〜7 黄 / 8〜9 橙 / 10 赤。
  **既存のトークンだけを使う**（新しい色を増やさない）
- **色だけに頼らない。** 数字・帯の呼び名（楽／普通／きつい／かなり／最大）・説明文を必ず一緒に出す。
  `aria-valuetext` にも同じ言葉を入れるので、読み上げでも同じ情報が届く
- キーボードは矢印・Home・End がそのまま効く

### 見つけたもの: RPEの入力欄は4か所あった

調査では2か所（結果入力・セッション実行）と報告したが、**実際は4か所**だった。

- `app/past/page.tsx` 過去データの遡り入力
- `pwa/entry.tsx` FIT取込の本人確認 ── ここは `step="0.5"` で **7.5 のような値が入れられた**。
  RPEは整数の段階として定義してあるので、半端な値は意味を持たない

`pwa/entry.tsx` は PWA 側にしか無い画面なので、`app/` を走査した最初の一覧から漏れた。
4か所とも同じ部品にそろえた。

### 内容に合わせて使い分けた

| 項目 | 変更後 | 理由 |
| --- | --- | --- |
| RPE・痛みの強さ | スライダー | 段階の意味があり、打ち間違いが致命的 |
| 主観・翌日の脚・スキップ理由・風・レストの種類 | 選択チップ | select は開く→選ぶ→閉じるで3タップ。3〜6択なら1タップ |
| 痛む部位 | 部位チップ＋自由記述 | 自由記述だけだと「右アキレス」「右アキレス腱」が別物として溜まり、同じ場所を繰り返し痛めているのか分からない |
| 本数・レスト | ステッパー | 「だいたい決まっていて1つ足す・引く」が多い。レストは5秒／50m刻み |
| 距離・設定・ラップ | テンキーのまま | 値の幅が広く、押して合わせると回数がかさむ |
| 疲労・睡眠・モチベーション | 変更なし | すでに5段階ボタン（`Scale5`）だった |
| 日付・時刻 | 変更なし | すでに専用ピッカー |

**ステッパーでも入力欄は残す。** 押すだけにすると、20本を入れるのに20回押すことになる。

### 主観を必須にした

以前は `moderate` が既定で入っていた。RPEと同じ理由で外し、選ばないと保存できないようにした。
`SessionResult.subjective` は省略できない項目なので、既定値を置くか必須にするかの二択になる。

### やらなかったこと

**達成度は4択にしていない。** あれは入力欄ではなく、実測から機械的に決まる値
（`inferAchievement`。本数の完遂率と設定を満たした割合で判定）。
型も3値で「設定以上」が無い。手入力に変えると実測より申告が優先され、CFEの扱いまで変わる。

**路面・シューズは入力欄を作っていない。** 型にはあるが本人が変える画面が無く、
`surface` はルール判定に効くので、足すと判定結果が変わる。今回は入力方式の変更なので範囲外。

### 検証

`tests/rpe.test.ts` — 1〜10が抜けなくあること、全部に説明があること、
帯の割り当てが指定どおりで**途中で戻らない**こと、色が既存トークンだけであること、
境界（0・11・7.5・空文字）を弾くこと、痛み側（0始まり）も同じ性質を持つこと。

E2Eは、説明がその場で変わること、10の説明、`step=1`、
キーボード（←・Home）、`aria-valuetext` に数値と言葉が入ること、チップが44pt以上あること。

RPE 10 の説明を空にすると単体テスト1件とE2Eが落ちる（確認済み）。

E2E側でも1つ直した。N-1（1文字打つたびにキーボードが閉じないか）はRPEの入力欄で見ていたが、
スライダーになったので**同じフォームの「設定(秒)」に付け替えた**——
見ているのは「入力欄が作り直されないか」なので、対象はテキスト入力ならどれでもよい。

## 送った週が戻らないようにする（forge-v96 / 2026-08-15）

### 何が起きていたか

先の週まで送って日付をタップ → メニューを見て戻ると、**今週に戻っていた**。
予定を組んでいる最中だと、毎回そこまで送り直すことになる。

カレンダーの表示期間（週/月・何週間）は端末に覚えさせていたのに、
**どの週を見ているか（`anchor`）だけ覚えていなかった**。
画面を離れるとコンポーネントが作り直され、`useState` の初期値（今週）に戻っていた。

### 直したこと

見ている週を覚える。ただし**表示期間とは保存先を変えた**。

- 表示期間（週/月・何週間）→ `localStorage`。次に開いたときも同じ見え方でよい
- 見ている週 → **`sessionStorage`**。アプリを開いているあいだだけ

来月の予定を見たまま閉じて、翌日開いたら来月が出るのは困る。
アプリを閉じれば今週に戻り、開いているあいだの行き来では保たれる、が正しい。

保存は `anchor` の変化を1か所（`useEffect`）で拾う。
`setAnchor` を呼ぶ場所ごとに保存を書くと、経路が増えたときに書き忘れて同じことがまた起きる。

覚えた値は形（`YYYY-MM-DD`）と、今日から1年以内かを確かめてから使う。
壊れていれば今週で開く（推測で近い日付に寄せない）。
「今日」ボタンで今週に戻せるのは変わらない。

### E2E側で直したこと

週を覚えるようになったので、**カレンダーを送る検証が後続に影響するようになった**。
月送りの検証（3か月先へ送る）とスワイプの検証（1週間送る）のあと、
そのままだと「今週の予定を見る」検証が別の週を開いて落ちる。
カレンダーの検証の最後に「今日」へ戻すようにした。

これは実装の問題ではなく、**画面が状態を持つようになったぶん、検証も後始末が要るようになった**という話。

復元を外すとE2Eが落ちる（確認済み）。
最初の破壊確認は置換が空振りして素通りしていたので、置換できたかを確かめてからやり直した。

## 天候・路面のタグとシューズ（forge-v97 / 2026-08-15）

### 何のために入れたか

**「設定は同じなのにRPEが上がった」の理由を、あとから見分けられるようにする。**

記録が無いと、雨で滑ったのか・暑かったのか・能力が落ちたのかが全部
「調子が悪かった」に丸められ、設定を下げる判断に紛れ込む。

### 天候・路面（複数選択）

`SessionResult.conditions` に文字列の配列で持つ。
晴れ／曇り／雨／強風／暑熱／寒冷、トラック／トラック濡れ／滑りやすい／ロード／不整地／室内。
同時にいくつも当てはまるので複数選択にした（雨で、かつトラックが濡れていた、など）。

**判定には使わない。** 暑熱条件フラグ（その練習を能力推定から外すかどうか）は
今までどおり気温と湿度のWBGTだけで決める。
タグを判定に混ぜると、**付け忘れが能力の変化として現れる**。
テストでも「タグをどう付けても `isHeatFlagged` が変わらない」ことを固定してある。

タグのIDは変えない（過去の記録が指しているため）。表示名だけなら直してよい。

### 分析: 条件でRPEがどれだけ変わるか

分析の「推移」に、タグごとの平均RPEの差を出す。
**両方が2回以上たまったタグだけ**出す——1回ずつを比べても、
その日の体調と条件のどちらが効いたのか分けられない。

ここでも**設定は動かさない**。見て本人が判断する材料。

### シューズ

設定画面で製品名と種類（スパイク／厚底／薄底／トレーニング／トレイル）を登録し、
記録するときに選ぶ。**最後に使ったものが先頭**に来る（毎回同じ靴なら1タップ）。

**使用距離は持たない。記録から毎回足し上げる。**
カウンタを持つと、記録を消したり直したりしたときにずれて、
しかも**ずれたことに気づけない**（記録は正しいのに合計だけが違う）。
テストで「1件消したら合計も減る」ことを固定してある。

セッションごとに選ぶ（1日1足にしない）。実際に履き替えることがあるため。

使った記録がある靴は**消せない**。過去の記録が指す先が無くなり、
「何を履いていたか」が分からなくなる。使い終わったものは「引退」にする
（選択肢から外れ、記録は残る）。

一覧は kv に置いた。数が十数足で検索の条件にもならないため、専用のテーブルを作っていない。
kv は書き出し・復元の両方に乗っている。

### 途中で見つけた不具合

**主観のチップが、選んだあとにもう一度押すと外れていた。**
`allowEmpty` を付けていたので、必須の欄なのに未入力へ戻せてしまい、保存が止まった。
任意の欄（翌日の脚）とは扱いを分ける。E2Eの調査中に見つけたもので、
チップに変えた forge-v95 から入っていた。

**インターバルの距離が0になる経路があった。**
`results?.length ?? reps` と書いていたが、`results` が空配列のとき
`length` は 0 で nullish ではないため、本数が0のまま計算されていた。
シューズの使用距離のテストで捕まえた。

### 検証

`tests/conditionsShoes.test.ts` — タグのIDが重複しないこと、一覧に無いIDを捨てること、
並びが入れた順で変わらないこと、条件別RPEが2回ずつたまってから出ること、
**タグを付けても暑熱判定が変わらないこと**、
使用距離が持続走・インターバルの両方から出ること、
**1件消したら合計も減ること**、最後に使った靴が先頭に来ること、引退が選択肢から外れること。

E2Eは、シューズ登録 → 記録に天候2つ・路面1つ・靴を付けて保存 →
保存されていること → 使用距離が積まれること → **開き直して選択が戻ること**まで。

条件を送るのをやめるとE2Eが落ちる（確認済み）。

---

## 途中でやめた理由（forge-v98 / 2026-08-16）

### 何が問題だったか

**打ち切りは1つの真偽値しか持っていなかった。** そして、その真偽値は
**機械が決めていた**——M-3 の中止基準（設定から+N秒を超えた）に引っかかったときだけ
`aborted: true` になる。

ここに2つの穴があった。

**穴1: 痛くて止めても「完走」になる。** 設定どおりのタイムで走っていれば
中止基準には引っかからないので、脚に違和感が出て3本目で止めた練習は
「2本で完了した練習」として残る。何本の予定だったかは記録に残るが、
途中でやめたという事実そのものが消えていた。

**穴2: 止めた理由で扱いが変わらない。** 影響が一番大きいのは設定ペースの自動補正
（`adaptive.ts` の `executionTrend`）。ここは**打ち切りが2回続くと設定を緩める**。

```
if (paceAbortCount >= 2) { ... verdict: "ease" }
```

雨が強くなって止めた・電車の時間で止めた回数も、同じ2回に数えられていた。
つまり**実力は落ちていないのに設定だけが下がり続ける。**
しかも下がった設定なら当然こなせるので、下がったこと自体に気づけない。
CLAUDE.md の「実行できなかったことは能力低下ではない」を、設定側でも守れていなかった。

### 理由を6つに分けた

`src/lib/core/abortCause.ts` が唯一の出どころ。

| 理由 | いつ | 設定を緩める材料 | 体への負担の証拠 |
| --- | --- | --- | --- |
| `pace` 設定が高すぎた | 前半から離れて、続けても狙った刺激が入らない | ○ | ○ |
| `fatigue` 疲労が残っていた | 設定は妥当だが今日は体が動かない | ○ | ○ |
| `pain` 痛み・違和感 | 安全のため止めた | × | ○ |
| `condition` 天候・路面 | 雨・風・場所の状態 | × | × |
| `schedule` 時間・予定 | 時間切れ | × | × |
| `other` その他 | 自由記述で残す | × | × |

**設定を緩める材料に数えるのは `pace` と `fatigue` だけ。**
M-2 が動かすのは「今日出せる値」なので、体が動かなかったのは材料になる。
雨・時間・痛みは「今日出せる値」の証拠ではない。

**痛みは設定を動かさないが、体への負担としては数える。**
形式の選択（`progression.ts` の `strainedCount`）と疲労の裏付け
（`rules.ts` の `recoveryEvidence` / `acwrCorroboration`）には効く。
一方で天候・時間は、どちらにも数えない。
外的な事情で止めたことが「回復していない証拠」として使われると、
走りと関係ない理由で高負荷の配置に警告が出続ける。

### 未入力（旧データ）は今までどおり数える

`countsTowardPaceEase(undefined) === true`。

これは推測ではない。理由を選べるようにする前の打ち切りは、
M-3 の中止基準で自動判定されたものしか無い。つまり**当時の判定そのものが `pace` に当たる**。
ここを「数えない」に倒すと、これまでの打ち切りが一斉に無効になって、
**今動いている補正が静かに止まる**。

### CFEは理由に関わらず触らない

打ち切りは能力低下ではないので、どの理由でもCFEの扱いは変えていない
（`cfe.ts` の SKIP-06 は従来どおり `!result.aborted` を見る）。
理由で分けたのは**設定ペース**と**疲労の裏付け**だけ。

### どこで聞くか

**run画面**: 予定より少ない本数で終えるときは、中止基準に引っかかったかどうかに関係なく聞く。
判定を `evaluation.verdict === "stop"` から `reps.length < progress.plannedReps` に変えた（穴1）。
理由を選ぶまで保存ボタンを押せない。空欄で保存できると「設定が高すぎた」として数えられてしまう。

**結果入力**: 本数が処方より少ないとき（`structure.slots.length` と比べる）。
一度選んだ理由は、本数の見え方が変わっても保存し直す——
run画面で終えた記録は `interval.reps` に**予定の**本数が入るので、
そこだけを条件にすると開き直して保存した瞬間に理由が消える。

選んだ理由が何に効いたかは、その場で `describeAbortCause` の一文として返す。
黙って扱いを変えない。

痛みを選んだ場合は故障ログ（部位・強さ）への導線を出す。
打ち切りの記録だけでは、どこがどれだけ痛いか分からないので次の判定に届かない。
既存の `activeInjuriesAt` に乗るので、新しいルールは足していない。

### ついでに直したもの

**入れ替えが通ったときに画面が無反応に見える**（`app/analysis/page.tsx`）。
「4週間のバランス」の入れ替えが**通った**予定は、もう候補ではなくなるので行ごと消える。
結果の一文は行の内側に出していたので、一緒に消えていた。
止まったときだけ文が残るので、**通ったときだけ何も言わない**状態だった。
消えた行のぶんはカード側に残すようにした。

E2Eは「行の中に結果が出るか」だけを見ていて、通った日には行が見つからず落ちていた
（どちらに転ぶかが日付で変わる）。通った側は「画面に出ること」と
「予定が実際に変わっていること」の両方を見るようにした。

### 検証

`tests/abortCause.test.ts` — 語彙のID重複、一覧に無い値を捨てること、
設定・疲労だけを数えること、**未入力は数えること**、痛みは負担としてだけ数えること、
理由を選べば設定どおりでも打ち切りになること、
数えなかったぶんも理由の文に出ること、**どの理由でもCFEが動かないこと**、
週次レビューに理由が出ること。

E2Eは、設定どおりの1本だけ入れて終える（＝中止基準には引っかからない）→
理由の選択が出ること → **理由が未入力では保存できないこと** →
天候を選ぶと「数えません」が出ること → 保存後に理由が残ることまで。

壊して確認済み: run画面が理由を送らないようにすると3件落ちる。
判定を `verdict === "stop"` に戻すと2件落ちる。
`countsTowardPaceEase` を常にtrueにするとユニットが8件落ちる。

---

## 本数の入力欄が押せなかった（forge-v99 / 2026-08-16）

結果入力の「本数」が、数字も見えず**タップしても入力できない**状態だった。

### 原因

ステッパー（`−` `入力欄` `＋`）を**3列のグリッドに入れていた**。

iPhone幅（390pt）でカードの中身は約330pt、3列なら1列あたり約105pt。
`−` と `＋` はタップ領域の下限として44ptずつ固定してあるので、隙間を含めて約96pt。
**入力欄に残るのは十数pt**しかない。数字は見えず、指も入らない。
おまけに `＋` が隣の列にはみ出していた。

### なぜ検査で見つからなかったか

**横はみ出し検査（`scrollWidth`）では捕まらない。**
潰れた欄はカードの枠の中に収まっているので、ページの横幅は増えない。
`＋` が隣の列に重なっても、ページ全体としてははみ出していない。

見た目に出ない不具合なので、実寸を測るしかなかった。

### 直したもの

**行を分けた。** 本数だけを1行（幅220ptまで）にし、距離と設定を2列にした。
コメントに「ステッパーを3列に入れない」と理由つきで残してある。

**`Stepper` 側にも下限を置いた。** 入力欄を `min-w-0` にしていたので、
狭い枠に置かれると無言でゼロまで潰れる。`minWidth: 56` にして、
狭すぎるときは**目に見えてはみ出す**ようにした。静かに壊れるより気づける。

### 検査

E2Eに実寸の測定を足した（390px幅・320px幅）。

- 値を打ち込む欄で幅22px未満のものがゼロであること（チェックボックスとラジオは16px角が正しいので除く）
- **本数の欄そのもの**が44pt以上あること
- 測る前に「ステッパーが画面に出ている」ことを確かめる。
  出ていないまま通すと、何も見ていない検査になる（最初これで素通りした）

壊して確認済み: 3列グリッドと `min-w-0` に戻すと、390px幅・320px幅の両方で
「本数の入力欄が狭すぎる（24px）」が出る。
なお24pxは一般の閾値（22px）より広いので、**一般の走査だけでは捕まらない**。
本数を名指しで測る検査のほうが効いている。

---

## シューズの推薦（forge-v105 / 2026-08-17）

登録した靴のうち、その日の練習に合うものを1足薦める。

### 持っていない靴は薦めない

候補は**登録済みの靴だけ**。市販モデルの一覧は持っていない。

作らなかった理由は、**性能値を裏取りできないから**。
「このモデルは反発4」と書けば推薦の根拠にはなるが、その4がどこから来たのかを
あとから確かめられない。読めなかったものを推測で埋めない、というここの規則に反する。

代わりに、登録時に選ぶ**種類**（スパイク・厚底・薄底・トレーニング・トレイル）を
出発点にした。種類ごとの既定値は `core/shoeProfile.ts` の `KIND_DEFAULTS` にある。
これは「スパイクは軽くて反発が高くクッションが薄い」といった、
種類の定義そのものから言えることだけを置いている。

### 手で直した値が必ず勝つ

既定値が実物と違うことはある（同じ「厚底」でも別物）。
そこで設定画面で項目ごとに1〜5で上書きできるようにし、
`profileOf()` は**上書きがあれば必ずそちらを使う**。既定値が上書きを打ち消すことはない。

上書きした項目は設定画面に印を出す。どれが自分で決めた値かが分からなくなると、
推薦がずれたときに原因を追えない。

### 理屈は1か所にしかない

`core/shoeRecommend.ts` の `recommendShoes()` だけが判断する。
練習詳細・記録画面・設定画面はすべてこれを呼ぶ。

画面ごとに書くと、同じ練習なのに画面によって薦める靴が変わる。
E2Eで**練習詳細と記録画面の1番目が一致すること**を検査しているのはこのため。
壊して確認済み: 記録画面が推薦順を無視して登録順に戻すと、
「練習詳細と記録画面で1番目が違う」で落ちる。

### 減点の理由

| 減点 | 理由 |
| --- | --- |
| 路面が合わない −8 | スパイクをロードに履くなど。走れはするが本来の用途ではない |
| ポイント以外でスパイク −20 | ジョグにスパイクは薦めない。故障の入口になる |
| 濡れているとき `(グリップ−3)×4` | 雨・濡れた路面ではグリップだけが効く |
| 疲労・痛みがあるとき スパイク −10 / 薄いクッション −6 | 脚が残っていないときに衝撃の大きい靴を薦めない |
| 走行距離が交換目安を超えている −5 | 薦めはするが、注意点として必ず出す |
| 用途が合っている +6 | 本人が「レース用」と決めた靴を、レース想定の練習で優先する |

同点のときは**登録順**で決める。同じ条件なら毎回同じ靴が出る
（LLMを使わないので、同じ入力からは必ず同じ結果が出る）。

### 実績が少ないことを隠さない

その靴での練習が3回（`RECOMMEND_MIN_SAMPLES`）に満たないうちは、
実績による補正をかけず、「まだ実績が足りません」と画面に出す。

黙って薦めると「使ってみて良かったから薦めている」と読める。
実際には種類の既定値から出しているだけなので、そう見せてはいけない。
E2Eでこの断り書きが出ることを検査している。

### 薦めた靴と違うものを選べる

推薦は並び順と `★` だけ。選択肢は絞らない。
薦めなかった靴を選んでも警告は出さないし、走行距離は**実際に選んだ靴**に加算される。

---

## ポイント練習前のアップ（forge-v106 / 2026-08-17）

アップを**主練習の子データ**として記録する。独立した練習にはしない。

目的はアップそのものを評価することではなく、
当日の準備状態・実際の総負荷・主練習との相性を見る材料にすること。

### 流す先と流さない先

| 流す | 流さない |
| --- | --- |
| 距離 | 週間の刺激回数 |
| 時間 | カテゴリ配分 |
| 負荷（ACWRの材料） | CFEの更新 |
| シューズの走行距離 | 進行段階の判定 |
| | 主練習のカテゴリ |

右側に流すと、**アップを記録しただけでポイント練習が1回増えたことになる**。
週2回のポイントが3回に見え、生成器が休養を挟む。
記録を細かく付けた週ほど練習が減るという、逆のことが起きる。

この境界は `tests/warmupAggregation.test.ts` が固定している。
壊して確認済み: アップを独立セッションとして保存する実装に変えると、
「カレンダーに独立したセッションを作らない」「その日のセッション数が増えない」が落ちる。
E2Eでも同じことを見ている（実装を壊すと「カレンダーのセッションが増えた（1 → 2）」で落ちる）。

### 二重計上をどう防いでいるか

FITを1ファイル丸ごと取り込むと、**主練習の距離に既にアップが入っている**。
そのまま足すと距離が倍になる。

`WarmupRecord.includedInMainTotals` がこの区別を持つ。合計を出すときは
`warmupAddedDistanceKm` / `warmupAddedDurationMin` を必ず通す（含まれていれば0を返す）。

どちらになるかは取り込み方で決まる。

| 取り込み方 | 主練習の距離 | アップ |
| --- | --- | --- |
| インターバル | 「メイン」の周だけ | 入っていない → 足す |
| 持続走（ファイル丸ごと1本） | ファイル全体 | **入っている → 足さない** |

`warmupFromFitLaps` はこの判断を引数として受け取る。呼び出し側に推測させない。
記録画面は自分がどちらのモードで入力しているかを知っているので、そこから渡す。

壊して確認済み: `includedInMainTotals` の分岐を外すと、
距離・シューズ距離・週間距離の3か所で「主練習側に含まれていれば足さない」が落ちる。

### アップの負荷に主練習のRPEを使わない

アップにRPEを入力させていない。入力を増やすと、答えないと記録できない仕組みになる。

代わりに区間の種別ごとの目安（`SEGMENT_RPE`）を置き、合計時間を距離の比で按分する。
イージージョグ3・ビルドアップ4・流し／加速走／短い刺激5。

**主練習のRPEを流用しない。** 流用すると、きつい主練習の日だけアップの負荷が跳ね上がり、
「アップの内容が変わった」のか「主練習がきつかった」のかを区別できなくなる。

### 相性の見方——3回に満たなければ結論を出さない

`WARMUP_MIN_SAMPLES = 3`。主練習のカテゴリ × アップの内容（ジョグのみ／流しまで／刺激まで）で
割ると、1つの組み合わせは月に数回にしかならない。

- 3回未満: **数字を出さない。** 件数と「傾向を出すには3回必要です」だけ
- 3回以上: 平均を出すが「暫定」と明示し、件数を必ず併記する
- どちらでも**因果としては書かない**

3にしたのは、2回だと「1勝1敗」しか起こらず何も言えないため。
アップの内容を3段階に畳んでいるのも、細かく分けるほど1つあたりが減って
いつまでも3回に届かないから。

**同じ日に睡眠・気温・前日の練習も違う。** アップだけを原因にはできないので、
表示の最後に必ずその断りを入れ、変えるかどうかは本人に残す。
自動でアップや主練習を変える経路は作っていない。

壊して確認済み: `WARMUP_MIN_SAMPLES` を1に下げると4件、
表示を「〜だから〜すべき」に変えると2件のテストが落ちる。

### 保存経路

アップ専用の保存口は作っていない。`/api/results` に結果と一緒に送る。
分けると、アップだけ保存されて主練習が保存されない状態が作れてしまう。

`/api/warmup` は**読むだけ**（前回と同じ・型・FITからの候補・分析）。

外から来た値は `normalizeWarmup` が必ず通る。知らない区間種別は捨てる——
「たぶんジョグ」として残すと、実際には流しを入れた日がジョグとして数えられる。

---

## 3秒で読める画面にする（forge-v107 / 2026-08-17）

利用者からの指摘は「メニュー名だけでなく設定タイムまで切れている」。

```
高乳酸　高乳酸セッシ…
400m×3 @400m 52.7〜53…
```

### 文字数で切るのをやめた

原文をCSSで切ると**前から残る**ので、一番見たい設定タイムが真っ先に消える。
「前から何文字」ではなく「何を残すか」を決める必要がある。

`core/prescriptionSummary.ts` の `prescriptionParts()` が処方を
**切ってよい部分と切ってはいけない部分に分ける**。

| 切らない | 切ってよい |
| --- | --- |
| 距離×本数（今日走る形） | 目的・注記 |
| 設定（今日出す数字） | セッション名 |
| レスト（つなぎ方） | |

画面は2段に組む。

```
高乳酸｜400m×3
52.7〜53.3秒｜r5分
```

**切らないと決めたなら、狭い幅では折り返す。** 横にはみ出させない。
設定は区間ごとに別の塊にしてある（複合を1本の文字列にすると折り返せない）。

形（`shape`）は**短いことを関数の側で保証している**。
`40分ジョグ （カレンダー反映テスト）` のような自作メニューの説明が混ざって
215pxの切れない塊になり、320px幅で32pxはみ出したことがある。
括弧の中は落とし、呼び名が長ければ時間だけにする。

壊して確認済み: 設定をCSSで切る実装に戻すと
「切ってはいけない部分が切れている」で落ちる（390px・320pxの両方）。

### 重複する名称を省く

「高乳酸」の行に「高乳酸セッション（300m）」を並べても情報が増えない。
距離は距離×本数のほうに出るので、括弧の中も重複している。

`isRedundantName()` はカテゴリ名を含む名前を省く。
**固定枠のチーム練習や自作メニューの名前は残す**——他に出るところが無い。

### 操作を畳んだ

✎と＋を常に出していたとき、1日に2本ある日はボタンが3つ並び、
**予定そのものが読める幅を奪っていた**。`⋯` を押すまで出さない。
長押しでも同じ編集シートが開くので、これは取りこぼし用の導線。

### 分析は結論 → 行動 → 根拠

「課題」「判定材料」「4週間の変更」「不足データ」が同じ強さで並んでいて、
どれが結論なのかが分からなかった。

最上部は3つだけ。

1. 最大の課題
2. 今週変えること
3. 現在のリスク

根拠（400m/1500mからの推定・変更理由・使った記録・信頼度）は畳む。
**開くまでDOMに出さない**——`<details>` で畳むと閉じていても箱が残り、
最下部がタブバーの裏に入る（E2EのP-4が捕まえた）。

並べ方と言い方は `core/analysisHeadline.ts` が決める。判定はしない
（制限因子は `limiter.ts`、配分は `coverage.ts`、負荷は `load.ts` が決めている）。

「今週変えること」を**回数で書く**のは、持っている値が回数だから。
割合（＋30%）に直すには基準を決める必要があり、その基準が画面に出ないと
あとで「なぜ30%なのか」を追えない。

リスクは「危険」と書かない。ACWRは補助指標で、これ単体で練習をやめる判断はしない。

足りていないデータは1つのカードにまとめた。
以前は「600m通過の材料があと2本」「接地時間なし」「最大心拍の基準なし」が
別々の大きなカードで、結論と同じ強さで目に入っていた。

### 固定の「＋」を削除

カレンダーの日付・分析の文章・下部ナビの上に重なっていた。
しかも機能が重複していた（ホームの「記録する」・各日の＋・記録画面の入力）。

行き先が孤立しないことは先に確かめた——`/past` はナビ・ホーム・設定から、
実測データは記録画面のタブから辿れる。

`--fab-clearance` も詰めた。**消し忘れると78pxの死んだ余白が残る。**
ただし0にすると最下部がタブバーに触れるので20px残してある（E2EのP-4が捕まえた）。

E2Eは「浮いている＋が戻っていないこと」と
「確認ボタンの上に position:fixed のものが無いこと」を見るようにした。
押せたことだけを見ると、画面幅でたまたま重なっていないときに素通りする。

### ホームは数字と結論を先に

```
ジョグ40分
4:42〜5:02/km｜RPE 3〜4
暑熱時：感覚優先
```

理由の文は畳むが**消さない**。「なぜこの設定なのか」はあとから必ず要る
（設定が下がったのか実力が落ちたのかを判別するために、当時の理由が残っている必要がある）。

壊して確認済み: 理由を最初から出す実装に変えると
「狙いと注意点が最初から出ている」で落ちる。

なおこの検査は**一度空振りした**。「注記があれば見る」だけにしていたので、
その日の処方に注記が無くて検査ごと飛んでいた。
検査したい状態は検査の側で作るようにした。

---

## 主練習の時間帯を選べるようにした（forge-v108 / 2026-08-17）

これまで主練習は**午後で固定**だった。授業やグラウンドの都合で
午前にポイント練習をやる日があるので、曜日（周期なら位置）ごとに選べるようにした。

### 枠の中身と時間帯を分けて持つ

保存の形は変えていない。

| 持っているもの | 意味 |
| --- | --- |
| `slots[dow]` | **主練習**の枠 |
| `amSlots[dow]` | **補助（2部）**の枠 |
| `mainTimeOfDay[dow]` | 主練習をどちらの時間帯に置くか（未設定は午後） |

**中身を移し替えない。** 時間帯を入れ替えるたびに `slots` と `amSlots` を
入れ替える実装にすると、入れ替えた瞬間にどちらが主練習だったのか分からなくなる。
振り替えるのは時間帯だけにして、「主練習は主練習の枠」を保つ。

名前が `amSlots` のままなのは、改名すると保存済みのデータが読めなくなるため。
意味は「午前の枠」ではなく「補助の枠」に変わったので、画面のラベルは
`補助・2部（午前）` のように**実際に入る時間帯を出す**。

### 既定は午後のまま

`mainTimeOfDayOf` の既定は `"pm"`。**既定を変えると、設定していない曜日の
予定が黙って動く。** 保存も既定は書き込まない（あとで既定を変えたときに、
古い設定が既定を上書きし続けるのを避ける）。

### 補助枠は主練習の反対側

以前は補助枠を「午前」に固定していた。主練習を午前にすると同じ時間帯に2本入り、
セッションidが衝突して**片方が消える**。`subTimeOfDay` で必ず反対側に置く。

### 検証の文も主/補助で言う

「午後が休養なのに午前枠が入っています」のように時間帯を固定で書いていたので、
午前に主練習を置いた日は警告が逆さまになっていた。
主練習・補助という言い方にし、時間帯は `TIME_OF_DAY_LABELS` から出す。

壊して確認済み:

- 設定を無視して午後固定に戻すと「午前を選ぶと主練習が午前に入る」が落ちる
- 補助枠を午前固定に戻すと「同じ時間帯に2本入れない」「片方消えない」が落ちる
- 既定も保存するようにすると「既定は書き込まない」が落ちる
- 画面が保存しないようにすると「保存されていない」が落ちる（E2E）

---

## 靴の用途を複数選べるようにした（forge-v109 / 2026-08-17）

1つしか選べなかったとき、厚底のように**レースにもポイント練習にも履く靴**を
表せなかった。どちらかを選ぶと、選ばなかったほうの練習で加点されない。

### 古いデータを読めなくしない

`ShoeProfile.purpose`（単数）を `purposes`（配列）に変えたが、
端末には単数で保存された靴が入っている。

`readPurposes()` は次の順で読む。

1. `purposes` に有効な値があればそれ
2. 無ければ `purpose`（単数）を1件の配列として読む
3. どちらも無ければ種類の既定

**読めなくすると、本人が設定した用途が黙って既定に戻る。**
`isOverridden("purposes")` も単数のデータを「本人が決めた」として扱う
（そうしないと、設定済みの靴が画面で「既定」と表示される）。

### 「決めていない」は単独

`normalizePurposes()` が `any` を含む選択を `["any"]` に潰す。
併用できると「決めていないが、レース用でもある」という読めない設定が保存できる。

### 加点は1回だけ

用途が2つ噛み合っても加点は +6 のまま。

**選ぶほど有利にしない。** 数だけ加点すると、用途を多く付けた靴が
どの練習でも上に来る。用途は「その練習に向いているか」の合図であって、
多いほど良い靴という意味ではない。

逆に、全部が噛み合うことを求めるのも違う（選択を増やすほど不利になる）。
**1つでも噛み合えば加点、ただし1回だけ**にした。

壊して確認済み:

- 古い単数データを読まないようにすると「単数で保存されたものを1件の配列として読む」が落ちる
- `any` を併用可にすると「「決めていない」は単独にする」が落ちる
- 噛み合った数だけ加点すると「加点は1回だけ」が落ちる
- 画面が1つしか保存しないようにすると「2つ選んでも両方保存されない」で落ちる（E2E）

---

## アップ欄の入力を直す（forge-v110 / 2026-08-18）

利用者からの指摘4件。

### 1. 押せるのか分からない

文字が並んでいるだけだった。枠・背景・開閉のしるし（▸／▾）を付け、
`aria-expanded` も持たせた。中身が無いときは「タップして入力」と出す。

### 2. `2.199999999` になる

`2.3 - 0.1` は二進小数で `2.1999999999999997`。
`stepperNext()` が**刻みの桁で丸める**（0.1刻みなら1桁）。

**表示だけ丸めない。** 表示を丸めても保存される値は端数のままで、
画面の 2.2 と保存された 2.1999… が食い違う。値そのものを丸める。

ステッパーは全画面で使うので、関数は `core/inputFormat.ts` に置いてテストした
（`.tsx` の中では vitest が読めない）。

### 3. 小数点を打てない

iOSは `inputMode="decimal"` だけだと**小数点キーの無いテンキー**を出すことがある。
`pattern="[0-9]*[.,]?[0-9]*"` を添えた。

### 4. ペースから合計を計算する

区間に**ペース**（分:秒/km）を持たせた。距離×本数×ペースで時間が出るので、
合計時間を手で計算して入れる必要がなくなる。

**ペースの無い区間は時間に入れない。** 「だいたい5分/km」で埋めると、
実際より速い/遅い時間が合計に混ざる。何区間ぶん出せなかったかを画面に出す。

**手で入れた合計を計算で上書きしない。** 合計欄を直したら
「手で入れた合計を使っています」に切り替わり、そこから先は計算で触らない。
戻したいときは「区間から計算し直す」を押す。
どちらの値を使っているかを常に画面に出しておくのは、
あとで合わないときにどちらを疑えばいいか分かるようにするため。

壊して確認済み（1回のE2Eで3件まとめて）:

- 丸めをやめる → `−を押すと桁が増える（2.9999999999999996）`
- `pattern` を外す → `小数点キーが出る指定が無い`
- ペースを合計に反映しない → `ペースから合計時間が計算されていない（25分）`

なおこのE2Eは**一度、実装が正しいのに落ちた**。
合計を手で触ってから（＝計算をやめる状態にしてから）ペースを入れていたため。
検査の順番が仕様と噛み合っていないと、正しい実装を落とす。

---

## 予定と違う量をやった日を一目で分かるようにする（forge-v111 / 2026-08-18）

「30分ジョグの予定を50分にしたら、カレンダーで一目で分かるようにしたい」という指摘。

### なぜ出ていなかったか

`actualDiffersFromPlan` が**種目の食い違いしか見ていなかった**。
ジョグの予定でジョグをやれば種目は同じなので、量が倍でも何も出ず、
カレンダー上は予定どおりに見えていた。

### 何を「違う」と言うか

割合と絶対値の**両方**を満たしたときだけ。

| 見るもの | 割合 | 最低の差 |
| --- | --- | --- |
| 持続走の時間 | 15% | 5分 |
| 持続走の距離 | 15% | 1km |
| インターバルの距離 | — | 1mでも違えば |

割合だけだと `10分 → 12分`（20%）が引っかかり、
絶対値だけだと `90分 → 95分` が引っかかる。どちらも普通の揺れで、
**印が付いていること自体が情報でなくなる**。

インターバルの**本数**は数えない。減った場合は「中止 2/4本」の印で既に出していて、
同じことを2つの印で言うと、どちらを見ればいいのか分からなくなる。

### 予定と実際を並べて出す

以前は実際だけを出していたので、**何から変わったのか**が読めなかった。

```
有酸素 ジョグ
30分 → 10km 50分
```

予定側には取り消し線を付ける（色だけに頼らない）。

**予定データは書き換えない。** 表示を補うだけで、予定は予定として残す。
書き換えると「もともと何を予定していたか」が失われ、
設定が下がったのか本人が変えたのかを後から追えなくなる。

壊して確認済み:

- 量の食い違いを見ない実装に戻すと、テスト4件が落ちる
- 予定側を出さないようにすると `予定側が出ていない（何から変わったのか読めない）` で落ちる（E2E）

---

## 神経系の基準を400mPBに移す（forge-v112 / 2026-08-18）

`GRP_RATIOS.neural`（目標800mの 0.88〜0.92 倍）で流しの設定タイムを出していた。
2つ問題があった。

### 1. 流しが全力だった

目標1:48.9 で 150m 18.0〜18.8秒。
**400mPB 49.0 の150m通過は18.4秒**なので、400mレースペースそのものを
「流し」として処方していた。流しは動きを作るもので、全力で走るものではない。

### 2. 目標を速くすると流しも速くなる逆転

流しは**今の走力**に紐づくべきもので、目標に紐づくものではない。
目標を1:47に書き換えただけで流しが速くなるのは、根拠の無い変化だった。

### 直したもの

`neuralPace()` を新設し、基準を **400mPB** に移した。

| 用途 | 距離 | 400mペースに対する比 |
| --- | --- | --- |
| stride（流し） | 80〜150m | 1.05〜1.12（400mペースより遅い） |
| rep（レペ） | 150〜300m | 0.98〜1.05（400mペース前後） |

400mPB 49.0 での設定:

| | 設定タイム |
| --- | --- |
| 流し 150m | 19.3〜20.6秒 |
| 流し 100m | 12.9〜13.7秒 |
| レペ 200m | 24.0〜25.7秒 |
| レペ 300m | 36.0〜38.6秒 |

**400mPBが無いときだけ**これまでどおり目標800m基準に落とし、`isEstimated` を立てる。

レース直前の流しは 100m → 80m に詰めた（動きを作るだけで疲労を残さない）。
坂ダッシュはタイム設定を持たないので変えていない。

**"rep" を使う処方は現状ひとつも無い**（`RECIPE_CATALOG` に `neural` の節が無く、
神経系でタイムを持つのは流しだけ）。神経系のレペを処方に足すときのために
用途として定義してある。

CFE・ガードレール・暑熱補正・実測経路（`estimateLtFromMarkers`）には触れていない。

---

## LT推定のフォールバックを段階式にする（forge-v112 / 2026-08-18）

実測が足りないとき、LTは800m能力から一段で出していた。

```
LT = (CFE800 / 0.8) * 1.6
```

CFE 1:49.5 で **LT 3:39/km**。ところが同じ選手の実測経路（`ltOffsetFor`）は
**3:16/km** を返す。**同じソフトの中で答えが23秒/km食い違っていた。**

しかもCVはCFEを更新しないので、このずれは画面のどこにも出ない。
実測を入れた瞬間に閾値走の設定が23秒/km速くなり、
「設定が下がったのか実力が上がったのか」を判別できない状態だった。

### 直したもの

`ltFallback()` を新設し、**近い種目から順に落とす**段階式にした。

| 段 | 条件 | 式 | 出どころの表示 |
| --- | --- | --- | --- |
| 1 | 1500mPBがある | `(pb1500 / 1.5) * 1.24` | 「1500mPBからの推定値」 |
| 2 | 無ければ | `(CFE800 / 0.8) * 1.42` | 「CFEからの推定値」 |

**採用した段を必ず `sourceDescription` に書く。** どの段も推定なので
`isEstimated` は立てたまま、`confidence` は `low` のまま。

係数は**3経路が同じ答えに寄るように**取った。揃わない係数は使わない。

| 経路 | 入力 | LT |
| --- | --- | --- |
| 段1（1500m） | 3:56 | 195秒/km |
| 段2（CFE） | 1:49.5 | 194秒/km |
| 実測 | 5000m 15:22 | 196秒/km |

この3つが5秒/km以内に収まることをテストで固定した（`tests/ltFallback.test.ts`）。

### 3000m・5000mのPBを段に入れなかった理由

あれは `FitnessMarker` として日付つきで入れるもので、実測経路が先に拾う
（`types.ts` の `Athlete` のコメント）。静的なPBとして二重に持つと
LTの出どころが2つになり、どちらを信じるのか分からなくなる。

### 壊して確認したこと

- 係数を1.6に戻すと7件落ちる
- 段1を無効にすると「1500mPBからの推定値」を見ている検査が落ちる

CFE・ガードレール・暑熱補正・実測経路（`estimateLtFromMarkers`）には触れていない。
既存 `SessionResult` の再計算も行っていない。

---

## 効くのに効くと書いていない欄をなくす（forge-v112 / 2026-08-18）

身長・骨格筋量・3000m/5000m には「記録用」「有酸素マーカーへ」と書いてある。
**入力できるのに何にも効かない欄は「効いているはず」と読まれる**ため。

その逆が抜けていた。400mPBと1500mPBは forge-v112 で実際に処方を動かすように
なったが、画面には何も書いていない。**効くのに効くと書いていないと、
空欄のまま放置しても気づけない。**

`/setup` の欄そのものに注記を付けた（本文のどこかにあるだけでは、
どの欄の話なのか読めない）。

| 欄 | 注記 |
| --- | --- |
| 400m PB | 流しの設定タイムの基準。空だと目標800mから推定します |
| 1500m PB | 実測が無いときのLT推定に使います。空だとCFEから推定します |

**空欄のときどうなるかまで書く。** そこが読めないと、入れるかどうかを判断できない。

処方の中身は変えていない。表示だけ。

### 壊して確認したこと

- `data-field-note` を消すと「注記が付いていない」で2件落ちる
- 注記から効き先（流し／LT）を消すと「効き先を書いていない」で落ちる
- 「空だと」を消すと「空欄のときどうなるかを書いていない」で2件落ちる

---

## 処方の文章が形の欄に入って行が崩れる（forge-v113 / 2026-08-18）

カレンダーで行が横にはみ出しているという指摘。

`prescriptionParts` の `shape` は、画面が**縮めずに置く**場所（切ってはいけないと決めた
距離×本数がここ）。ところが「設定が無い処方」の分岐が、**本文をまるごと** `shape` に
入れていた。回復ジョグの処方は

```
20〜30分・会話可能・RPE 2以下。痛みが増す、走動作が変わる場合は中止して完全休養。
```

という文章なので、切れない長い塊になる。

### 見えていたより広い範囲が壊れていた

**2部練習の日は1枚のカードに午前と午後が入る。**
午前の回復ジョグの文章がカードの中身を押し広げ、
同じカードの午後（`300m×6 r4分 jog`）まで画面外へ押し出していた。
崩れていたのは指摘された行だけではなかった。

### 直したもの

`shape` に置いてよいのは形だけ、と作りで保証する。

| 入力 | shape |
| --- | --- |
| `休養` / `補強` / `チーム練習` | そのまま（短い） |
| `20〜30分・会話可能・RPE 2以下。…` | `20〜30分`（先頭の量だけ） |
| `回復促進と有酸素土台の維持。設定ペースは…` | 無し（原文を切ってよい場所に出す） |

句読点があれば文章。無くても14文字を超えれば形ではない
（14文字は、いちばん長い実物 `500m×3＋300m×2` が入る幅）。

**文章だからと丸ごと捨ててはいけない。** 先頭の `20〜30分` は量そのもので、
捨てると予定と実際を並べる行から予定側が消える（実際に消えて、E2Eが落ちた）。

### 検査が捕まえられていなかった理由

ページ全体の `scrollWidth` を見る既存の検査では捕まらない。
カードの中で隠れるだけで、文書は広がらないため。
要素ごとの `scrollWidth > clientWidth` でも捕まらない——
`flex-shrink-0` の要素は**中では切れておらず、外へ伸びている**。

要素の右端が画面の外に出ていないかを直接測る検査を足した。

### 壊して確認したこと

- `shape` に本文をそのまま入れる実装に戻すと、新しい検査が390px幅と320px幅で落ちる
- 落ちる要素は `300m×6 r4分 jog`（押し出された側）。押し出した側だけを見ていては気づけない
- 先頭の量を取り出さないと「予定側が出ていない」で落ちる（E2E）
