# FORGE バックログ

**このファイルは未完了のものだけを置く。** 終わったら消して `CHANGELOG.md` に移す。

未実装かどうかを機械で判定するのは難しい（コードがあっても繋がっていないことがある）。
**だから「何が残っているか」はこのファイルを唯一の正とする。** 他の文書に一覧を書き写さない。

| 文書 | 役割 |
| --- | --- |
| `README.md` | なぜその設計にしたか |
| `HANDOFF.md` | いまどうなっているか |
| `BACKLOG.md` | 何が残っているか（このファイル） |
| `CHANGELOG.md` | 何をやってきたか |

---

## 進め方

上から順に進める。1つ終えるごとに `npm run verify` を通し、この行を消す。
飛ばす場合は理由を残す。

**既存の数値ロジック（CFEの係数・閾値）は、検証結果が出るまで変更しない。**
凍結するのは係数であって、コードの整理まで止める意味ではない。

---

## A. 検証の土台

### A-1. カバレッジの空白を埋める ← 進行中

計測は入った（`npm run test:coverage` → `npm run coverage:report`）。
閾値は現在値を下限にしてあるので、下がれば落ちる。**上げるのはこれから。**

未検証の分岐が多い順（測定値。数字は `coverage:report` が出す）:

| ファイル | 見るべき理由 |
| --- | --- |
| `src/lib/service/workflow.ts` | 未検証の分岐が突出して多い。B-1（分割）と合わせて進める |
| ~~`src/lib/core/weekTemplate.ts`~~ | 済（`tests/cycleWeekTemplate.test.ts`） |
| ~~`src/lib/core/split600.ts`~~ | 済（`tests/split600.test.ts`） |
| `src/lib/core/weeklyReview.ts` | 文章生成の条件分岐が薄い |
| `src/lib/core/backfill.ts` | 遡り入力。欠損データの扱いが分岐の塊 |

埋めたぶんだけ `scripts/ci/coverage-thresholds.json` を手で上げる。
**自動で現在値に追従させない**（一時的な低下まで基準になる）。

#### どこが空いているか（`npm run coverage:gaps`）

```bash
npm run test:coverage && npm run coverage:gaps
```

引数でファイルを変えられる（`npm run coverage:gaps core/backfill.ts`）。
**「194本ある」だけでは手が付かない。** 関数ごとにまとめると順番が決まる。

`workflow.ts` の内訳（測定時点）。上から埋めると効く順ではなく、
**壊れたときの実害が大きい順**に並べ直してある:

| 関数 | 未検証 | なぜ効くか |
| --- | --- | --- |
| ~~`processResultCore`~~ | 済 | `tests/processResultCore.test.ts` |
| ~~`regeneratePlanCore`~~ | 済 | `tests/regeneratePlanCore.test.ts` |
| `hasRecentLoadConcern` | 7 | 疲労の裏付け。**非公開なので `buildRuleContext` 経由で叩く** |
| `processSkip` | 4 | スキップの判定 |
| `resultAudit` | 4 | 記録の点検 |
| `dashboard` | 11 | 表示のみ。落としても数値は狂わない |
| `assistantContext` | 16 | 相談に渡す文の組み立て。表示のみ |

**`dashboard` と `assistantContext` は数が多いが後回しでよい。**
数を減らしたいだけなら先に埋まるが、守りたいのは数字ではない。

### A-2b. 「出力が出すぎた」を設定を上げる材料にするか（データ待ち・約束）

**本人からの要望。忘れないためにここに置く。**

調子が良くて設定より速く入り、狙った刺激から外れて止めた——これは
「設定が低すぎた」ことを示している。いまは記録するだけで、
**設定を上げる材料にしていない**。

上げなかった理由:

- 1回の打ち切りで設定が上がる仕組みは危ない（速く走れた日は誰にでもある）
- 締める側の判定（`executionTrend` の tighten）に材料を足すのは**係数の変更**にあたる。
  A-2 の検証が済むまで数値ロジックは動かさないと決めてある

**数える場所はアプリの中（分析→現在地の「途中でやめた練習」）。**
定期実行では数えられない——本物の記録は端末のIndexedDBにあり、
PC側のSQLiteは開発用のダミーだから。

**着手の条件（これを満たしたら検討する）:**

```bash
npm run test:coverage   # ではなく、記録から数える
```

- 「出力が出すぎた」の打ち切りが**3回以上**たまっている
  （達したら分析画面がその場で知らせる。`TOO_FAST_REVIEW_COUNT`）
- かつ A-2（実行可能率）の測定が済んでいて、
  同じカテゴリの実測が全体としても速い側に寄っていること

**片方だけでは動かさない。** 打ち切りが3回あっても実測が設定どおりなら、
たまたま調子が良かった日が3回あっただけかもしれない。

判断は `src/lib/core/abortCause.ts` の `countsTowardPaceEase` の直上に
書いてあるコメントと対で読むこと。

### A-2. 設定ペースの実行可能率を測る

**レースより先にこちらをやる。サンプルが桁違いに多い。**

インターバルは毎回「設定タイム＝予測」と「実測＝答え合わせ」の組になっている。
レースは年に数本だが、これは何百本もある。

測るもの:

- 設定に対する乖離の分布（カテゴリ別・フェーズ別）
- 実行可能率（設定どおりに回った割合）
- 暑熱・雨・睡眠不足で乖離がどう変わるか
- 打ち切りの理由別の内訳（forge-v98 から記録している）
- **「出力が出すぎた」が何回あったか**（A-2b の着手条件。数えたらそちらへ戻る）

**係数を決めるためではなく、いまの設定が現実的かを見るため。**
同じデータで係数調整と精度評価を同時にやらない。

### A-2c. カテゴリ内の距離と設定ペースの関係を検証する

`specificPace` は、同じカテゴリ内なら距離によらず同じGRP比率のペースを処方している。
この距離非依存モデルが、150〜800mの反復走で妥当かは未検証。

直すなら、処方側の `specificPace` と実測の能力換算側を**必ず同時に**変える。
換算側だけに距離補正を入れると、設定どおり走っただけでCFEが動き、
CFEが「どの距離を練習したか」の関数になる。

Riegel指数1.06はこの距離帯を対象にした根拠ではない。標準距離や指数を結論に合わせて
選ばず、カテゴリ・距離・レストを揃えた本人の実測から先に検証する。
検証中はCFEや設定へ反映しないシャドー計算に留める。

検証手順:

1. 1セッションごとに、カテゴリ、レップ距離、レスト（時間と方法）、予定本数と完遂本数、
   セッションRPE、各レップの実測タイム、日付、気温を集める。当時の設定タイムとCFEも、
   実測が設定からどちらへ外れたかを比較する基準として保存する。
2. RECIPE_CATALOGで実際に使う同一カテゴリの2距離を1組とし、カテゴリ・本数・RPE・
   レスト条件が揃い、距離だけが違う実測を突き合わせる。RPEまたはレストが揃わない組は
   使わず、日付と気温は暑熱や時期の偏りを分けて確認する。
3. 距離ごとの「実測ペース÷現行設定ペース」とレップ内の低下幅を比較する。最初の5組を
   探索用にし、距離による偏りが同じ方向に再現するかを見る。5組なら、無関係な上下が
   偶然すべて同方向になる確率は単純な符号比較で1/32であり、1〜2回の好不調だけで
   方向を決めるよりは堅い。ただし、この5組だけで係数採用は決めない。
4. 探索用データを見てから候補式・標準距離・指数を決めて固定し、その後に集めた独立した
   3組で検証する。したがって判断に必要なのは、1カテゴリ・1距離組につき各距離8件、
   合計16セッション（探索5組＋検証3組）。この件数に届かない間はBを維持する。
5. シャドー計算は将来 `scripts/analysis/interval-distance-shadow.mjs` に置き、書き出しJSONを
   読み取り専用で受け、標準出力へ比較表を出す。`src/`、`app/`、Store、サービス層からは
   呼ばず、CFE保存・予定生成・設定更新のAPIを持たせない。入力JSONの前後ハッシュ一致と、
   `src/` / `app/` / `pwa/` からこのスクリプトへのimportが無いことを検査し、シャドー値が
   CFEや設定へ流れないことを保証する。

Bを見直すのは、必要件数を満たしたうえで、固定後の検証3組でも距離に沿った偏りの方向が
再現し、候補式が現行式より外部検証の絶対誤差を一貫して小さくし、かつ処方と逆換算を
同時に使った不変条件（設定どおりなら距離によらず implied800 = CFE）が保たれた場合だけ。
期待する制限因子が出るかは採否条件にしない。

### A-3. CFEのバックテスト（レース本数を数えてから判断）

過去の800mレースについて、そのレースより前のデータだけで当時のCFEを再現する。

**先に数えること。レースが8本未満なら、いまやっても結論は出ない。**
walk-forward検証の評価点が3〜5個では、CFEと単純モデルの差より
レースごとのばらつきのほうが大きい。

数が足りたら、単純モデルと比べる:

- 直近800mレースをそのまま使う
- PB＋1.5秒
- 現行CFE

**目的は係数の最適化ではなく、現行CFEが単純モデルに負けていないかの確認。**

評価: レース7日前・前日のCFE / 実記録 / 誤差 / MAE / 速め遅めの偏り /
±1秒以内率 / ±2秒以内率 / サンプル数 / 暑熱・雨・故障・体調。

**±1.5秒クリップは予測係数ではなく安全装置。精度だけを理由に外さない。**

未記録期間の +0.4秒/週 は、CFEを悪化させる方式と信頼度だけを下げる方式を比べる。
「FORGEに記録が無い」と「実際に練習していない」は別物。

### A-4. 見た目と操作の回帰テスト

**いまある検査は、壊れてから足したものばかり。**
forge-v99 の「本数の欄が押せない」は、横はみ出し検査を素通りした
（潰れた欄は枠の中に収まるので `scrollWidth` が増えない）。
実寸の測定はそのとき足したが、次に別の形で潰れたら同じことになる。

スクリーンショットの基準画像との比較（`pixelmatch`）を常設する。
日付・アニメーション・データを固定しないと毎回差分が出る。

---

## B. 保守性

### B-1. サービス層の分割 ← 着手済み（続き）

**当初の8ファイル案は成り立たなかった。** 参照を数えたら
`plan` と `result` が閉じた輪になっていた。

```
結果 → 予定  processResult が regeneratePlan を呼ぶ（保存したら作り直す）
予定 → 結果  FIT取込・セッション実行が processResult を呼ぶ
```

これは乱れではなく、このアプリの中心そのもの
（結果入力 → CFE → 再計算 → 波及 → ルール再検証）。
**輪の途中で切ると、片方向にするための間に合わせの層が要る。**
そこまでして分けるものではないので、予定と結果は1つに残す。

代わりに**呼ばれるだけで誰からも参照されない端**から外へ出す。

いまの形:

```
src/lib/service/
├── index.ts     入口。再exportだけ（呼ぶ側は @/lib/service のまま）
├── run.ts       M-4 セッション中の入力
├── adaptive.ts  M-2/M-3/M-9 適応的な処方
├── health.ts    Apple Health の取り込み（何にも依存しない端）
└── workflow.ts  予定と結果の輪。ここから順に端を剥がす
```

向きは `npm run ci:layers` が見張っている。下から上への import で落ちる。
（循環しても ESM の巻き上げで**動いてしまう**ので、動作では気づけない）

**片方向で切れる端は出し切った。** 残りはすべて逆向きの参照がある。

| 残り | 逆向きの参照 |
| --- | --- |
| FIT取込 | `trustedResults` `lastRecordedDate` が広く使われている |
| 一括入力 | `listPhrases` `savePhrase` を他から呼んでいる |
| M-5 予定の編集 | `editSession` `PlanEditResult` を他から呼んでいる |
| M-7〜M-12 分析・書き出し | 6件 |

**次は共通の下請けを下層へ降ろす。** `trustedResults` `buildRuleContext`
`heatFlaggedDates` `lastRecordedDate` `isOwnedByAthlete` あたりを
`shared.ts`（層0）に出せば、上の4つが順に切れるようになる。

**移動のときは中身を変えない。** 各段階で typecheck・test・build・E2E を通す。

### B-2. `app/results/page.tsx` の状態管理 ← いまここ（着手済み）

分割の前に、壊れると困る2点をE2Eで固定した（壊して落ちることまで確認済み）。

- 隠れているモードの値が保存に混ざらない
- モードを切り替えても入力が消えない

**この2つは逆向きの要求で、同時に満たす必要がある。**
「混ぜない」を state を消して実現すると後者が壊れ、
「消さない」を保存にも渡すと前者が壊れる。片方だけの検査だと、
もう片方を壊す直し方が通ってしまう。

そのうえで最初の2つを切り出した（中身は変えていない）。

| 部品 | 中身 |
| --- | --- |
| `SubjectiveFields` | RPE・主観・翌日の脚・途中でやめた理由 |
| `ConditionFields` | 天候・路面・シューズ |
| `RestFields` | レスト内容・指定方法・値 |
| `checkResultDraft` | 保存させるかどうかの判定（`src/lib/core/resultDraft.ts`） |
| `buildContinuousPayload` / `buildIntervalPayload` | 保存する中身の組み立て（`src/lib/core/resultPayload.ts`） |

**どちらもモジュール直下に置いた。** 関数の中で関数コンポーネントを定義すると
再描画のたびに別の関数になり、React が中身の input を作り直す。
入力中にフォーカスが外れ、iOSでは1文字ごとにキーボードが閉じる。
画面には何も出ないので気づけない。

入れ子のコンポーネントは `npm run ci:nested` が禁じている。
E2Eに欄を1つずつ足していくより、**形のほうを禁じた**ほうが
欄が増えても勝手に守られる（N-1 は `設定(秒)` 1つしか見ていない）。

判定は core へ出したので単体テストが書けるようになった
（それまで画面の中にあり、E2Eで1経路ずつ叩くしかなかった）。

組み立ても core へ出した。**モードごとに引数の型が別**なので、
表示していないほうの値は渡しようがない。入力欄の state は残したまま
（切り替えで打ち直しにならないように）、混ざるのは型で止まる。

**残り: `useState` の整理。** 73個あるのは変わっていない。
ここまでは欄の置き場所と、判定・組み立ての切り出し。
状態の持ち方（`useResultDraft`）はまだ手つかず。
組み立てが純関数になったので、次は
「どの state がどの draft に入るか」が型から追える状態になっている。


### B-3. `pwa/e2e.mjs` の分割

5651行。**本番コードの整理が済んでから。**

初期設定 / プラン生成 / 記録 / FIT取込 / 同期 / バックアップ・復元 /
冬季・N日周期 / 天候・シューズ / PWA更新 / オフライン。

**検証内容を削らない。** 共通化するのは fixture と assertion だけ。

### B-4. `any` の削減とESLint

現在71件で、CIの上限も71件。増加は止まるが、減らす圧力が無い。

- main との差分で、新規・変更行の `any` 増加を禁止する
- 全体件数は上限以下を維持
- 減らすたびに上限を手で下げる（71 → 65 → 55）
- `no-explicit-any` は最初 warning、新規コードは error
- 外部データは `unknown` ＋検証
- B-1 の分割に合わせて段階的に型を付ける

**現在値へ自動追従して上限を書き換える方式は使わない**（一時的な増加まで基準になる）。

---

## C. 画面

### C-1. カレンダー・分析の情報密度 ← 着手済み

済んだもの:

- **グレー文字のコントラスト** — `--text-3` は面に対して4.00しか無く、
  小さい文字の基準4.5を下回っていた（しかも10.5pxの処方文に使っていた）。
  色味は変えず明度だけ上げて4.61にした
- **メニュー本文の省略方法** — 原文をCSSで切ると `@300m 41.2〜41.6秒` の手前で
  切れて**設定タイムが真っ先に消えていた**。距離×本数と設定だけに組み直す
  （`core/prescriptionSummary.ts`）。読めない形なら原文を出す
- **長い根拠文の折りたたみ** — 制限因子と600m通過の根拠を畳んだ。
  材料不足のときの文は畳まない（あれは初めて見る内容だから）
- **主/補助の区別** — 2部練習の日に「午前」を出し、午前が先に並ぶようにした。
  並べ替えていなかったので、保存順によっては**午後の本練習が先**に来ていた。
  午後には印を付けない（全部に付けると印であること自体が情報でなくなる）

残り:

複数セッションの日に名称と処方が切れる。分析は説明文が長く、練習前に読めない。

- 複数セッション日の表示切れ
- メニュー本文の省略方法
- 長い根拠文の折りたたみ（結論と根拠を階層に）
- グレー文字のコントラスト
- 縦スクロール量
- 練習前に要る情報を先に出す

**機能は減らさない。最初に出す量だけ減らす。**
B-2 とはファイルも問題も違うので、同じ変更にまとめない。

---

## D. 実機・外部

### D-1. 実機での起動性能の計測

`bundle.js` は非圧縮 540KB / gzip 約167KB。
**サイズだけで遅いとは言えない。先に実機iPhoneで測る。**

初回起動 / 2回目以降 / TODAY表示まで / 操作可能まで / オフライン起動 /
Service Worker初回インストール時の通信量。

実測で問題が出た場合だけ、分析の遅延読込・Supabaseの遅延読込・
FIT解析の分離・ホームで不要なロジックの除外・プリキャッシュ対象の見直しを検討する。

### D-2. 実機iPhone受入試験

自動テストでは確認できないもの。

| # | 確認すること |
| --- | --- |
| 1 | Safari とホーム画面PWAの両方で起動できる |
| 2 | Googleサインイン後に同期画面へ戻る（PC / iPhone 両方） |
| 3 | PWAとSafariで認証状態・保存領域が別であること |
| 4 | オフラインで起動し、記録できる |
| 5 | 更新を受け取ったあともデータが残る |
| 6 | ホーム画面アイコンが新しくなる（**削除して追加し直す必要がある**） |
| 7 | キーボード表示中に保存ボタンが隠れない／1文字ごとに閉じない |
| 8 | Safe Area（ノッチ・ホームインジケータ）と重ならない |
| 9 | FIT / Apple Health の取込が実用的な時間で終わる |

> iOSのホーム画面アイコンは追加時に焼き付けられるので、配信を差し替えても変わらない。
> 削除→再追加が必要で、その際にストレージが分離される（先にバックアップを取る）。

### D-3. 外部設定

リポジトリ外の操作。**キーを担当者に渡す必要はない。値は本人の端末にだけ入る。**
`service_role` キーは絶対にクライアントへ置かない。

| 項目 | 状態 |
| --- | --- |
| Supabase プロジェクト / `forge` bucket / Storage RLS | 済（2026-07-31 に本人と確認。uid別パスで分離） |
| Google OAuth・Redirect URL | 済 |
| テーブル側のRLS（SELECT/INSERT/UPDATE/DELETE を本人に限定） | 未 |

---

## E. 保留（判断待ち）

### E-1. バックアップにFIT元ファイルが全件入る

同期スナップショットとバックアップに元ファイル（base64）が全件入るため、
取込が増えるほどサイズが際限なく膨らむ。

**正しさの問題ではない。** 保持期間か間引きの方針を決める必要がある。

### E-2. Apple Health の大容量対応

XML書き出しの手動取込は動く。ZIP対応・進捗・キャンセル・重複防止は未着手。
HealthKitはWebから読めないので、自動化するならショートカット → Supabase → アプリ。

### E-3. Codex が追加したロジックの未レビュー

セッション形式の複数候補化、`preferred`/`fixed` の曜日、`trainingClassification.ts`。
数値定数が変更されていないことだけ確認済み（`227c972` と `6424518` の差分）。

### E-4. 選手タイプと制限因子の役割を揃える（重大度: 中）

事実。現在のPBでは `diagnose()` は `lactate_tolerant`・最大の伸びしろ「前半経済性」、
`assessLimiter()` は `endurance`・「後半の維持」と判定する。前者の `athleteType` は
同一カテゴリ内の候補形式の加点、後者はカテゴリ枠の振り替えとセッション案の推奨に使われ、
両方が同時に生成へ効く。診断結果と制限因子は画面にも出るが、CFE更新には使われない。

元の「どちらを正とするか」は不正確。2つは分類軸が違うため、片方を正として消すのではなく、
形式選択とカテゴリ配分それぞれの責務を定義し、同時適用した結果が競技的に整合するかを検証する。

### E-5. 回復間隔の役割差を明文化する（重大度: 低）

事実。`readiness.ts` は高負荷の中1日を準備度から減点する一方、実予定に対するRULE-03は
連日または同日だけを違反として返すため、中1日は違反なしになる。ただし曜日・周期テンプレートの
事前検査は中1日をWARNにする。準備度は当日の余裕、RULE-03は配置違反、テンプレートWARNは
境界上の注意を扱っており、同じ結論に一本化すべきものとは確認できない。

元の「同じ実装へ集める」は訂正する。共通化するなら日数の数え方と用語だけに留めるか、
3つの判定目的を先に定義する。影響は準備度表示と警告表示で、生成内容やCFEは直接変えない。

### E-7. LT信頼度の段差を検証する（重大度: 低）

事実。採用サンプルが1件でも用途ラベルが明示されていれば `medium`、用途不明でも2件あれば
`medium` になる。信頼度ラベルは結果画面に表示されるだけで、ラベル自体は設定生成やCFE更新の
分岐に使われない。ただし同じサンプルから算出したLT値そのものは有酸素設定に使われる。
実測数と用途ラベルの重みが、利用者へ示す確からしさとして妥当かを検証する。

### E-8. バックアップJSONのネスト深さを制限するか決める

ファイルサイズ上限内でも、極端に深くネストしたJSONは `JSON.parse` を失敗させうる。
単一利用者のローカルファイルに限られるリスクと、深さ制限パーサの複雑さを比較して決める。

### F-1. レースペース経済走に101〜103%帯を足す（実装済み・記録）

設定ペースのGRP比に **101〜103% の空白**があった。

| カテゴリ | GRP比 | 目標1:48.9での600m |
| --- | --- | --- |
| 高乳酸 | 95〜97% | 77.6〜79.2秒 |
| モデリング | 99〜100% | 80.9〜81.7秒（**分割走のみ**） |
| （空白） | 101〜103% | 82.5〜84.2秒 |
| 経済走 | 104〜106% | 84.9〜86.6秒 |

対象選手の制限因子は `limiter.ts` の判定で「後半の維持＝レースペース経済性」。
その帯を**連続走で**鍛える枠が無かった。モデリングは分割走なので、
つなぎを挟まずレースペース付近を保つ練習が存在しなかった。

判断した4点:

**(1) どのカテゴリに置くか → `race_economy` に足す。新カテゴリは作らない。**
刺激の性質が同じ（`middle_distance_specific`）で、104〜106%とは同じ軸の濃さ違い。
新カテゴリはフェーズ表・補強表・rationale・分類器・CFE換算比率・ルールエンジンの
全部に波及する。同じ軸の濃さ違いにその重さを払う理由がない。
制限因子の振り替え（M-7）が `category: "race_economy", weight: 1.3` で効いているので、
カテゴリを分けるとこの帯が振り替えの対象から外れるという実害もある。

**(2) `specificPace` の床1.04 → 下げない。レシピ側に比率の上書きを持たせる。**
床1.04は `economyProgressWeek` の**導入の到達点**（106%から入って週ごとに104%へ寄せる）
であって、経済走全体の下限ではない。ここを1.01まで下げると導入期の経済走が
週を追うだけで101%まで速くなる。導入は「レースペースに慣れる」ためのもので目的が違う。
レシピ側に持たせれば、どの処方が何%かがレシピを見れば分かる。

**(3) `recoveryDays` → 3から4へ。**
600m×2を101〜103%・完全回復6分で走るのは104〜106%より明確に深い。
モデリングと高乳酸が5なので、その中間。RULE-01の5日間隔とは別の値で、
配置間隔は直接決めない（`readiness` と候補選びに効く）。

**(4) 追加ではなく置換。**
同じ `race_economy` カテゴリの別レシピなので週の枠数は増えない。
`progressionStage` を既存より1つ上に置き、**104〜106%を2回うまく実施したら**
次の段として選ばれるようにした。カレンダーで勝手に進まない。

### F-2. 距離の漸進（400→500→600）を階段にするか

いまの `variationGroup`（short/medium/long）は**選手タイプで選ぶ選択肢**として
機能している（400=endurance / 500=balanced / 600=speed・lactate_tolerant）。
知識ベースの「質の拡張（Extension of Quality）」＝同じペースのまま距離を伸ばす、
を漸進軸にするなら、これを階段に組み替えることになる。

ただし組み替えると**選択の意味が変わる**（タイプに合った形式を選ぶ、が消える）。
どちらを取るかは競技的な判断なので、F-1とは別に決める。

決めること: 距離を階段にするのか、タイプ別の選択肢のまま「同じ距離で本数を増やす」
を質の拡張とみなすのか。後者なら `protectSpecificVolume` が本数を据え置いている
現状と矛盾するので、そこも合わせて見直す。
